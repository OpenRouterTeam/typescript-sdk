/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b9a0f03a45a0
 */

import { OpenRouterCore } from "../core.js";
import { encodeFormQuery, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { resolveSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { OpenRouterError } from "../models/errors/openroutererror.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";
import {
  createPageIterator,
  haltIterator,
  PageIterator,
  Paginator,
} from "../types/operations.js";

/**
 * List models allowed by the API key's workspace
 *
 * @remarks
 * List models filtered by the provider preferences, privacy settings, and guardrails associated with the API key's workspace.
 */
export function modelsListForWorkspace(
  client: OpenRouterCore,
  security: operations.ListModelsWorkspaceSecurity,
  request?: operations.ListModelsWorkspaceRequest | undefined,
  options?: RequestOptions,
): APIPromise<
  PageIterator<
    Result<
      operations.ListModelsWorkspaceResponse,
      | errors.BadRequestResponseError
      | errors.UnauthorizedResponseError
      | errors.InternalServerResponseError
      | OpenRouterError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    { offset: number }
  >
> {
  return new APIPromise($do(
    client,
    security,
    request,
    options,
  ));
}

async function $do(
  client: OpenRouterCore,
  security: operations.ListModelsWorkspaceSecurity,
  request?: operations.ListModelsWorkspaceRequest | undefined,
  options?: RequestOptions,
): Promise<
  [
    PageIterator<
      Result<
        operations.ListModelsWorkspaceResponse,
        | errors.BadRequestResponseError
        | errors.UnauthorizedResponseError
        | errors.InternalServerResponseError
        | OpenRouterError
        | ResponseValidationError
        | ConnectionError
        | RequestAbortedError
        | RequestTimeoutError
        | InvalidRequestError
        | UnexpectedClientError
        | SDKValidationError
      >,
      { offset: number }
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      operations.ListModelsWorkspaceRequest$outboundSchema.optional().parse(
        value,
      ),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [haltIterator(parsed), { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const path = pathToFunc("/models/workspace")();

  const query = encodeFormQuery({
    "arch": payload?.arch,
    "category": payload?.category,
    "context": payload?.context,
    "distillable": payload?.distillable,
    "input_modalities": payload?.input_modalities,
    "limit": payload?.limit,
    "max_age_days": payload?.max_age_days,
    "max_agentic_index": payload?.max_agentic_index,
    "max_coding_index": payload?.max_coding_index,
    "max_intelligence_index": payload?.max_intelligence_index,
    "max_output_price": payload?.max_output_price,
    "max_price": payload?.max_price,
    "max_tool_success_rate": payload?.max_tool_success_rate,
    "min_age_days": payload?.min_age_days,
    "min_agentic_index": payload?.min_agentic_index,
    "min_coding_index": payload?.min_coding_index,
    "min_intelligence_index": payload?.min_intelligence_index,
    "min_output_price": payload?.min_output_price,
    "min_price": payload?.min_price,
    "min_tool_success_rate": payload?.min_tool_success_rate,
    "model_authors": payload?.model_authors,
    "offset": payload?.offset,
    "output_modalities": payload?.output_modalities,
    "providers": payload?.providers,
    "q": payload?.q,
    "region": payload?.region,
    "sort": payload?.sort,
    "supported_parameters": payload?.supported_parameters,
    "zdr": payload?.zdr,
  });

  const headers = new Headers(compactMap({
    Accept: "application/json",
    "HTTP-Referer": encodeSimple(
      "HTTP-Referer",
      payload?.["HTTP-Referer"] ?? client._options.httpReferer,
      { explode: false, charEncoding: "none" },
    ),
    "X-OpenRouter-Categories": encodeSimple(
      "X-OpenRouter-Categories",
      payload?.appCategories ?? client._options.appCategories,
      { explode: false, charEncoding: "none" },
    ),
    "X-OpenRouter-Title": encodeSimple(
      "X-OpenRouter-Title",
      payload?.appTitle ?? client._options.appTitle,
      { explode: false, charEncoding: "none" },
    ),
  }));

  const requestSecurity = resolveSecurity(
    [
      {
        fieldName: "Authorization",
        type: "http:bearer",
        value: security?.bearer,
      },
    ],
  );

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "listModelsWorkspace",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || {
        strategy: "backoff",
        backoff: {
          initialInterval: 500,
          maxInterval: 60000,
          exponent: 1.5,
          maxElapsedTime: 3600000,
        },
        retryConnectionErrors: true,
      }
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["5XX"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [haltIterator(requestRes), { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [haltIterator(doResult), { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result, raw] = await M.match<
    operations.ListModelsWorkspaceResponse,
    | errors.BadRequestResponseError
    | errors.UnauthorizedResponseError
    | errors.InternalServerResponseError
    | OpenRouterError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, operations.ListModelsWorkspaceResponse$inboundSchema, {
      key: "Result",
    }),
    M.jsonErr(400, errors.BadRequestResponseError$inboundSchema),
    M.jsonErr(401, errors.UnauthorizedResponseError$inboundSchema),
    M.jsonErr(500, errors.InternalServerResponseError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [haltIterator(result), {
      status: "complete",
      request: req,
      response,
    }];
  }

  const nextFunc = (
    responseData: unknown,
  ): {
    next: Paginator<
      Result<
        operations.ListModelsWorkspaceResponse,
        | errors.BadRequestResponseError
        | errors.UnauthorizedResponseError
        | errors.InternalServerResponseError
        | OpenRouterError
        | ResponseValidationError
        | ConnectionError
        | RequestAbortedError
        | RequestTimeoutError
        | InvalidRequestError
        | UnexpectedClientError
        | SDKValidationError
      >
    >;
    "~next"?: { offset: number };
  } => {
    const offset = request?.offset ?? 0;

    if (!responseData) {
      return { next: () => null };
    }
    const results = (responseData as { data: unknown }).data;
    if (!Array.isArray(results) || !results.length) {
      return { next: () => null };
    }
    const limit = request?.limit ?? 500;
    if (results.length < limit) {
      return { next: () => null };
    }
    const nextOffset = offset + results.length;

    const nextVal = () =>
      modelsListForWorkspace(
        client,
        security,
        {
          ...request!,
          offset: nextOffset,
        },
        options,
      );

    return { next: nextVal, "~next": { offset: nextOffset } };
  };

  const page = { ...result, ...nextFunc(raw) };
  return [{ ...page, ...createPageIterator(page, (v) => !v.ok) }, {
    status: "complete",
    request: req,
    response,
  }];
}
