/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a103cd018c9b
 */

import { agentsCreate } from "../funcs/agentsCreate.js";
import { agentsDelete } from "../funcs/agentsDelete.js";
import { agentsGet } from "../funcs/agentsGet.js";
import { agentsList } from "../funcs/agentsList.js";
import { agentsSendMessage } from "../funcs/agentsSendMessage.js";
import { agentsUpdate } from "../funcs/agentsUpdate.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as models from "../models/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";

export class Agents extends ClientSDK {
  /**
   * List agents
   *
   * @remarks
   * Returns the agents the caller can see — owner-scoped by default. Use `owner=public` to also return first-party / public agents.
   */
  async list(
    request?: operations.ListAgentsRequest | undefined,
    options?: RequestOptions,
  ): Promise<PageIterator<operations.ListAgentsResponse, { cursor: string }>> {
    return unwrapResultIterator(agentsList(
      this,
      request,
      options,
    ));
  }

  /**
   * Create a user-defined agent
   *
   * @remarks
   * Registers a new agent under the calling user / org. Bring-your-own cron schedule, custom code hook, tool list, and natural-language instructions. The agent is dispatched through one of the underlying provider adapters at invoke time. Agents created here show up in the same models / agents pages as first-party agents.
   */
  async create(
    request: operations.CreateAgentRequest,
    options?: RequestOptions,
  ): Promise<models.AgentResponse> {
    return unwrapAsync(agentsCreate(
      this,
      request,
      options,
    ));
  }

  /**
   * Delete an agent
   *
   * @remarks
   * Soft-deletes an agent. Existing sessions stay accessible by id, but the agent disappears from list and is no longer schedulable.
   */
  async delete(
    request: operations.DeleteAgentRequest,
    options?: RequestOptions,
  ): Promise<models.DeleteAgentResponse> {
    return unwrapAsync(agentsDelete(
      this,
      request,
      options,
    ));
  }

  /**
   * Get an agent
   *
   * @remarks
   * Returns a single agent by id.
   */
  async get(
    request: operations.GetAgentRequest,
    options?: RequestOptions,
  ): Promise<models.AgentResponse> {
    return unwrapAsync(agentsGet(
      this,
      request,
      options,
    ));
  }

  /**
   * Update an agent
   *
   * @remarks
   * Partially updates an agent. Only the fields present in the body are modified — omit fields to leave them unchanged.
   */
  async update(
    request: operations.UpdateAgentRequest,
    options?: RequestOptions,
  ): Promise<models.AgentResponse> {
    return unwrapAsync(agentsUpdate(
      this,
      request,
      options,
    ));
  }

  /**
   * Talk to an agent to mutate its behavior
   *
   * @remarks
   * Appends a message to the agent's configuration thread. The platform interprets the message and rewrites the agent's instructions, schedule, or tools as appropriate. Use this to evolve an agent over time without manually editing the structured config.
   */
  async sendMessage(
    request: operations.SendAgentMessageRequest,
    options?: RequestOptions,
  ): Promise<models.SendAgentMessageResponse> {
    return unwrapAsync(agentsSendMessage(
      this,
      request,
      options,
    ));
  }
}
