/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 72ce7b70fba8
 */

import { benchmarksGetBenchmarks } from "../funcs/benchmarksGetBenchmarks.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as models from "../models/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class Benchmarks extends ClientSDK {
  /**
   * List Benchmarks
   *
   * @remarks
   * Unified benchmark endpoint that aggregates scores from multiple benchmark sources (Artificial Analysis, Design Arena, and OpenRouter's own tau-bench, GPQA, and web-search evals). Filter by source to reproduce the exact shapes from the legacy per-source endpoints, or use task_type to find models suited for specific workloads. Use task_type=search (or a search_* benchmark_type) for OpenRouter's search benchmark suites, which publish each model's median evaluation configuration, with same-configuration runs combined by task-weighted mean. Authenticate with any valid OpenRouter API key. Rate-limited to 30 requests/minute per key and 500 requests/day per account.
   */
  async getBenchmarks(
    request?: operations.GetBenchmarksRequest | undefined,
    options?: RequestOptions,
  ): Promise<models.UnifiedBenchmarksResponse> {
    return unwrapAsync(benchmarksGetBenchmarks(
      this,
      request,
      options,
    ));
  }
}
