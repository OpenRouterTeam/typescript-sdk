/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * Video input content item
 */
export type InputVideo = {
  type: "input_video";
  /**
   * A base64 data URL or remote URL that resolves to a video file
   */
  videoUrl: string;
};

/** @internal */
export type InputVideo$Outbound = {
  type: "input_video";
  video_url: string;
};

/** @internal */
export const InputVideo$outboundSchema: z.ZodType<
  InputVideo$Outbound,
  InputVideo
> = z.object({
  type: z.literal("input_video"),
  videoUrl: z.string(),
}).transform((v) => {
  return remap$(v, {
    videoUrl: "video_url",
  });
});

export function inputVideoToJSON(inputVideo: InputVideo): string {
  return JSON.stringify(InputVideo$outboundSchema.parse(inputVideo));
}
