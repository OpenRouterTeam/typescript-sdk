/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6d79794dae8e
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { OpenAIFile, OpenAIFile$inboundSchema } from "./openaifile.js";

export const OpenAIFileListObject = {
  List: "list",
} as const;
export type OpenAIFileListObject = ClosedEnum<typeof OpenAIFileListObject>;

/**
 * A page of files in OpenAI's shape.
 */
export type OpenAIFileList = {
  shape: "openai";
  data: Array<OpenAIFile>;
  firstId: string | null;
  hasMore: boolean;
  lastId: string | null;
  object: OpenAIFileListObject;
};

/** @internal */
export const OpenAIFileListObject$inboundSchema: z.ZodEnum<
  typeof OpenAIFileListObject
> = z.enum(OpenAIFileListObject);

/** @internal */
export const OpenAIFileList$inboundSchema: z.ZodType<OpenAIFileList, unknown> =
  z.object({
    _shape: z.literal("openai"),
    data: z.array(OpenAIFile$inboundSchema),
    first_id: z.nullable(z.string()),
    has_more: z.boolean(),
    last_id: z.nullable(z.string()),
    object: OpenAIFileListObject$inboundSchema,
  }).transform((v) => {
    return remap$(v, {
      "_shape": "shape",
      "first_id": "firstId",
      "has_more": "hasMore",
      "last_id": "lastId",
    });
  });

export function openAIFileListFromJSON(
  jsonString: string,
): SafeParseResult<OpenAIFileList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OpenAIFileList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OpenAIFileList' from JSON`,
  );
}
