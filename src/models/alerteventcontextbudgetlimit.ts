/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5a82cec516f7
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * The stable budget reset interval: daily, weekly, monthly, or lifetime.
 */
export const Interval = {
  Daily: "daily",
  Weekly: "weekly",
  Monthly: "monthly",
  Lifetime: "lifetime",
} as const;
/**
 * The stable budget reset interval: daily, weekly, monthly, or lifetime.
 */
export type Interval = OpenEnum<typeof Interval>;

/**
 * PII-free context for a budget-limit alert event.
 */
export type AlertEventContextBudgetLimit = {
  /**
   * The stable budget reset interval: daily, weekly, monthly, or lifetime.
   */
  interval: Interval;
  /**
   * A workspace budget limit alert.
   */
  kind: "budget-limit";
  /**
   * The budget limit in US dollars.
   */
  limitUsd: number;
  /**
   * The spend counted against the budget, in US dollars.
   */
  spend: number;
  /**
   * The budget utilization threshold as a percent from 0–100.
   */
  threshold: number;
  /**
   * The workspace whose budget produced this alert.
   */
  workspaceId: string;
  /**
   * The workspace name used when rendering this alert.
   */
  workspaceName: string;
};

/** @internal */
export const Interval$inboundSchema: z.ZodType<Interval, unknown> = openEnums
  .inboundSchema(Interval);
/** @internal */
export const Interval$outboundSchema: z.ZodType<string, Interval> = openEnums
  .outboundSchema(Interval);

/** @internal */
export const AlertEventContextBudgetLimit$inboundSchema: z.ZodType<
  AlertEventContextBudgetLimit,
  unknown
> = z.object({
  interval: Interval$inboundSchema,
  kind: z.literal("budget-limit"),
  limitUsd: z.number(),
  spend: z.number(),
  threshold: z.number(),
  workspaceId: z.string(),
  workspaceName: z.string(),
});
/** @internal */
export type AlertEventContextBudgetLimit$Outbound = {
  interval: string;
  kind: "budget-limit";
  limitUsd: number;
  spend: number;
  threshold: number;
  workspaceId: string;
  workspaceName: string;
};

/** @internal */
export const AlertEventContextBudgetLimit$outboundSchema: z.ZodType<
  AlertEventContextBudgetLimit$Outbound,
  AlertEventContextBudgetLimit
> = z.object({
  interval: Interval$outboundSchema,
  kind: z.literal("budget-limit"),
  limitUsd: z.number(),
  spend: z.number(),
  threshold: z.number(),
  workspaceId: z.string(),
  workspaceName: z.string(),
});

export function alertEventContextBudgetLimitToJSON(
  alertEventContextBudgetLimit: AlertEventContextBudgetLimit,
): string {
  return JSON.stringify(
    AlertEventContextBudgetLimit$outboundSchema.parse(
      alertEventContextBudgetLimit,
    ),
  );
}
export function alertEventContextBudgetLimitFromJSON(
  jsonString: string,
): SafeParseResult<AlertEventContextBudgetLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AlertEventContextBudgetLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AlertEventContextBudgetLimit' from JSON`,
  );
}
