/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Event emitted when a refusal delta is streamed
 */
export type RefusalDeltaEvent = {
  type: "response.refusal.delta";
  outputIndex: number;
  itemId: string;
  contentIndex: number;
  delta: string;
  sequenceNumber: number;
};

/** @internal */
export const RefusalDeltaEvent$inboundSchema: z.ZodType<
  RefusalDeltaEvent,
  unknown
> = z.object({
  type: z.literal("response.refusal.delta"),
  output_index: z.number(),
  item_id: z.string(),
  content_index: z.number(),
  delta: z.string(),
  sequence_number: z.number(),
}).transform((v) => {
  return remap$(v, {
    "output_index": "outputIndex",
    "item_id": "itemId",
    "content_index": "contentIndex",
    "sequence_number": "sequenceNumber",
  });
});

export function refusalDeltaEventFromJSON(
  jsonString: string,
): SafeParseResult<RefusalDeltaEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RefusalDeltaEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RefusalDeltaEvent' from JSON`,
  );
}
