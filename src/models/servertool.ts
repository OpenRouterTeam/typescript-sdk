/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 51581c50c8ce
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * An OpenRouter built-in server tool that models can call during a request
 */
export type ServerTool = {
  /**
   * Human-readable description of the tool
   */
  description: string;
  /**
   * JSON Schema for the optional parameters object accepted by this tool
   */
  parametersSchema?: { [k: string]: any | null } | null | undefined;
  /**
   * The tool type identifier used in the tools array of a request
   */
  type: string;
};

/** @internal */
export const ServerTool$inboundSchema: z.ZodType<ServerTool, unknown> = z
  .object({
    description: z.string(),
    parameters_schema: z.nullable(z.record(z.string(), z.nullable(z.any())))
      .optional(),
    type: z.string(),
  }).transform((v) => {
    return remap$(v, {
      "parameters_schema": "parametersSchema",
    });
  });

export function serverToolFromJSON(
  jsonString: string,
): SafeParseResult<ServerTool, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ServerTool$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ServerTool' from JSON`,
  );
}
