/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 82b5c9f01e00
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  BenchmarkPricing,
  BenchmarkPricing$inboundSchema,
} from "./benchmarkpricing.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Percentile rankings across all evaluated models.
 */
export type Percentiles = {
  /**
   * Percentile rank for the agentic index (0–100).
   */
  agentic: number | null;
  /**
   * Percentile rank for the coding index (0–100).
   */
  coding: number | null;
  /**
   * Percentile rank for the intelligence index (0–100).
   */
  intelligence: number | null;
};

/**
 * Composite benchmark index scores.
 */
export type Scores = {
  /**
   * Artificial Analysis Agentic Index score (0–1 scale).
   */
  agentic: number | null;
  /**
   * Artificial Analysis Coding Index score (0–1 scale).
   */
  coding: number | null;
  /**
   * Artificial Analysis Intelligence Index score (0–1 scale).
   */
  intelligence: number | null;
};

export type BenchmarksAAItem = {
  /**
   * Stable OpenRouter model identifier (e.g. `anthropic/claude-sonnet-4`).
   */
  modelPermaslug: string;
  /**
   * Human-readable model name from Artificial Analysis.
   */
  name: string;
  /**
   * Percentile rankings across all evaluated models.
   */
  percentiles: Percentiles;
  /**
   * OpenRouter pricing per token for this model. Null if pricing is unavailable.
   */
  pricing: BenchmarkPricing | null;
  /**
   * Composite benchmark index scores.
   */
  scores: Scores;
};

/** @internal */
export const Percentiles$inboundSchema: z.ZodType<Percentiles, unknown> = z
  .object({
    agentic: z.nullable(z.number()),
    coding: z.nullable(z.number()),
    intelligence: z.nullable(z.number()),
  });

export function percentilesFromJSON(
  jsonString: string,
): SafeParseResult<Percentiles, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Percentiles$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Percentiles' from JSON`,
  );
}

/** @internal */
export const Scores$inboundSchema: z.ZodType<Scores, unknown> = z.object({
  agentic: z.nullable(z.number()),
  coding: z.nullable(z.number()),
  intelligence: z.nullable(z.number()),
});

export function scoresFromJSON(
  jsonString: string,
): SafeParseResult<Scores, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Scores$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Scores' from JSON`,
  );
}

/** @internal */
export const BenchmarksAAItem$inboundSchema: z.ZodType<
  BenchmarksAAItem,
  unknown
> = z.object({
  model_permaslug: z.string(),
  name: z.string(),
  percentiles: z.lazy(() => Percentiles$inboundSchema),
  pricing: z.nullable(BenchmarkPricing$inboundSchema),
  scores: z.lazy(() => Scores$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "model_permaslug": "modelPermaslug",
  });
});

export function benchmarksAAItemFromJSON(
  jsonString: string,
): SafeParseResult<BenchmarksAAItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BenchmarksAAItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BenchmarksAAItem' from JSON`,
  );
}
