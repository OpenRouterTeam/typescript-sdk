/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f1edf3b92523
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import * as discriminatedUnionTypes from "../types/discriminatedUnion.js";
import { discriminatedUnion } from "../types/discriminatedUnion.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { AnthropicFile, AnthropicFile$inboundSchema } from "./anthropicfile.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { OpenAIFile, OpenAIFile$inboundSchema } from "./openaifile.js";
import {
  OpenRouterFile,
  OpenRouterFile$inboundSchema,
} from "./openrouterfile.js";

/**
 * A stored file. The shape is negotiated per request — see the endpoint description.
 */
export type FileResponse =
  | (AnthropicFile & { shape: "anthropic" })
  | (OpenAIFile & { shape: "openai" })
  | (OpenRouterFile & { shape: "openrouter" })
  | discriminatedUnionTypes.Unknown<"shape">;

/** @internal */
export const FileResponse$inboundSchema: z.ZodType<FileResponse, unknown> =
  discriminatedUnion("_shape", {
    anthropic: AnthropicFile$inboundSchema.and(
      z.object({ _shape: z.literal("anthropic") }).transform((v) => ({
        shape: v._shape,
      })),
    ),
    openai: OpenAIFile$inboundSchema.and(
      z.object({ _shape: z.literal("openai") }).transform((v) => ({
        shape: v._shape,
      })),
    ),
    openrouter: OpenRouterFile$inboundSchema.and(
      z.object({ _shape: z.literal("openrouter") }).transform((v) => ({
        shape: v._shape,
      })),
    ),
  }, { outputPropertyName: "shape" });

export function fileResponseFromJSON(
  jsonString: string,
): SafeParseResult<FileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FileResponse' from JSON`,
  );
}
