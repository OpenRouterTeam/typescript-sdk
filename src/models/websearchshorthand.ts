/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 20e3c2bd41b7
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import * as openEnums from "../types/enums.js";
import { ClosedEnum, OpenEnum } from "../types/enums.js";

export const WebSearchShorthandType = {
  WebSearch: "web_search",
  WebSearchPreview: "web_search_preview",
  WebSearchPreview20250311: "web_search_preview_2025_03_11",
  WebSearch20250826: "web_search_2025_08_26",
} as const;
export type WebSearchShorthandType = OpenEnum<typeof WebSearchShorthandType>;

/**
 * Which search engine to use. "auto" (default) uses native if the provider supports it, otherwise Exa. "native" forces the provider's built-in search (parameters like max_results, search_context_size, and domain filters are not forwarded to the provider). "exa" forces the Exa search API.
 */
export const WebSearchShorthandEngine = {
  Auto: "auto",
  Native: "native",
  Exa: "exa",
} as const;
/**
 * Which search engine to use. "auto" (default) uses native if the provider supports it, otherwise Exa. "native" forces the provider's built-in search (parameters like max_results, search_context_size, and domain filters are not forwarded to the provider). "exa" forces the Exa search API.
 */
export type WebSearchShorthandEngine = OpenEnum<
  typeof WebSearchShorthandEngine
>;

/**
 * How much context to retrieve per result. Defaults to medium (15000 chars). Only applies when using the Exa engine; ignored with native provider search.
 */
export const WebSearchShorthandSearchContextSize = {
  Low: "low",
  Medium: "medium",
  High: "high",
} as const;
/**
 * How much context to retrieve per result. Defaults to medium (15000 chars). Only applies when using the Exa engine; ignored with native provider search.
 */
export type WebSearchShorthandSearchContextSize = OpenEnum<
  typeof WebSearchShorthandSearchContextSize
>;

export const WebSearchShorthandTypeApproximate = {
  Approximate: "approximate",
} as const;
export type WebSearchShorthandTypeApproximate = ClosedEnum<
  typeof WebSearchShorthandTypeApproximate
>;

/**
 * Approximate user location for location-biased results.
 */
export type WebSearchShorthandUserLocation = {
  type?: WebSearchShorthandTypeApproximate | undefined;
  city?: string | undefined;
  region?: string | undefined;
  country?: string | undefined;
  timezone?: string | undefined;
};

/**
 * Which search engine to use. "auto" (default) uses native if the provider supports it, otherwise Exa. "native" forces the provider's built-in search (parameters like max_results, search_context_size, and domain filters are not forwarded to the provider). "exa" forces the Exa search API.
 */
export const WebSearchShorthandParametersEngine = {
  Auto: "auto",
  Native: "native",
  Exa: "exa",
} as const;
/**
 * Which search engine to use. "auto" (default) uses native if the provider supports it, otherwise Exa. "native" forces the provider's built-in search (parameters like max_results, search_context_size, and domain filters are not forwarded to the provider). "exa" forces the Exa search API.
 */
export type WebSearchShorthandParametersEngine = OpenEnum<
  typeof WebSearchShorthandParametersEngine
>;

/**
 * How much context to retrieve per result. Defaults to medium (15000 chars). Only applies when using the Exa engine; ignored with native provider search.
 */
export const WebSearchShorthandParametersSearchContextSize = {
  Low: "low",
  Medium: "medium",
  High: "high",
} as const;
/**
 * How much context to retrieve per result. Defaults to medium (15000 chars). Only applies when using the Exa engine; ignored with native provider search.
 */
export type WebSearchShorthandParametersSearchContextSize = OpenEnum<
  typeof WebSearchShorthandParametersSearchContextSize
>;

export const WebSearchShorthandParametersType = {
  Approximate: "approximate",
} as const;
export type WebSearchShorthandParametersType = ClosedEnum<
  typeof WebSearchShorthandParametersType
>;

/**
 * Approximate user location for location-biased results.
 */
export type WebSearchShorthandParametersUserLocation = {
  type?: WebSearchShorthandParametersType | undefined;
  city?: string | undefined;
  region?: string | undefined;
  country?: string | undefined;
  timezone?: string | undefined;
};

export type WebSearchShorthandParameters = {
  /**
   * Which search engine to use. "auto" (default) uses native if the provider supports it, otherwise Exa. "native" forces the provider's built-in search (parameters like max_results, search_context_size, and domain filters are not forwarded to the provider). "exa" forces the Exa search API.
   */
  engine?: WebSearchShorthandParametersEngine | undefined;
  /**
   * Maximum number of search results to return per search call. Defaults to 5. Only applies when using the Exa engine; ignored with native provider search.
   */
  maxResults?: number | undefined;
  /**
   * Maximum total number of search results across all search calls in a single request. Once this limit is reached, the tool will stop returning new results. Useful for controlling cost and context size in agentic loops.
   */
  maxTotalResults?: number | undefined;
  /**
   * How much context to retrieve per result. Defaults to medium (15000 chars). Only applies when using the Exa engine; ignored with native provider search.
   */
  searchContextSize?: WebSearchShorthandParametersSearchContextSize | undefined;
  /**
   * Approximate user location for location-biased results.
   */
  userLocation?: WebSearchShorthandParametersUserLocation | undefined;
  /**
   * Limit search results to these domains. Only applies when using the Exa engine; ignored with native provider search.
   */
  allowedDomains?: Array<string> | undefined;
  /**
   * Exclude search results from these domains. Only applies when using the Exa engine; ignored with native provider search.
   */
  excludedDomains?: Array<string> | undefined;
};

/**
 * Web search tool using OpenAI Responses API syntax. Automatically converted to openrouter:web_search.
 */
export type WebSearchShorthand = {
  type: WebSearchShorthandType;
  /**
   * Which search engine to use. "auto" (default) uses native if the provider supports it, otherwise Exa. "native" forces the provider's built-in search (parameters like max_results, search_context_size, and domain filters are not forwarded to the provider). "exa" forces the Exa search API.
   */
  engine?: WebSearchShorthandEngine | undefined;
  /**
   * Maximum number of search results to return per search call. Defaults to 5. Only applies when using the Exa engine; ignored with native provider search.
   */
  maxResults?: number | undefined;
  /**
   * Maximum total number of search results across all search calls in a single request. Once this limit is reached, the tool will stop returning new results. Useful for controlling cost and context size in agentic loops.
   */
  maxTotalResults?: number | undefined;
  /**
   * How much context to retrieve per result. Defaults to medium (15000 chars). Only applies when using the Exa engine; ignored with native provider search.
   */
  searchContextSize?: WebSearchShorthandSearchContextSize | undefined;
  /**
   * Approximate user location for location-biased results.
   */
  userLocation?: WebSearchShorthandUserLocation | undefined;
  /**
   * Limit search results to these domains. Only applies when using the Exa engine; ignored with native provider search.
   */
  allowedDomains?: Array<string> | undefined;
  /**
   * Exclude search results from these domains. Only applies when using the Exa engine; ignored with native provider search.
   */
  excludedDomains?: Array<string> | undefined;
  parameters?: WebSearchShorthandParameters | undefined;
};

/** @internal */
export const WebSearchShorthandType$outboundSchema: z.ZodType<
  string,
  WebSearchShorthandType
> = openEnums.outboundSchema(WebSearchShorthandType);

/** @internal */
export const WebSearchShorthandEngine$outboundSchema: z.ZodType<
  string,
  WebSearchShorthandEngine
> = openEnums.outboundSchema(WebSearchShorthandEngine);

/** @internal */
export const WebSearchShorthandSearchContextSize$outboundSchema: z.ZodType<
  string,
  WebSearchShorthandSearchContextSize
> = openEnums.outboundSchema(WebSearchShorthandSearchContextSize);

/** @internal */
export const WebSearchShorthandTypeApproximate$outboundSchema: z.ZodEnum<
  typeof WebSearchShorthandTypeApproximate
> = z.enum(WebSearchShorthandTypeApproximate);

/** @internal */
export type WebSearchShorthandUserLocation$Outbound = {
  type?: string | undefined;
  city?: string | undefined;
  region?: string | undefined;
  country?: string | undefined;
  timezone?: string | undefined;
};

/** @internal */
export const WebSearchShorthandUserLocation$outboundSchema: z.ZodType<
  WebSearchShorthandUserLocation$Outbound,
  WebSearchShorthandUserLocation
> = z.object({
  type: WebSearchShorthandTypeApproximate$outboundSchema.optional(),
  city: z.string().optional(),
  region: z.string().optional(),
  country: z.string().optional(),
  timezone: z.string().optional(),
});

export function webSearchShorthandUserLocationToJSON(
  webSearchShorthandUserLocation: WebSearchShorthandUserLocation,
): string {
  return JSON.stringify(
    WebSearchShorthandUserLocation$outboundSchema.parse(
      webSearchShorthandUserLocation,
    ),
  );
}

/** @internal */
export const WebSearchShorthandParametersEngine$outboundSchema: z.ZodType<
  string,
  WebSearchShorthandParametersEngine
> = openEnums.outboundSchema(WebSearchShorthandParametersEngine);

/** @internal */
export const WebSearchShorthandParametersSearchContextSize$outboundSchema:
  z.ZodType<string, WebSearchShorthandParametersSearchContextSize> = openEnums
    .outboundSchema(WebSearchShorthandParametersSearchContextSize);

/** @internal */
export const WebSearchShorthandParametersType$outboundSchema: z.ZodEnum<
  typeof WebSearchShorthandParametersType
> = z.enum(WebSearchShorthandParametersType);

/** @internal */
export type WebSearchShorthandParametersUserLocation$Outbound = {
  type?: string | undefined;
  city?: string | undefined;
  region?: string | undefined;
  country?: string | undefined;
  timezone?: string | undefined;
};

/** @internal */
export const WebSearchShorthandParametersUserLocation$outboundSchema: z.ZodType<
  WebSearchShorthandParametersUserLocation$Outbound,
  WebSearchShorthandParametersUserLocation
> = z.object({
  type: WebSearchShorthandParametersType$outboundSchema.optional(),
  city: z.string().optional(),
  region: z.string().optional(),
  country: z.string().optional(),
  timezone: z.string().optional(),
});

export function webSearchShorthandParametersUserLocationToJSON(
  webSearchShorthandParametersUserLocation:
    WebSearchShorthandParametersUserLocation,
): string {
  return JSON.stringify(
    WebSearchShorthandParametersUserLocation$outboundSchema.parse(
      webSearchShorthandParametersUserLocation,
    ),
  );
}

/** @internal */
export type WebSearchShorthandParameters$Outbound = {
  engine?: string | undefined;
  max_results?: number | undefined;
  max_total_results?: number | undefined;
  search_context_size?: string | undefined;
  user_location?: WebSearchShorthandParametersUserLocation$Outbound | undefined;
  allowed_domains?: Array<string> | undefined;
  excluded_domains?: Array<string> | undefined;
};

/** @internal */
export const WebSearchShorthandParameters$outboundSchema: z.ZodType<
  WebSearchShorthandParameters$Outbound,
  WebSearchShorthandParameters
> = z.object({
  engine: WebSearchShorthandParametersEngine$outboundSchema.optional(),
  maxResults: z.number().optional(),
  maxTotalResults: z.number().optional(),
  searchContextSize:
    WebSearchShorthandParametersSearchContextSize$outboundSchema.optional(),
  userLocation: z.lazy(() =>
    WebSearchShorthandParametersUserLocation$outboundSchema
  ).optional(),
  allowedDomains: z.array(z.string()).optional(),
  excludedDomains: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    maxResults: "max_results",
    maxTotalResults: "max_total_results",
    searchContextSize: "search_context_size",
    userLocation: "user_location",
    allowedDomains: "allowed_domains",
    excludedDomains: "excluded_domains",
  });
});

export function webSearchShorthandParametersToJSON(
  webSearchShorthandParameters: WebSearchShorthandParameters,
): string {
  return JSON.stringify(
    WebSearchShorthandParameters$outboundSchema.parse(
      webSearchShorthandParameters,
    ),
  );
}

/** @internal */
export type WebSearchShorthand$Outbound = {
  type: string;
  engine?: string | undefined;
  max_results?: number | undefined;
  max_total_results?: number | undefined;
  search_context_size?: string | undefined;
  user_location?: WebSearchShorthandUserLocation$Outbound | undefined;
  allowed_domains?: Array<string> | undefined;
  excluded_domains?: Array<string> | undefined;
  parameters?: WebSearchShorthandParameters$Outbound | undefined;
};

/** @internal */
export const WebSearchShorthand$outboundSchema: z.ZodType<
  WebSearchShorthand$Outbound,
  WebSearchShorthand
> = z.object({
  type: WebSearchShorthandType$outboundSchema,
  engine: WebSearchShorthandEngine$outboundSchema.optional(),
  maxResults: z.number().optional(),
  maxTotalResults: z.number().optional(),
  searchContextSize: WebSearchShorthandSearchContextSize$outboundSchema
    .optional(),
  userLocation: z.lazy(() => WebSearchShorthandUserLocation$outboundSchema)
    .optional(),
  allowedDomains: z.array(z.string()).optional(),
  excludedDomains: z.array(z.string()).optional(),
  parameters: z.lazy(() => WebSearchShorthandParameters$outboundSchema)
    .optional(),
}).transform((v) => {
  return remap$(v, {
    maxResults: "max_results",
    maxTotalResults: "max_total_results",
    searchContextSize: "search_context_size",
    userLocation: "user_location",
    allowedDomains: "allowed_domains",
    excludedDomains: "excluded_domains",
  });
});

export function webSearchShorthandToJSON(
  webSearchShorthand: WebSearchShorthand,
): string {
  return JSON.stringify(
    WebSearchShorthand$outboundSchema.parse(webSearchShorthand),
  );
}
