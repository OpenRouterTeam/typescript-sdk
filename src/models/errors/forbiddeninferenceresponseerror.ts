/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9e3112a39dce
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import * as models from "../index.js";
import { OpenRouterError } from "./openroutererror.js";

/**
 * Forbidden - Access denied on an inference endpoint. Causes include insufficient permissions, a guardrail block (with `openrouter_metadata.pipeline` stage details when the metadata header is opted in), moderation flags, or other access restrictions.
 */
export type ForbiddenInferenceResponseErrorData = {
  /**
   * Error data for ForbiddenInferenceResponse
   */
  error: models.ForbiddenInferenceResponseErrorData;
  /**
   * Opt-in routing snapshot. Present when the request includes `X-OpenRouter-Experimental-Metadata: enabled` and the failure occurs after the router has captured usable state. Same shape as `openrouter_metadata` on successful responses — includes routing strategy, endpoint info, and pipeline guardrail stages.
   */
  openrouterMetadata?: { [k: string]: any | null } | null | undefined;
  userId?: string | null | undefined;
};

/**
 * Forbidden - Access denied on an inference endpoint. Causes include insufficient permissions, a guardrail block (with `openrouter_metadata.pipeline` stage details when the metadata header is opted in), moderation flags, or other access restrictions.
 */
export class ForbiddenInferenceResponseError extends OpenRouterError {
  /**
   * Error data for ForbiddenInferenceResponse
   */
  error: models.ForbiddenInferenceResponseErrorData;
  /**
   * Opt-in routing snapshot. Present when the request includes `X-OpenRouter-Experimental-Metadata: enabled` and the failure occurs after the router has captured usable state. Same shape as `openrouter_metadata` on successful responses — includes routing strategy, endpoint info, and pipeline guardrail stages.
   */
  openrouterMetadata?: { [k: string]: any | null } | null | undefined;
  userId?: string | null | undefined;

  /** The original data that was passed to this error instance. */
  data$: ForbiddenInferenceResponseErrorData;

  constructor(
    err: ForbiddenInferenceResponseErrorData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.error?.message
      || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    this.error = err.error;
    if (err.openrouterMetadata != null) {
      this.openrouterMetadata = err.openrouterMetadata;
    }
    if (err.userId != null) this.userId = err.userId;

    this.name = "ForbiddenInferenceResponseError";
  }
}

/** @internal */
export const ForbiddenInferenceResponseError$inboundSchema: z.ZodType<
  ForbiddenInferenceResponseError,
  unknown
> = z.object({
  error: models.ForbiddenInferenceResponseErrorData$inboundSchema,
  openrouter_metadata: z.nullable(z.record(z.string(), z.nullable(z.any())))
    .optional(),
  user_id: z.nullable(z.string()).optional(),
  request$: z.custom<Request>(x => x instanceof Request),
  response$: z.custom<Response>(x => x instanceof Response),
  body$: z.string(),
})
  .transform((v) => {
    const remapped = remap$(v, {
      "openrouter_metadata": "openrouterMetadata",
      "user_id": "userId",
    });

    return new ForbiddenInferenceResponseError(remapped, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  });
