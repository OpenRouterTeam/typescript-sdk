/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2d141acc5a71
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import * as models from "../index.js";
import { OpenRouterError } from "./openroutererror.js";

/**
 * Batch metadata with results withheld (results: null) plus the standard error envelope. Add credits to unlock the already-computed results; the batch is not re-run.
 */
export type BatchPaymentRequiredResponseErrorData = {
  completionWindow: models.BatchPaymentRequiredResponseCompletionWindow;
  createdAt: number;
  endpoint: string;
  error: models.BatchPaymentRequiredResponseError;
  finalizedAt: number | null;
  id: string;
  model: string;
  object: models.BatchPaymentRequiredResponseObject;
  requestCounts: models.BatchPaymentRequiredResponseRequestCounts;
  /**
   * Always null: results are withheld until the batch charge is covered.
   */
  results: any | null;
  status: models.BatchPaymentRequiredResponseStatus;
  usage: models.BatchPaymentRequiredResponseUsage | null;
};

/**
 * Batch metadata with results withheld (results: null) plus the standard error envelope. Add credits to unlock the already-computed results; the batch is not re-run.
 */
export class BatchPaymentRequiredResponseError extends OpenRouterError {
  completionWindow: models.BatchPaymentRequiredResponseCompletionWindow;
  createdAt: number;
  endpoint: string;
  error: models.BatchPaymentRequiredResponseError;
  finalizedAt: number | null;
  id: string;
  model: string;
  object: models.BatchPaymentRequiredResponseObject;
  requestCounts: models.BatchPaymentRequiredResponseRequestCounts;
  /**
   * Always null: results are withheld until the batch charge is covered.
   */
  results: any | null;
  status: models.BatchPaymentRequiredResponseStatus;
  usage: models.BatchPaymentRequiredResponseUsage | null;

  /** The original data that was passed to this error instance. */
  data$: BatchPaymentRequiredResponseErrorData;

  constructor(
    err: BatchPaymentRequiredResponseErrorData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.error?.message
      || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    this.completionWindow = err.completionWindow;
    this.createdAt = err.createdAt;
    this.endpoint = err.endpoint;
    this.error = err.error;
    this.finalizedAt = err.finalizedAt;
    this.id = err.id;
    this.model = err.model;
    this.object = err.object;
    this.requestCounts = err.requestCounts;
    this.results = err.results;
    this.status = err.status;
    this.usage = err.usage;

    this.name = "BatchPaymentRequiredResponseError";
  }
}

/** @internal */
export const BatchPaymentRequiredResponseError$inboundSchema: z.ZodType<
  BatchPaymentRequiredResponseError,
  unknown
> = z.object({
  completion_window:
    models.BatchPaymentRequiredResponseCompletionWindow$inboundSchema,
  created_at: z.int(),
  endpoint: z.string(),
  error: z.lazy(() => models.BatchPaymentRequiredResponseError$inboundSchema),
  finalized_at: z.nullable(z.int()),
  id: z.string(),
  model: z.string(),
  object: models.BatchPaymentRequiredResponseObject$inboundSchema,
  request_counts: z.lazy(() =>
    models.BatchPaymentRequiredResponseRequestCounts$inboundSchema
  ),
  results: z.nullable(z.any()),
  status: models.BatchPaymentRequiredResponseStatus$inboundSchema,
  usage: z.nullable(
    z.lazy(() => models.BatchPaymentRequiredResponseUsage$inboundSchema),
  ),
  request$: z.custom<Request>(x => x instanceof Request),
  response$: z.custom<Response>(x => x instanceof Response),
  body$: z.string(),
})
  .transform((v) => {
    const remapped = remap$(v, {
      "completion_window": "completionWindow",
      "created_at": "createdAt",
      "finalized_at": "finalizedAt",
      "request_counts": "requestCounts",
    });

    return new BatchPaymentRequiredResponseError(remapped, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  });
