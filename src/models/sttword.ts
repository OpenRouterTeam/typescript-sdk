/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 748744003b2c
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A timestamped word, returned when the provider includes word-level timestamps
 */
export type STTWord = {
  /**
   * Word end time in seconds
   */
  end: number;
  /**
   * Word start time in seconds
   */
  start: number;
  /**
   * The transcribed word
   */
  word: string;
};

/** @internal */
export const STTWord$inboundSchema: z.ZodType<STTWord, unknown> = z.object({
  end: z.number(),
  start: z.number(),
  word: z.string(),
});

export function sttWordFromJSON(
  jsonString: string,
): SafeParseResult<STTWord, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => STTWord$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'STTWord' from JSON`,
  );
}
