/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 1c11d45b66cd
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Language the snippet is written in. Determines which sandbox runtime executes it.
 */
export const Language = {
  Typescript: "typescript",
  Python: "python",
  Bash: "bash",
} as const;
/**
 * Language the snippet is written in. Determines which sandbox runtime executes it.
 */
export type Language = OpenEnum<typeof Language>;

/**
 * Inline custom code executed at every step of the agent loop. Optional.
 */
export type AgentCustomCode = {
  /**
   * Optional entry function name. Defaults to the language convention (e.g. `default` for TS).
   */
  entry?: string | undefined;
  /**
   * Language the snippet is written in. Determines which sandbox runtime executes it.
   */
  language: Language;
  /**
   * Inline source for the custom hook. Executed in a sandboxed runtime at every step of the agent loop. Loaded once per session, not per step.
   */
  source: string;
};

/** @internal */
export const Language$inboundSchema: z.ZodType<Language, unknown> = openEnums
  .inboundSchema(Language);
/** @internal */
export const Language$outboundSchema: z.ZodType<string, Language> = openEnums
  .outboundSchema(Language);

/** @internal */
export const AgentCustomCode$inboundSchema: z.ZodType<
  AgentCustomCode,
  unknown
> = z.object({
  entry: z.string().optional(),
  language: Language$inboundSchema,
  source: z.string(),
});
/** @internal */
export type AgentCustomCode$Outbound = {
  entry?: string | undefined;
  language: string;
  source: string;
};

/** @internal */
export const AgentCustomCode$outboundSchema: z.ZodType<
  AgentCustomCode$Outbound,
  AgentCustomCode
> = z.object({
  entry: z.string().optional(),
  language: Language$outboundSchema,
  source: z.string(),
});

export function agentCustomCodeToJSON(
  agentCustomCode: AgentCustomCode,
): string {
  return JSON.stringify(AgentCustomCode$outboundSchema.parse(agentCustomCode));
}
export function agentCustomCodeFromJSON(
  jsonString: string,
): SafeParseResult<AgentCustomCode, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentCustomCode$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentCustomCode' from JSON`,
  );
}
