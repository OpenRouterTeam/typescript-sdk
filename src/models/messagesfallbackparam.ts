/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 990ce0f7bc03
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * Fallback model to try when the primary model fails or refuses. Only the `model` field is supported; per-attempt overrides are rejected.
 */
export type MessagesFallbackParam = {
  model: string;
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export type MessagesFallbackParam$Outbound = {
  model: string;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const MessagesFallbackParam$outboundSchema: z.ZodType<
  MessagesFallbackParam$Outbound,
  MessagesFallbackParam
> = z.object({
  model: z.string(),
  additionalProperties: z.record(z.string(), z.any()).optional(),
}).transform((v) => {
  return {
    ...v.additionalProperties,
    ...remap$(v, {
      additionalProperties: null,
    }),
  };
});

export function messagesFallbackParamToJSON(
  messagesFallbackParam: MessagesFallbackParam,
): string {
  return JSON.stringify(
    MessagesFallbackParam$outboundSchema.parse(messagesFallbackParam),
  );
}
