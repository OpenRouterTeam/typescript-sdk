/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d075ad6f16bf
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * The shell commands requested by the model.
 */
export type OutputShellServerToolAction = {
  /**
   * Shell commands to run, each in its own invocation, in order.
   */
  commands: Array<string>;
  maxOutputLength: number | null;
  timeoutMs: number | null;
};

/** @internal */
export const OutputShellServerToolAction$inboundSchema: z.ZodType<
  OutputShellServerToolAction,
  unknown
> = z.object({
  commands: z.array(z.string()),
  max_output_length: z.nullable(z.int()),
  timeout_ms: z.nullable(z.int()),
}).transform((v) => {
  return remap$(v, {
    "max_output_length": "maxOutputLength",
    "timeout_ms": "timeoutMs",
  });
});

export function outputShellServerToolActionFromJSON(
  jsonString: string,
): SafeParseResult<OutputShellServerToolAction, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OutputShellServerToolAction$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OutputShellServerToolAction' from JSON`,
  );
}
