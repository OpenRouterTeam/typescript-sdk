/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e13b5353df71
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  CapabilityDescriptor,
  CapabilityDescriptor$inboundSchema,
} from "./capabilitydescriptor.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ImagePricingEntry,
  ImagePricingEntry$inboundSchema,
} from "./imagepricingentry.js";

/**
 * An endpoint that serves a given image model.
 */
export type ImageEndpoint = {
  /**
   * Provider-specific options accepted under provider.options[provider_slug].
   */
  allowedPassthroughParameters: Array<string>;
  /**
   * Billable pricing lines for this endpoint.
   */
  pricing: Array<ImagePricingEntry>;
  /**
   * Provider display name
   */
  providerName: string;
  /**
   * Provider slug
   */
  providerSlug: string;
  /**
   * Provider tag for request-side selection
   */
  providerTag: string | null;
  supportedParameters: { [k: string]: CapabilityDescriptor };
  /**
   * Whether this endpoint supports native SSE streaming (`stream: true` in the request).
   */
  supportsStreaming: boolean;
};

/** @internal */
export const ImageEndpoint$inboundSchema: z.ZodType<ImageEndpoint, unknown> = z
  .object({
    allowed_passthrough_parameters: z.array(z.string()),
    pricing: z.array(ImagePricingEntry$inboundSchema),
    provider_name: z.string(),
    provider_slug: z.string(),
    provider_tag: z.nullable(z.string()),
    supported_parameters: z.record(
      z.string(),
      CapabilityDescriptor$inboundSchema,
    ),
    supports_streaming: z.boolean(),
  }).transform((v) => {
    return remap$(v, {
      "allowed_passthrough_parameters": "allowedPassthroughParameters",
      "provider_name": "providerName",
      "provider_slug": "providerSlug",
      "provider_tag": "providerTag",
      "supported_parameters": "supportedParameters",
      "supports_streaming": "supportsStreaming",
    });
  });

export function imageEndpointFromJSON(
  jsonString: string,
): SafeParseResult<ImageEndpoint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ImageEndpoint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ImageEndpoint' from JSON`,
  );
}
