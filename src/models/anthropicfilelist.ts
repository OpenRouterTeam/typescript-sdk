/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7ef7b8e43b6c
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { AnthropicFile, AnthropicFile$inboundSchema } from "./anthropicfile.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A page of files in Anthropic's shape.
 */
export type AnthropicFileList = {
  shape: "anthropic";
  data: Array<AnthropicFile>;
  firstId: string | null;
  hasMore: boolean;
  lastId: string | null;
};

/** @internal */
export const AnthropicFileList$inboundSchema: z.ZodType<
  AnthropicFileList,
  unknown
> = z.object({
  _shape: z.literal("anthropic"),
  data: z.array(AnthropicFile$inboundSchema),
  first_id: z.nullable(z.string()),
  has_more: z.boolean(),
  last_id: z.nullable(z.string()),
}).transform((v) => {
  return remap$(v, {
    "_shape": "shape",
    "first_id": "firstId",
    "has_more": "hasMore",
    "last_id": "lastId",
  });
});

export function anthropicFileListFromJSON(
  jsonString: string,
): SafeParseResult<AnthropicFileList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnthropicFileList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnthropicFileList' from JSON`,
  );
}
