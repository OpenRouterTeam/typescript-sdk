/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 12f1f7100366
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  CapabilityDescriptor,
  CapabilityDescriptor$inboundSchema,
} from "./capabilitydescriptor.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ImageModelArchitecture,
  ImageModelArchitecture$inboundSchema,
} from "./imagemodelarchitecture.js";

/**
 * A single image model in the discovery listing.
 */
export type ImageModelListItem = {
  architecture: ImageModelArchitecture;
  /**
   * Unix timestamp (seconds) of when the model was created
   */
  created: number;
  description: string;
  /**
   * Relative URL to the full per-endpoint records for this model
   */
  endpoints: string;
  /**
   * Model slug
   */
  id: string;
  /**
   * Display name
   */
  name: string;
  /**
   * Union of supported parameters across every endpoint of this model. Coarse discovery aid; the definitive per-endpoint set is behind the endpoints URL.
   */
  supportedParameters: { [k: string]: CapabilityDescriptor };
  /**
   * Whether any endpoint of this model supports native SSE streaming on the dedicated Image API (i.e. `stream: true` in the request). OR across endpoints.
   */
  supportsStreaming: boolean;
};

/** @internal */
export const ImageModelListItem$inboundSchema: z.ZodType<
  ImageModelListItem,
  unknown
> = z.object({
  architecture: ImageModelArchitecture$inboundSchema,
  created: z.int(),
  description: z.string(),
  endpoints: z.string(),
  id: z.string(),
  name: z.string(),
  supported_parameters: z.record(
    z.string(),
    CapabilityDescriptor$inboundSchema,
  ),
  supports_streaming: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "supported_parameters": "supportedParameters",
    "supports_streaming": "supportsStreaming",
  });
});

export function imageModelListItemFromJSON(
  jsonString: string,
): SafeParseResult<ImageModelListItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ImageModelListItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ImageModelListItem' from JSON`,
  );
}
