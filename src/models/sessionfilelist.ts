/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f4b7ddcbde4e
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { SessionFile, SessionFile$inboundSchema } from "./sessionfile.js";

/**
 * A page of files created by a server-side tool sandbox during a session.
 */
export type SessionFileList = {
  shape: "session";
  /**
   * Opaque cursor for the next page; null when there are no more results.
   */
  cursor: string | null;
  data: Array<SessionFile>;
  hasMore: boolean;
};

/** @internal */
export const SessionFileList$inboundSchema: z.ZodType<
  SessionFileList,
  unknown
> = z.object({
  _shape: z.literal("session"),
  cursor: z.nullable(z.string()),
  data: z.array(SessionFile$inboundSchema),
  has_more: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    "_shape": "shape",
    "has_more": "hasMore",
  });
});

export function sessionFileListFromJSON(
  jsonString: string,
): SafeParseResult<SessionFileList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionFileList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionFileList' from JSON`,
  );
}
