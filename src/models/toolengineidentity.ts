/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ede49ecb5d86
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { ToolOwnerKind, ToolOwnerKind$inboundSchema } from "./toolownerkind.js";
import {
  ToolReviewStatus,
  ToolReviewStatus$inboundSchema,
} from "./toolreviewstatus.js";
import {
  ToolVisibility,
  ToolVisibility$inboundSchema,
} from "./toolvisibility.js";

/**
 * Identity fields for a catalog tool engine.
 */
export type ToolEngineIdentity = {
  engineSlug: string;
  fullName: string;
  ownerId: string;
  /**
   * Owner kind for a catalog tool engine.
   */
  ownerKind: ToolOwnerKind;
  ownerSlug: string;
  /**
   * Marketplace review status for a tool engine.
   */
  reviewStatus: ToolReviewStatus;
  typeSlug: string;
  /**
   * Catalog visibility level for a tool engine.
   */
  visibility: ToolVisibility;
};

/** @internal */
export const ToolEngineIdentity$inboundSchema: z.ZodType<
  ToolEngineIdentity,
  unknown
> = z.object({
  engineSlug: z.string(),
  fullName: z.string(),
  ownerId: z.string(),
  ownerKind: ToolOwnerKind$inboundSchema,
  ownerSlug: z.string(),
  reviewStatus: ToolReviewStatus$inboundSchema,
  typeSlug: z.string(),
  visibility: ToolVisibility$inboundSchema,
});

export function toolEngineIdentityFromJSON(
  jsonString: string,
): SafeParseResult<ToolEngineIdentity, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ToolEngineIdentity$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ToolEngineIdentity' from JSON`,
  );
}
