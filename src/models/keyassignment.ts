/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0daee4bf9653
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type KeyAssignment = {
  /**
   * Unique identifier for the assignment
   */
  id: string;
  /**
   * Hash of the assigned API key
   */
  keyHash: string;
  /**
   * ID of the guardrail
   */
  guardrailId: string;
  /**
   * Name of the API key
   */
  keyName: string;
  /**
   * Label of the API key
   */
  keyLabel: string;
  /**
   * User ID of who made the assignment
   */
  assignedBy: string | null;
  /**
   * ISO 8601 timestamp of when the assignment was created
   */
  createdAt: string;
};

/** @internal */
export const KeyAssignment$inboundSchema: z.ZodType<KeyAssignment, unknown> = z
  .object({
    id: z.string(),
    key_hash: z.string(),
    guardrail_id: z.string(),
    key_name: z.string(),
    key_label: z.string(),
    assigned_by: z.nullable(z.string()),
    created_at: z.string(),
  }).transform((v) => {
    return remap$(v, {
      "key_hash": "keyHash",
      "guardrail_id": "guardrailId",
      "key_name": "keyName",
      "key_label": "keyLabel",
      "assigned_by": "assignedBy",
      "created_at": "createdAt",
    });
  });

export function keyAssignmentFromJSON(
  jsonString: string,
): SafeParseResult<KeyAssignment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KeyAssignment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KeyAssignment' from JSON`,
  );
}
