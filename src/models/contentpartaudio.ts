/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 04ae0dfceba1
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

export type AudioUrl = {
  url: string;
};

export type ContentPartAudio = {
  audioUrl: AudioUrl;
  type: "audio_url";
};

/** @internal */
export type AudioUrl$Outbound = {
  url: string;
};

/** @internal */
export const AudioUrl$outboundSchema: z.ZodType<AudioUrl$Outbound, AudioUrl> = z
  .object({
    url: z.string(),
  });

export function audioUrlToJSON(audioUrl: AudioUrl): string {
  return JSON.stringify(AudioUrl$outboundSchema.parse(audioUrl));
}

/** @internal */
export type ContentPartAudio$Outbound = {
  audio_url: AudioUrl$Outbound;
  type: "audio_url";
};

/** @internal */
export const ContentPartAudio$outboundSchema: z.ZodType<
  ContentPartAudio$Outbound,
  ContentPartAudio
> = z.object({
  audioUrl: z.lazy(() => AudioUrl$outboundSchema),
  type: z.literal("audio_url"),
}).transform((v) => {
  return remap$(v, {
    audioUrl: "audio_url",
  });
});

export function contentPartAudioToJSON(
  contentPartAudio: ContentPartAudio,
): string {
  return JSON.stringify(
    ContentPartAudio$outboundSchema.parse(contentPartAudio),
  );
}
