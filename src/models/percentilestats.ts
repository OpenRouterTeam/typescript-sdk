/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f8001516363f
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Latency percentiles in milliseconds over the last 30 minutes. Latency measures time to first token. Only visible when authenticated with an API key or cookie; returns null for unauthenticated requests.
 */
export type PercentileStats = {
  /**
   * Median (50th percentile)
   */
  p50: number;
  /**
   * 75th percentile
   */
  p75: number;
  /**
   * 90th percentile
   */
  p90: number;
  /**
   * 99th percentile
   */
  p99: number;
};

/** @internal */
export const PercentileStats$inboundSchema: z.ZodType<
  PercentileStats,
  unknown
> = z.object({
  p50: z.number(),
  p75: z.number(),
  p90: z.number(),
  p99: z.number(),
});

export function percentileStatsFromJSON(
  jsonString: string,
): SafeParseResult<PercentileStats, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PercentileStats$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PercentileStats' from JSON`,
  );
}
