/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 200bb5d5edad
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AgentUsage = {
  /**
   * Total cost of the session in USD, including provider compute and OR fees.
   */
  costUsd?: number | undefined;
  /**
   * Wall-clock duration of the session in milliseconds.
   */
  durationMs?: number | undefined;
};

/** @internal */
export const AgentUsage$inboundSchema: z.ZodType<AgentUsage, unknown> = z
  .object({
    cost_usd: z.number().optional(),
    duration_ms: z.int().optional(),
  }).transform((v) => {
    return remap$(v, {
      "cost_usd": "costUsd",
      "duration_ms": "durationMs",
    });
  });

export function agentUsageFromJSON(
  jsonString: string,
): SafeParseResult<AgentUsage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentUsage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentUsage' from JSON`,
  );
}
