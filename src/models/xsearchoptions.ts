/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d7a145785d12
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { RFCDate } from "../types/rfcdate.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Enable SpaceXAI X (Twitter) search alongside native web search, with optional filters. Only applies to SpaceXAI endpoints with native search; omit to search the web only. X search is billed separately by SpaceXAI, per post and per user profile fetched.
 */
export type XSearchOptions = {
  /**
   * Only include posts from these X handles (max 20). Cannot be used with excluded_x_handles.
   */
  allowedXHandles?: Array<string> | undefined;
  /**
   * Analyze images attached to matching posts.
   */
  enableImageUnderstanding?: boolean | undefined;
  /**
   * Analyze videos attached to matching posts.
   */
  enableVideoUnderstanding?: boolean | undefined;
  /**
   * Exclude posts from these X handles (max 20). Cannot be used with allowed_x_handles.
   */
  excludedXHandles?: Array<string> | undefined;
  /**
   * Start of the post date range (ISO 8601 date, e.g. "2025-01-01").
   */
  fromDate?: RFCDate | undefined;
  /**
   * End of the post date range (ISO 8601 date, e.g. "2025-12-31").
   */
  toDate?: RFCDate | undefined;
};

/** @internal */
export const XSearchOptions$inboundSchema: z.ZodType<XSearchOptions, unknown> =
  z.object({
    allowed_x_handles: z.array(z.string()).optional(),
    enable_image_understanding: z.boolean().optional(),
    enable_video_understanding: z.boolean().optional(),
    excluded_x_handles: z.array(z.string()).optional(),
    from_date: z.string().transform(v => new RFCDate(v)).optional(),
    to_date: z.string().transform(v => new RFCDate(v)).optional(),
  }).transform((v) => {
    return remap$(v, {
      "allowed_x_handles": "allowedXHandles",
      "enable_image_understanding": "enableImageUnderstanding",
      "enable_video_understanding": "enableVideoUnderstanding",
      "excluded_x_handles": "excludedXHandles",
      "from_date": "fromDate",
      "to_date": "toDate",
    });
  });
/** @internal */
export type XSearchOptions$Outbound = {
  allowed_x_handles?: Array<string> | undefined;
  enable_image_understanding?: boolean | undefined;
  enable_video_understanding?: boolean | undefined;
  excluded_x_handles?: Array<string> | undefined;
  from_date?: string | undefined;
  to_date?: string | undefined;
};

/** @internal */
export const XSearchOptions$outboundSchema: z.ZodType<
  XSearchOptions$Outbound,
  XSearchOptions
> = z.object({
  allowedXHandles: z.array(z.string()).optional(),
  enableImageUnderstanding: z.boolean().optional(),
  enableVideoUnderstanding: z.boolean().optional(),
  excludedXHandles: z.array(z.string()).optional(),
  fromDate: z.custom<RFCDate>(x => x instanceof RFCDate).transform(v =>
    v.toString()
  ).optional(),
  toDate: z.custom<RFCDate>(x => x instanceof RFCDate).transform(v =>
    v.toString()
  ).optional(),
}).transform((v) => {
  return remap$(v, {
    allowedXHandles: "allowed_x_handles",
    enableImageUnderstanding: "enable_image_understanding",
    enableVideoUnderstanding: "enable_video_understanding",
    excludedXHandles: "excluded_x_handles",
    fromDate: "from_date",
    toDate: "to_date",
  });
});

export function xSearchOptionsToJSON(xSearchOptions: XSearchOptions): string {
  return JSON.stringify(XSearchOptions$outboundSchema.parse(xSearchOptions));
}
export function xSearchOptionsFromJSON(
  jsonString: string,
): SafeParseResult<XSearchOptions, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => XSearchOptions$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'XSearchOptions' from JSON`,
  );
}
