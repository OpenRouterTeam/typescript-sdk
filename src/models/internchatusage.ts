/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 49d346597315
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  InternChatPromptTokensDetails,
  InternChatPromptTokensDetails$inboundSchema,
} from "./internchatprompttokensdetails.js";

/**
 * Token usage for the run as the daemon reported it, on the final chunk before `[DONE]`. `null` when the daemon reported none, and always `null` after `tool_calls` because the turn is not over.
 */
export type InternChatUsage = {
  completionTokens: number;
  /**
   * The cost of the run in USD, when the daemon reported one.
   */
  cost?: number | undefined;
  promptTokens: number;
  /**
   * Prompt tokens served from cache, when the daemon reported them.
   */
  promptTokensDetails?: InternChatPromptTokensDetails | undefined;
  totalTokens: number;
};

/** @internal */
export const InternChatUsage$inboundSchema: z.ZodType<
  InternChatUsage,
  unknown
> = z.object({
  completion_tokens: z.int(),
  cost: z.number().optional(),
  prompt_tokens: z.int(),
  prompt_tokens_details: InternChatPromptTokensDetails$inboundSchema.optional(),
  total_tokens: z.int(),
}).transform((v) => {
  return remap$(v, {
    "completion_tokens": "completionTokens",
    "prompt_tokens": "promptTokens",
    "prompt_tokens_details": "promptTokensDetails",
    "total_tokens": "totalTokens",
  });
});

export function internChatUsageFromJSON(
  jsonString: string,
): SafeParseResult<InternChatUsage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InternChatUsage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InternChatUsage' from JSON`,
  );
}
