/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 031cd7aaeaa3
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  BatchDeletionOutcome,
  BatchDeletionOutcome$inboundSchema,
} from "./batchdeletionoutcome.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * OpenRouter-held request and result artifacts were purged.
 */
export const Openrouter = {
  Deleted: "deleted",
} as const;
/**
 * OpenRouter-held request and result artifacts were purged.
 */
export type Openrouter = ClosedEnum<typeof Openrouter>;

/**
 * The upstream batch deletion outcome; omitted when no provider was assigned.
 */
export type Upstream = {
  /**
   * The provider name stored on the batch.
   */
  provider: string;
  /**
   * Outcome for one deletion target: `deleted` (removed), `unsupported` (the provider has no batch-delete API; supported file cleanup still runs), or `not_applicable` (the batch never reached that target).
   */
  status: BatchDeletionOutcome;
};

/**
 * OpenRouter cleanup and, when a provider was assigned, the upstream batch deletion outcome.
 */
export type BatchDeletionTargets = {
  /**
   * OpenRouter-held request and result artifacts were purged.
   */
  openrouter: Openrouter;
  /**
   * The upstream batch deletion outcome; omitted when no provider was assigned.
   */
  upstream?: Upstream | undefined;
};

/** @internal */
export const Openrouter$inboundSchema: z.ZodEnum<typeof Openrouter> = z.enum(
  Openrouter,
);

/** @internal */
export const Upstream$inboundSchema: z.ZodType<Upstream, unknown> = z.object({
  provider: z.string(),
  status: BatchDeletionOutcome$inboundSchema,
});

export function upstreamFromJSON(
  jsonString: string,
): SafeParseResult<Upstream, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Upstream$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Upstream' from JSON`,
  );
}

/** @internal */
export const BatchDeletionTargets$inboundSchema: z.ZodType<
  BatchDeletionTargets,
  unknown
> = z.object({
  openrouter: Openrouter$inboundSchema,
  upstream: z.lazy(() => Upstream$inboundSchema).optional(),
});

export function batchDeletionTargetsFromJSON(
  jsonString: string,
): SafeParseResult<BatchDeletionTargets, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BatchDeletionTargets$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BatchDeletionTargets' from JSON`,
  );
}
