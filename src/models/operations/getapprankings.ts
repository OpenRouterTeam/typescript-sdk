/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: edd3364e84c1
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

export type GetAppRankingsGlobals = {
  /**
   * The app identifier should be your app's URL and is used as the primary identifier for rankings.
   *
   * @remarks
   * This is used to track API usage per application.
   */
  httpReferer?: string | undefined;
  /**
   * The app display name allows you to customize how your app appears in OpenRouter's dashboard.
   *
   * @remarks
   */
  appTitle?: string | undefined;
  /**
   * Comma-separated list of app categories (e.g. "cli-agent,cloud-agent"). Used for marketplace rankings.
   *
   * @remarks
   */
  appCategories?: string | undefined;
};

/**
 * Marketplace category group to filter by (e.g. `coding`). Only apps tagged with a subcategory inside this group are returned. Mutually combinable with `subcategory` — when both are supplied the `subcategory` must belong to the `category` group.
 */
export const GetAppRankingsCategory = {
  Coding: "coding",
  Creative: "creative",
  Productivity: "productivity",
  Entertainment: "entertainment",
} as const;
/**
 * Marketplace category group to filter by (e.g. `coding`). Only apps tagged with a subcategory inside this group are returned. Mutually combinable with `subcategory` — when both are supplied the `subcategory` must belong to the `category` group.
 */
export type GetAppRankingsCategory = OpenEnum<typeof GetAppRankingsCategory>;

/**
 * Marketplace subcategory to filter by (e.g. `cli-agent`). Takes precedence over `category` for the actual filter; when `category` is also supplied the pair must be consistent.
 */
export const Subcategory = {
  CliAgent: "cli-agent",
  IdeExtension: "ide-extension",
  CloudAgent: "cloud-agent",
  ProgrammingApp: "programming-app",
  NativeAppBuilder: "native-app-builder",
  CreativeWriting: "creative-writing",
  VideoGen: "video-gen",
  ImageGen: "image-gen",
  AudioGen: "audio-gen",
  Roleplay: "roleplay",
  Game: "game",
  WritingAssistant: "writing-assistant",
  GeneralChat: "general-chat",
  PersonalAgent: "personal-agent",
  Legal: "legal",
} as const;
/**
 * Marketplace subcategory to filter by (e.g. `cli-agent`). Takes precedence over `category` for the actual filter; when `category` is also supplied the pair must be consistent.
 */
export type Subcategory = OpenEnum<typeof Subcategory>;

/**
 * `popular` ranks apps by total token volume inside the date window. `trending` ranks apps by absolute excess token growth: window volume minus the average volume of the three equal-length periods immediately preceding the window. Apps with no excess growth are omitted from `trending` results.
 */
export const Sort = {
  Popular: "popular",
  Trending: "trending",
} as const;
/**
 * `popular` ranks apps by total token volume inside the date window. `trending` ranks apps by absolute excess token growth: window volume minus the average volume of the three equal-length periods immediately preceding the window. Apps with no excess growth are omitted from `trending` results.
 */
export type Sort = OpenEnum<typeof Sort>;

export type GetAppRankingsRequest = {
  /**
   * The app identifier should be your app's URL and is used as the primary identifier for rankings.
   *
   * @remarks
   * This is used to track API usage per application.
   */
  httpReferer?: string | undefined;
  /**
   * The app display name allows you to customize how your app appears in OpenRouter's dashboard.
   *
   * @remarks
   */
  appTitle?: string | undefined;
  /**
   * Comma-separated list of app categories (e.g. "cli-agent,cloud-agent"). Used for marketplace rankings.
   *
   * @remarks
   */
  appCategories?: string | undefined;
  /**
   * Marketplace category group to filter by (e.g. `coding`). Only apps tagged with a subcategory inside this group are returned. Mutually combinable with `subcategory` — when both are supplied the `subcategory` must belong to the `category` group.
   */
  category?: GetAppRankingsCategory | undefined;
  /**
   * Marketplace subcategory to filter by (e.g. `cli-agent`). Takes precedence over `category` for the actual filter; when `category` is also supplied the pair must be consistent.
   */
  subcategory?: Subcategory | undefined;
  /**
   * `popular` ranks apps by total token volume inside the date window. `trending` ranks apps by absolute excess token growth: window volume minus the average volume of the three equal-length periods immediately preceding the window. Apps with no excess growth are omitted from `trending` results.
   */
  sort?: Sort | undefined;
  /**
   * Start of the date window in YYYY-MM-DD (UTC), inclusive. Defaults to 30 days before `end_date`. The dataset begins at 2025-01-01; earlier values are clamped forward to that floor and the resolved value is echoed in `meta.start_date`.
   */
  startDate?: string | undefined;
  /**
   * End of the date window in YYYY-MM-DD (UTC), inclusive. Defaults to the most recent completed UTC day. Must be on or after 2025-01-01; earlier values are rejected with a 400.
   */
  endDate?: string | undefined;
  /**
   * Maximum number of apps to return (1-100). Defaults to 50.
   */
  limit?: number | undefined;
  /**
   * Number of ranked apps to skip before the first returned row (0-100). Defaults to 0. `rank` stays absolute, so the first row of `offset=50` is `rank: 51`.
   */
  offset?: number | null | undefined;
};

export type GetAppRankingsResponse = {
  result: models.AppRankingsResponse;
};

/** @internal */
export const GetAppRankingsCategory$outboundSchema: z.ZodType<
  string,
  GetAppRankingsCategory
> = openEnums.outboundSchema(GetAppRankingsCategory);

/** @internal */
export const Subcategory$outboundSchema: z.ZodType<string, Subcategory> =
  openEnums.outboundSchema(Subcategory);

/** @internal */
export const Sort$outboundSchema: z.ZodType<string, Sort> = openEnums
  .outboundSchema(Sort);

/** @internal */
export type GetAppRankingsRequest$Outbound = {
  "HTTP-Referer"?: string | undefined;
  appTitle?: string | undefined;
  appCategories?: string | undefined;
  category?: string | undefined;
  subcategory?: string | undefined;
  sort: string;
  start_date?: string | undefined;
  end_date?: string | undefined;
  limit: number;
  offset: number | null;
};

/** @internal */
export const GetAppRankingsRequest$outboundSchema: z.ZodType<
  GetAppRankingsRequest$Outbound,
  GetAppRankingsRequest
> = z.object({
  httpReferer: z.string().optional(),
  appTitle: z.string().optional(),
  appCategories: z.string().optional(),
  category: GetAppRankingsCategory$outboundSchema.optional(),
  subcategory: Subcategory$outboundSchema.optional(),
  sort: Sort$outboundSchema.default("popular"),
  startDate: z.string().optional(),
  endDate: z.string().optional(),
  limit: z.int().default(50),
  offset: z.nullable(z.int().default(0)),
}).transform((v) => {
  return remap$(v, {
    httpReferer: "HTTP-Referer",
    startDate: "start_date",
    endDate: "end_date",
  });
});

export function getAppRankingsRequestToJSON(
  getAppRankingsRequest: GetAppRankingsRequest,
): string {
  return JSON.stringify(
    GetAppRankingsRequest$outboundSchema.parse(getAppRankingsRequest),
  );
}

/** @internal */
export const GetAppRankingsResponse$inboundSchema: z.ZodType<
  GetAppRankingsResponse,
  unknown
> = z.object({
  Result: models.AppRankingsResponse$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Result": "result",
  });
});

export function getAppRankingsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetAppRankingsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetAppRankingsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetAppRankingsResponse' from JSON`,
  );
}
