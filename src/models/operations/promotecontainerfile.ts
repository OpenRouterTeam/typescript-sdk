/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 83724b6f486c
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";

export type PromoteContainerFileGlobals = {
  /**
   * The app identifier should be your app's URL and is used as the primary identifier for rankings.
   *
   * @remarks
   * This is used to track API usage per application.
   */
  httpReferer?: string | undefined;
  /**
   * The app display name allows you to customize how your app appears in OpenRouter's dashboard.
   *
   * @remarks
   */
  appTitle?: string | undefined;
  /**
   * Comma-separated list of app categories (e.g. "cli-agent,cloud-agent"). Used for marketplace rankings.
   *
   * @remarks
   */
  appCategories?: string | undefined;
};

export type PromoteContainerFileRequest = {
  /**
   * The app identifier should be your app's URL and is used as the primary identifier for rankings.
   *
   * @remarks
   * This is used to track API usage per application.
   */
  httpReferer?: string | undefined;
  /**
   * The app display name allows you to customize how your app appears in OpenRouter's dashboard.
   *
   * @remarks
   */
  appTitle?: string | undefined;
  /**
   * Comma-separated list of app categories (e.g. "cli-agent,cloud-agent"). Used for marketplace rankings.
   *
   * @remarks
   */
  appCategories?: string | undefined;
  /**
   * The canonical container id, exactly as returned in a bash/shell tool result — a restarted session has its own `-r<nonce>`-suffixed id. A session-derived id is always `sess_` + the sanitized session key, which is not necessarily the raw session id that was sent.
   */
  containerId: string;
  /**
   * Container file id (`cfile_` + base64url of the file path).
   */
  fileId: string;
};

/** @internal */
export type PromoteContainerFileRequest$Outbound = {
  "HTTP-Referer"?: string | undefined;
  appTitle?: string | undefined;
  appCategories?: string | undefined;
  container_id: string;
  file_id: string;
};

/** @internal */
export const PromoteContainerFileRequest$outboundSchema: z.ZodType<
  PromoteContainerFileRequest$Outbound,
  PromoteContainerFileRequest
> = z.object({
  httpReferer: z.string().optional(),
  appTitle: z.string().optional(),
  appCategories: z.string().optional(),
  containerId: z.string(),
  fileId: z.string(),
}).transform((v) => {
  return remap$(v, {
    httpReferer: "HTTP-Referer",
    containerId: "container_id",
    fileId: "file_id",
  });
});

export function promoteContainerFileRequestToJSON(
  promoteContainerFileRequest: PromoteContainerFileRequest,
): string {
  return JSON.stringify(
    PromoteContainerFileRequest$outboundSchema.parse(
      promoteContainerFileRequest,
    ),
  );
}
