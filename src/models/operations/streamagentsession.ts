/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a983522a05fd
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

export type StreamAgentSessionGlobals = {
  /**
   * The app identifier should be your app's URL and is used as the primary identifier for rankings.
   *
   * @remarks
   * This is used to track API usage per application.
   */
  httpReferer?: string | undefined;
  /**
   * The app display name allows you to customize how your app appears in OpenRouter's dashboard.
   *
   * @remarks
   */
  appTitle?: string | undefined;
  /**
   * Comma-separated list of app categories (e.g. "cli-agent,cloud-agent"). Used for marketplace rankings.
   *
   * @remarks
   */
  appCategories?: string | undefined;
};

export type StreamAgentSessionRequest = {
  /**
   * The app identifier should be your app's URL and is used as the primary identifier for rankings.
   *
   * @remarks
   * This is used to track API usage per application.
   */
  httpReferer?: string | undefined;
  /**
   * The app display name allows you to customize how your app appears in OpenRouter's dashboard.
   *
   * @remarks
   */
  appTitle?: string | undefined;
  /**
   * Comma-separated list of app categories (e.g. "cli-agent,cloud-agent"). Used for marketplace rankings.
   *
   * @remarks
   */
  appCategories?: string | undefined;
  /**
   * The session ID returned from `POST /api/v1/agents/sessions`.
   */
  sessionId: string;
};

export type StreamAgentSessionResponse = models.AgentEvent | string;

/** @internal */
export type StreamAgentSessionRequest$Outbound = {
  "HTTP-Referer"?: string | undefined;
  appTitle?: string | undefined;
  appCategories?: string | undefined;
  session_id: string;
};

/** @internal */
export const StreamAgentSessionRequest$outboundSchema: z.ZodType<
  StreamAgentSessionRequest$Outbound,
  StreamAgentSessionRequest
> = z.object({
  httpReferer: z.string().optional(),
  appTitle: z.string().optional(),
  appCategories: z.string().optional(),
  sessionId: z.string(),
}).transform((v) => {
  return remap$(v, {
    httpReferer: "HTTP-Referer",
    sessionId: "session_id",
  });
});

export function streamAgentSessionRequestToJSON(
  streamAgentSessionRequest: StreamAgentSessionRequest,
): string {
  return JSON.stringify(
    StreamAgentSessionRequest$outboundSchema.parse(streamAgentSessionRequest),
  );
}

/** @internal */
export const StreamAgentSessionResponse$inboundSchema: z.ZodType<
  StreamAgentSessionResponse,
  unknown
> = z.union([models.AgentEvent$inboundSchema, z.string()]);

export function streamAgentSessionResponseFromJSON(
  jsonString: string,
): SafeParseResult<StreamAgentSessionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StreamAgentSessionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StreamAgentSessionResponse' from JSON`,
  );
}
