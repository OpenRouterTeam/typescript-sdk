/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: aad3082eb908
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import * as openEnums from "../types/enums.js";
import { ClosedEnum, OpenEnum } from "../types/enums.js";

/**
 * How much effort the model should put into its response. Higher effort levels may result in more thorough analysis but take longer. Valid values are `low`, `medium`, `high`, `xhigh`, or `max`.
 */
export const MessagesOutputConfigEffort = {
  Low: "low",
  Medium: "medium",
  High: "high",
  Xhigh: "xhigh",
  Max: "max",
} as const;
/**
 * How much effort the model should put into its response. Higher effort levels may result in more thorough analysis but take longer. Valid values are `low`, `medium`, `high`, `xhigh`, or `max`.
 */
export type MessagesOutputConfigEffort = OpenEnum<
  typeof MessagesOutputConfigEffort
>;

export const MessagesOutputConfigTypeJSONSchema = {
  JsonSchema: "json_schema",
} as const;
export type MessagesOutputConfigTypeJSONSchema = ClosedEnum<
  typeof MessagesOutputConfigTypeJSONSchema
>;

/**
 * A schema to specify Claude's output format in responses. See [structured outputs](https://platform.claude.com/docs/en/build-with-claude/structured-outputs).
 */
export type MessagesOutputConfigFormat = {
  schema: { [k: string]: any | null };
  type: MessagesOutputConfigTypeJSONSchema;
};

export const TypeTokens = {
  Tokens: "tokens",
} as const;
export type TypeTokens = ClosedEnum<typeof TypeTokens>;

/**
 * Task budget for an agentic turn. The model sees a countdown of remaining tokens and uses it to prioritize work and wind down gracefully. Advisory — does not enforce a hard cap.
 */
export type TaskBudget = {
  remaining?: number | null | undefined;
  total: number;
  type: TypeTokens;
};

/**
 * Configuration for controlling output behavior. Supports the effort parameter and structured output format.
 */
export type MessagesOutputConfig = {
  /**
   * How much effort the model should put into its response. Higher effort levels may result in more thorough analysis but take longer. Valid values are `low`, `medium`, `high`, `xhigh`, or `max`.
   */
  effort?: MessagesOutputConfigEffort | null | undefined;
  /**
   * A schema to specify Claude's output format in responses. See [structured outputs](https://platform.claude.com/docs/en/build-with-claude/structured-outputs).
   */
  format?: MessagesOutputConfigFormat | null | undefined;
  /**
   * Task budget for an agentic turn. The model sees a countdown of remaining tokens and uses it to prioritize work and wind down gracefully. Advisory — does not enforce a hard cap.
   */
  taskBudget?: TaskBudget | null | undefined;
};

/** @internal */
export const MessagesOutputConfigEffort$outboundSchema: z.ZodType<
  string,
  MessagesOutputConfigEffort
> = openEnums.outboundSchema(MessagesOutputConfigEffort);

/** @internal */
export const MessagesOutputConfigTypeJSONSchema$outboundSchema: z.ZodEnum<
  typeof MessagesOutputConfigTypeJSONSchema
> = z.enum(MessagesOutputConfigTypeJSONSchema);

/** @internal */
export type MessagesOutputConfigFormat$Outbound = {
  schema: { [k: string]: any | null };
  type: string;
};

/** @internal */
export const MessagesOutputConfigFormat$outboundSchema: z.ZodType<
  MessagesOutputConfigFormat$Outbound,
  MessagesOutputConfigFormat
> = z.object({
  schema: z.record(z.string(), z.nullable(z.any())),
  type: MessagesOutputConfigTypeJSONSchema$outboundSchema,
});

export function messagesOutputConfigFormatToJSON(
  messagesOutputConfigFormat: MessagesOutputConfigFormat,
): string {
  return JSON.stringify(
    MessagesOutputConfigFormat$outboundSchema.parse(messagesOutputConfigFormat),
  );
}

/** @internal */
export const TypeTokens$outboundSchema: z.ZodEnum<typeof TypeTokens> = z.enum(
  TypeTokens,
);

/** @internal */
export type TaskBudget$Outbound = {
  remaining?: number | null | undefined;
  total: number;
  type: string;
};

/** @internal */
export const TaskBudget$outboundSchema: z.ZodType<
  TaskBudget$Outbound,
  TaskBudget
> = z.object({
  remaining: z.nullable(z.int()).optional(),
  total: z.int(),
  type: TypeTokens$outboundSchema,
});

export function taskBudgetToJSON(taskBudget: TaskBudget): string {
  return JSON.stringify(TaskBudget$outboundSchema.parse(taskBudget));
}

/** @internal */
export type MessagesOutputConfig$Outbound = {
  effort?: string | null | undefined;
  format?: MessagesOutputConfigFormat$Outbound | null | undefined;
  task_budget?: TaskBudget$Outbound | null | undefined;
};

/** @internal */
export const MessagesOutputConfig$outboundSchema: z.ZodType<
  MessagesOutputConfig$Outbound,
  MessagesOutputConfig
> = z.object({
  effort: z.nullable(MessagesOutputConfigEffort$outboundSchema).optional(),
  format: z.nullable(z.lazy(() => MessagesOutputConfigFormat$outboundSchema))
    .optional(),
  taskBudget: z.nullable(z.lazy(() => TaskBudget$outboundSchema)).optional(),
}).transform((v) => {
  return remap$(v, {
    taskBudget: "task_budget",
  });
});

export function messagesOutputConfigToJSON(
  messagesOutputConfig: MessagesOutputConfig,
): string {
  return JSON.stringify(
    MessagesOutputConfig$outboundSchema.parse(messagesOutputConfig),
  );
}
