/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7655a528ded4
 */

import * as z from "zod/v4";

/**
 * Lets text-only models accept image inputs: images are replaced with deterministic placeholders and the model examines them on demand through the hidden `openrouter:inspect_image` server tool, which dispatches each question to a vision-capable model. Enabled per-request via `plugins` or the `:vision` model variant.
 */
export type VisionPlugin = {
  /**
   * Set to false to disable the vision plugin for this request.
   */
  enabled?: boolean | undefined;
  id: "vision";
  /**
   * Vision-capable model that answers `inspect_image` questions about the attached images. Defaults to the platform default vision model.
   */
  model?: string | undefined;
};

/** @internal */
export type VisionPlugin$Outbound = {
  enabled?: boolean | undefined;
  id: "vision";
  model?: string | undefined;
};

/** @internal */
export const VisionPlugin$outboundSchema: z.ZodType<
  VisionPlugin$Outbound,
  VisionPlugin
> = z.object({
  enabled: z.boolean().optional(),
  id: z.literal("vision"),
  model: z.string().optional(),
});

export function visionPluginToJSON(visionPlugin: VisionPlugin): string {
  return JSON.stringify(VisionPlugin$outboundSchema.parse(visionPlugin));
}
