/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 1767ec935316
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Session-size bucket.
 */
export const Bucket = {
  Single: "single",
  Short: "short",
  Core: "core",
  Long: "long",
} as const;
/**
 * Session-size bucket.
 */
export type Bucket = OpenEnum<typeof Bucket>;

export type SessionCostItem = {
  /**
   * Stable numeric identifier of the harness app.
   */
  appId: number;
  /**
   * Published harness display label.
   */
  appName: string;
  /**
   * Session-size bucket.
   */
  bucket: Bucket;
  /**
   * Median USD spend per sampled session.
   */
  medianSessionCostUsd: number;
  /**
   * Exact model permaslug, or `multi-model` for sessions without a dominant model.
   */
  modelPermaslug: string;
  /**
   * Number of sampled sessions in this published cell.
   */
  sessionCount: number;
  /**
   * Number of distinct users in this published cell.
   */
  userCount: number;
};

/** @internal */
export const Bucket$inboundSchema: z.ZodType<Bucket, unknown> = openEnums
  .inboundSchema(Bucket);

/** @internal */
export const SessionCostItem$inboundSchema: z.ZodType<
  SessionCostItem,
  unknown
> = z.object({
  app_id: z.int(),
  app_name: z.string(),
  bucket: Bucket$inboundSchema,
  median_session_cost_usd: z.number(),
  model_permaslug: z.string(),
  session_count: z.int(),
  user_count: z.int(),
}).transform((v) => {
  return remap$(v, {
    "app_id": "appId",
    "app_name": "appName",
    "median_session_cost_usd": "medianSessionCostUsd",
    "model_permaslug": "modelPermaslug",
    "session_count": "sessionCount",
    "user_count": "userCount",
  });
});

export function sessionCostItemFromJSON(
  jsonString: string,
): SafeParseResult<SessionCostItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionCostItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionCostItem' from JSON`,
  );
}
