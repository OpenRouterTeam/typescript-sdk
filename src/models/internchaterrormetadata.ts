/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 3a4e9b3de0d5
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A stable reason a client can branch on.
 */
export const InternChatErrorMetadataReason = {
  AttachmentFailed: "attachment_failed",
  BadRequest: "bad_request",
  Busy: "busy",
  ClientClosedRequest: "client_closed_request",
  InteractionNotPending: "interaction_not_pending",
  InteractionUnknown: "interaction_unknown",
  InternNotReady: "intern_not_ready",
  InternRejected: "intern_rejected",
  InternUnreachable: "intern_unreachable",
  NotFound: "not_found",
  PayloadTooLarge: "payload_too_large",
  RateLimited: "rate_limited",
  RunEnded: "run_ended",
  StreamSevered: "stream_severed",
  Timeout: "timeout",
  TurnFailed: "turn_failed",
} as const;
/**
 * A stable reason a client can branch on.
 */
export type InternChatErrorMetadataReason = OpenEnum<
  typeof InternChatErrorMetadataReason
>;

/**
 * Machine-readable detail for the failure.
 */
export type InternChatErrorMetadata = {
  /**
   * A stable reason a client can branch on.
   */
  reason: InternChatErrorMetadataReason;
  /**
   * Whether the same request may be sent again unchanged. Always `true` for the transient refusals — `busy`, `intern_not_ready`, `intern_unreachable`, `rate_limited`, `stream_severed` and `timeout` — and always `false` for the ones a retry cannot fix. For `turn_failed` it varies by failure and is the intern's own classification of what went wrong: `true` for an upstream overload, rate limit, timeout or transport fault, `false` for an authentication or bad-request failure that would be rejected the same way again. Branch on this field rather than on `reason` when deciding whether to retry. A `429`, and a `409` or `503` with reason `busy`, also carry a `Retry-After` header saying how long to wait.
   */
  retryable: boolean;
};

/** @internal */
export const InternChatErrorMetadataReason$inboundSchema: z.ZodType<
  InternChatErrorMetadataReason,
  unknown
> = openEnums.inboundSchema(InternChatErrorMetadataReason);

/** @internal */
export const InternChatErrorMetadata$inboundSchema: z.ZodType<
  InternChatErrorMetadata,
  unknown
> = z.object({
  reason: InternChatErrorMetadataReason$inboundSchema,
  retryable: z.boolean(),
});

export function internChatErrorMetadataFromJSON(
  jsonString: string,
): SafeParseResult<InternChatErrorMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InternChatErrorMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InternChatErrorMetadata' from JSON`,
  );
}
