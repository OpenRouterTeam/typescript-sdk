/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 78bcd1ddec85
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A single Design Arena benchmark entry for a specific arena+category
 */
export type DABenchmarkEntry = {
  /**
   * Arena type (e.g. models, builders, agents)
   */
  arena: string;
  /**
   * Category within the arena (e.g. website, gamedev, uicomponent)
   */
  category: string;
  /**
   * ELO rating from head-to-head arena battles
   */
  elo: number;
  /**
   * Rank position within this arena+category among models available on OpenRouter (1 = highest ELO)
   */
  rank: number;
  /**
   * Win rate percentage in arena battles
   */
  winRate: number;
};

/** @internal */
export const DABenchmarkEntry$inboundSchema: z.ZodType<
  DABenchmarkEntry,
  unknown
> = z.object({
  arena: z.string(),
  category: z.string(),
  elo: z.number(),
  rank: z.int(),
  win_rate: z.number(),
}).transform((v) => {
  return remap$(v, {
    "win_rate": "winRate",
  });
});

export function daBenchmarkEntryFromJSON(
  jsonString: string,
): SafeParseResult<DABenchmarkEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DABenchmarkEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DABenchmarkEntry' from JSON`,
  );
}
