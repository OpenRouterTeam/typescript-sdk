/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d8b27b19149c
 */

import * as z from "zod/v4";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";

/**
 * Outcome for one deletion target: `deleted` (removed), `unsupported` (the provider has no batch-delete API; supported file cleanup still runs), or `not_applicable` (the batch never reached that target).
 */
export const BatchDeletionOutcome = {
  Deleted: "deleted",
  Unsupported: "unsupported",
  NotApplicable: "not_applicable",
} as const;
/**
 * Outcome for one deletion target: `deleted` (removed), `unsupported` (the provider has no batch-delete API; supported file cleanup still runs), or `not_applicable` (the batch never reached that target).
 */
export type BatchDeletionOutcome = OpenEnum<typeof BatchDeletionOutcome>;

/** @internal */
export const BatchDeletionOutcome$inboundSchema: z.ZodType<
  BatchDeletionOutcome,
  unknown
> = openEnums.inboundSchema(BatchDeletionOutcome);
