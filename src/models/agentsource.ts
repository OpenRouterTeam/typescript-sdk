/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: fb4922dedeeb
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Source provider type. Only `github` is supported in v1.
 */
export const AgentSourceType = {
  Github: "github",
} as const;
/**
 * Source provider type. Only `github` is supported in v1.
 */
export type AgentSourceType = ClosedEnum<typeof AgentSourceType>;

/**
 * Where the agent should run. v1 ships GitHub-only; future versions will add other source types behind the `type` discriminator.
 */
export type AgentSource = {
  /**
   * Branch to base the agent session on. Defaults to the repo default branch when omitted.
   */
  branch?: string | undefined;
  /**
   * Optional commit SHA to pin the session to. Mutually exclusive with `branch` at runtime — when both are set, `commit_sha` wins.
   */
  commitSha?: string | undefined;
  /**
   * GitHub repo in `owner/repo` format.
   */
  repo: string;
  /**
   * Source provider type. Only `github` is supported in v1.
   */
  type: AgentSourceType;
};

/** @internal */
export const AgentSourceType$inboundSchema: z.ZodEnum<typeof AgentSourceType> =
  z.enum(AgentSourceType);
/** @internal */
export const AgentSourceType$outboundSchema: z.ZodEnum<typeof AgentSourceType> =
  AgentSourceType$inboundSchema;

/** @internal */
export const AgentSource$inboundSchema: z.ZodType<AgentSource, unknown> = z
  .object({
    branch: z.string().optional(),
    commit_sha: z.string().optional(),
    repo: z.string(),
    type: AgentSourceType$inboundSchema,
  }).transform((v) => {
    return remap$(v, {
      "commit_sha": "commitSha",
    });
  });
/** @internal */
export type AgentSource$Outbound = {
  branch?: string | undefined;
  commit_sha?: string | undefined;
  repo: string;
  type: string;
};

/** @internal */
export const AgentSource$outboundSchema: z.ZodType<
  AgentSource$Outbound,
  AgentSource
> = z.object({
  branch: z.string().optional(),
  commitSha: z.string().optional(),
  repo: z.string(),
  type: AgentSourceType$outboundSchema,
}).transform((v) => {
  return remap$(v, {
    commitSha: "commit_sha",
  });
});

export function agentSourceToJSON(agentSource: AgentSource): string {
  return JSON.stringify(AgentSource$outboundSchema.parse(agentSource));
}
export function agentSourceFromJSON(
  jsonString: string,
): SafeParseResult<AgentSource, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentSource$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentSource' from JSON`,
  );
}
