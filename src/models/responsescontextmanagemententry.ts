/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 41811b24eee2
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * A context management entry. Currently only `type: 'compaction'` is supported, which lets the provider automatically compact the conversation into an encrypted compaction item when the context window fills up (optionally at `compact_threshold` input tokens).
 */
export type ResponsesContextManagementEntry = {
  compactThreshold?: number | null | undefined;
  type: string;
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export type ResponsesContextManagementEntry$Outbound = {
  compact_threshold?: number | null | undefined;
  type: string;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const ResponsesContextManagementEntry$outboundSchema: z.ZodType<
  ResponsesContextManagementEntry$Outbound,
  ResponsesContextManagementEntry
> = z.object({
  compactThreshold: z.nullable(z.int()).optional(),
  type: z.string(),
  additionalProperties: z.record(z.string(), z.any()).optional(),
}).transform((v) => {
  return {
    ...v.additionalProperties,
    ...remap$(v, {
      compactThreshold: "compact_threshold",
      additionalProperties: null,
    }),
  };
});

export function responsesContextManagementEntryToJSON(
  responsesContextManagementEntry: ResponsesContextManagementEntry,
): string {
  return JSON.stringify(
    ResponsesContextManagementEntry$outboundSchema.parse(
      responsesContextManagementEntry,
    ),
  );
}
