/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 69284d63df45
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { StreamEvents, StreamEvents$inboundSchema } from "./streamevents.js";

export type ResponsesStreamingResponse = {
  /**
   * Union of all possible event types emitted during response streaming
   */
  data: StreamEvents;
};

/** @internal */
export const ResponsesStreamingResponse$inboundSchema: z.ZodType<
  ResponsesStreamingResponse,
  unknown
> = z.object({
  data: z.string().transform((v, ctx) => {
    try {
      return JSON.parse(v);
    } catch (err) {
      ctx.addIssue({
        input: v,
        code: "custom",
        message: `malformed json: ${err}`,
      });
      return z.NEVER;
    }
  }).pipe(StreamEvents$inboundSchema),
});

export function responsesStreamingResponseFromJSON(
  jsonString: string,
): SafeParseResult<ResponsesStreamingResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponsesStreamingResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponsesStreamingResponse' from JSON`,
  );
}
