/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6f17738c68f5
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type RankingsDailyItem = {
  /**
   * UTC calendar date the row is aggregated over (YYYY-MM-DD).
   */
  date: string;
  /**
   * Model permaslug (e.g. `openai/gpt-4o-2024-05-13`). The sentinel value `other` represents the per-day residue of all public models ranked outside the top 20.
   */
  modelPermaslug: string;
  /**
   * Sum of `prompt_tokens + completion_tokens` for the day, returned as a decimal string so 64-bit values are not truncated.
   */
  totalTokens: string;
};

/** @internal */
export const RankingsDailyItem$inboundSchema: z.ZodType<
  RankingsDailyItem,
  unknown
> = z.object({
  date: z.string(),
  model_permaslug: z.string(),
  total_tokens: z.string(),
}).transform((v) => {
  return remap$(v, {
    "model_permaslug": "modelPermaslug",
    "total_tokens": "totalTokens",
  });
});

export function rankingsDailyItemFromJSON(
  jsonString: string,
): SafeParseResult<RankingsDailyItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RankingsDailyItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RankingsDailyItem' from JSON`,
  );
}
