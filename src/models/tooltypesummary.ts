/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 437d2c1b7e85
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Summary metadata for a canonical tool type the dashboard can bind to.
 */
export type ToolTypeSummary = {
  description: string;
  displayName: string;
  fullName: string;
  inputSchemaJson: string;
  supportsNativePassthrough: boolean;
};

/** @internal */
export const ToolTypeSummary$inboundSchema: z.ZodType<
  ToolTypeSummary,
  unknown
> = z.object({
  description: z.string(),
  displayName: z.string(),
  fullName: z.string(),
  inputSchemaJson: z.string(),
  supportsNativePassthrough: z.boolean(),
});

export function toolTypeSummaryFromJSON(
  jsonString: string,
): SafeParseResult<ToolTypeSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ToolTypeSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ToolTypeSummary' from JSON`,
  );
}
