/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ee4283390600
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Emitted when a partial image becomes available during streaming generation
 */
export type ImageGenPartialImageEvent = {
  /**
   * Base64-encoded partial image data
   */
  b64Json: string;
  /**
   * 0-based index indicating which partial image this is in the sequence
   */
  partialImageIndex: number;
  /**
   * The event type
   */
  type: "image_generation.partial_image";
};

/** @internal */
export const ImageGenPartialImageEvent$inboundSchema: z.ZodType<
  ImageGenPartialImageEvent,
  unknown
> = z.object({
  b64_json: z.string(),
  partial_image_index: z.int(),
  type: z.literal("image_generation.partial_image"),
}).transform((v) => {
  return remap$(v, {
    "b64_json": "b64Json",
    "partial_image_index": "partialImageIndex",
  });
});

export function imageGenPartialImageEventFromJSON(
  jsonString: string,
): SafeParseResult<ImageGenPartialImageEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ImageGenPartialImageEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ImageGenPartialImageEvent' from JSON`,
  );
}
