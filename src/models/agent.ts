/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a498b7c4ee51
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { ClosedEnum, OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { AgentConfig, AgentConfig$inboundSchema } from "./agentconfig.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Underlying adapter the agent runs on.
 */
export const AgentAdapter = {
  CognitionDevin: "cognition/devin",
  CursorCloudAgent: "cursor/cloud-agent",
  AnthropicClaudeCodeRemoteTasks: "anthropic/claude-code-remote-tasks",
  OpenaiCodex: "openai/codex",
} as const;
/**
 * Underlying adapter the agent runs on.
 */
export type AgentAdapter = OpenEnum<typeof AgentAdapter>;

/**
 * Object type discriminator.
 */
export const AgentObject = {
  Agent: "agent",
} as const;
/**
 * Object type discriminator.
 */
export type AgentObject = ClosedEnum<typeof AgentObject>;

export type Agent = {
  adapter: AgentAdapter;
  config: AgentConfig;
  /**
   * ISO 8601 UTC timestamp.
   */
  createdAt: Date;
  /**
   * Free-form description. Null if not set.
   */
  description: string | null;
  /**
   * Human-readable name shown in the UI.
   */
  displayName: string;
  /**
   * OpenRouter-issued UUID. Use this for path parameters.
   */
  id: string;
  /**
   * Whether the agent is callable by anyone in the org. Defaults to false (owner-only).
   */
  isPublic: boolean;
  /**
   * Object type discriminator.
   */
  object: AgentObject;
  /**
   * Owning provider for this agent. For first-party agents this is `cognition`/`cursor`/etc. For user-defined agents it is the owning user or org slug.
   */
  providerSlug: string;
  /**
   * Stable slug surfaced in dashboards, of the form `<owner>/<name>`. Unique per owner. Cannot be changed after creation.
   */
  slug: string;
  /**
   * ISO 8601 UTC timestamp of the most recent state change.
   */
  updatedAt: Date;
};

/** @internal */
export const AgentAdapter$inboundSchema: z.ZodType<AgentAdapter, unknown> =
  openEnums.inboundSchema(AgentAdapter);

/** @internal */
export const AgentObject$inboundSchema: z.ZodEnum<typeof AgentObject> = z.enum(
  AgentObject,
);

/** @internal */
export const Agent$inboundSchema: z.ZodType<Agent, unknown> = z.object({
  adapter: AgentAdapter$inboundSchema,
  config: AgentConfig$inboundSchema,
  created_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  description: z.nullable(z.string()),
  display_name: z.string(),
  id: z.string(),
  is_public: z.boolean(),
  object: AgentObject$inboundSchema,
  provider_slug: z.string(),
  slug: z.string(),
  updated_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
    "display_name": "displayName",
    "is_public": "isPublic",
    "provider_slug": "providerSlug",
    "updated_at": "updatedAt",
  });
});

export function agentFromJSON(
  jsonString: string,
): SafeParseResult<Agent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Agent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Agent' from JSON`,
  );
}
