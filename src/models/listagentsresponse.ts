/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7c65510b134f
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { Agent, Agent$inboundSchema } from "./agent.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ListAgentsResponse = {
  data: Array<Agent>;
  /**
   * Cursor to pass into the next list call to continue paginating, or null if there are no more results.
   */
  nextCursor: string | null;
};

/** @internal */
export const ListAgentsResponse$inboundSchema: z.ZodType<
  ListAgentsResponse,
  unknown
> = z.object({
  data: z.array(Agent$inboundSchema),
  next_cursor: z.nullable(z.string()),
}).transform((v) => {
  return remap$(v, {
    "next_cursor": "nextCursor",
  });
});

export function listAgentsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListAgentsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAgentsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAgentsResponse' from JSON`,
  );
}
