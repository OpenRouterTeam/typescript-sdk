/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e64212a5b3eb
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { ClosedEnum, OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Inference = {
  costQualityTradeoff: number | null;
  costTier: string | null;
  maxTokens: number | null;
  pinModel: boolean | null;
  providerSort: string | null;
  reasoningEffort: string | null;
  temperature: number | null;
};

export const SearchEvaluationConfigurationMode = {
  Default: "default",
  Pinned: "pinned",
} as const;
export type SearchEvaluationConfigurationMode = OpenEnum<
  typeof SearchEvaluationConfigurationMode
>;

export type Routing = {
  endpointId: string | null;
  mode: SearchEvaluationConfigurationMode;
  providerName: string | null;
};

export const SchemaVersion = {
  SearchRunV1: "search-run-v1",
} as const;
export type SchemaVersion = ClosedEnum<typeof SchemaVersion>;

export const ExcludedDomainsPolicy = {
  BenchmarkDefault: "benchmark-default",
  Explicit: "explicit",
} as const;
export type ExcludedDomainsPolicy = OpenEnum<typeof ExcludedDomainsPolicy>;

export const SearchContextSize = {
  Low: "low",
  Medium: "medium",
  High: "high",
} as const;
export type SearchContextSize = OpenEnum<typeof SearchContextSize>;

export const Surface = {
  ServerTool: "server-tool",
  Plugin: "plugin",
} as const;
export type Surface = OpenEnum<typeof Surface>;

export type WebFetch = {
  configuredEngine: string | null;
  maxContentTokens: number | null;
  maxUses: number | null;
};

export type Search = {
  allowedDomains: Array<string> | null;
  configuredEngine: string;
  configuredMaxAgentTurns: number | null;
  configuredMaxResults: number | null;
  configuredMaxTotalResults: number | null;
  effectiveMaxAgentTurns: number | null;
  effectiveMaxResults: number;
  effectiveMaxTotalResults: number;
  excludedDomains: Array<string> | null;
  excludedDomainsPolicy: ExcludedDomainsPolicy;
  excludedDomainsSha256: string | null;
  maxCharacters: number | null;
  searchContextSize: SearchContextSize | null;
  searchPromptSha256: string | null;
  surface: Surface;
  webFetch: WebFetch | null;
};

/**
 * The evaluation configuration behind this result. `id` is a stable fingerprint: results are comparable if and only if their configuration ids match.
 */
export type SearchEvaluationConfiguration = {
  id: string;
  inference: Inference;
  routing: Routing;
  schemaVersion: SchemaVersion;
  search: Search;
};

/** @internal */
export const Inference$inboundSchema: z.ZodType<Inference, unknown> = z.object({
  cost_quality_tradeoff: z.nullable(z.number()),
  cost_tier: z.nullable(z.string()),
  max_tokens: z.nullable(z.number()),
  pin_model: z.nullable(z.boolean()),
  provider_sort: z.nullable(z.string()),
  reasoning_effort: z.nullable(z.string()),
  temperature: z.nullable(z.number()),
}).transform((v) => {
  return remap$(v, {
    "cost_quality_tradeoff": "costQualityTradeoff",
    "cost_tier": "costTier",
    "max_tokens": "maxTokens",
    "pin_model": "pinModel",
    "provider_sort": "providerSort",
    "reasoning_effort": "reasoningEffort",
  });
});

export function inferenceFromJSON(
  jsonString: string,
): SafeParseResult<Inference, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Inference$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Inference' from JSON`,
  );
}

/** @internal */
export const SearchEvaluationConfigurationMode$inboundSchema: z.ZodType<
  SearchEvaluationConfigurationMode,
  unknown
> = openEnums.inboundSchema(SearchEvaluationConfigurationMode);

/** @internal */
export const Routing$inboundSchema: z.ZodType<Routing, unknown> = z.object({
  endpoint_id: z.nullable(z.string()),
  mode: SearchEvaluationConfigurationMode$inboundSchema,
  provider_name: z.nullable(z.string()),
}).transform((v) => {
  return remap$(v, {
    "endpoint_id": "endpointId",
    "provider_name": "providerName",
  });
});

export function routingFromJSON(
  jsonString: string,
): SafeParseResult<Routing, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Routing$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Routing' from JSON`,
  );
}

/** @internal */
export const SchemaVersion$inboundSchema: z.ZodEnum<typeof SchemaVersion> = z
  .enum(SchemaVersion);

/** @internal */
export const ExcludedDomainsPolicy$inboundSchema: z.ZodType<
  ExcludedDomainsPolicy,
  unknown
> = openEnums.inboundSchema(ExcludedDomainsPolicy);

/** @internal */
export const SearchContextSize$inboundSchema: z.ZodType<
  SearchContextSize,
  unknown
> = openEnums.inboundSchema(SearchContextSize);

/** @internal */
export const Surface$inboundSchema: z.ZodType<Surface, unknown> = openEnums
  .inboundSchema(Surface);

/** @internal */
export const WebFetch$inboundSchema: z.ZodType<WebFetch, unknown> = z.object({
  configured_engine: z.nullable(z.string()),
  max_content_tokens: z.nullable(z.int()),
  max_uses: z.nullable(z.int()),
}).transform((v) => {
  return remap$(v, {
    "configured_engine": "configuredEngine",
    "max_content_tokens": "maxContentTokens",
    "max_uses": "maxUses",
  });
});

export function webFetchFromJSON(
  jsonString: string,
): SafeParseResult<WebFetch, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WebFetch$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WebFetch' from JSON`,
  );
}

/** @internal */
export const Search$inboundSchema: z.ZodType<Search, unknown> = z.object({
  allowed_domains: z.nullable(z.array(z.string())),
  configured_engine: z.string(),
  configured_max_agent_turns: z.nullable(z.int()),
  configured_max_results: z.nullable(z.int()),
  configured_max_total_results: z.nullable(z.int()),
  effective_max_agent_turns: z.nullable(z.int()),
  effective_max_results: z.int(),
  effective_max_total_results: z.int(),
  excluded_domains: z.nullable(z.array(z.string())),
  excluded_domains_policy: ExcludedDomainsPolicy$inboundSchema,
  excluded_domains_sha256: z.nullable(z.string()),
  max_characters: z.nullable(z.int()),
  search_context_size: z.nullable(SearchContextSize$inboundSchema),
  search_prompt_sha256: z.nullable(z.string()),
  surface: Surface$inboundSchema,
  web_fetch: z.nullable(z.lazy(() => WebFetch$inboundSchema)),
}).transform((v) => {
  return remap$(v, {
    "allowed_domains": "allowedDomains",
    "configured_engine": "configuredEngine",
    "configured_max_agent_turns": "configuredMaxAgentTurns",
    "configured_max_results": "configuredMaxResults",
    "configured_max_total_results": "configuredMaxTotalResults",
    "effective_max_agent_turns": "effectiveMaxAgentTurns",
    "effective_max_results": "effectiveMaxResults",
    "effective_max_total_results": "effectiveMaxTotalResults",
    "excluded_domains": "excludedDomains",
    "excluded_domains_policy": "excludedDomainsPolicy",
    "excluded_domains_sha256": "excludedDomainsSha256",
    "max_characters": "maxCharacters",
    "search_context_size": "searchContextSize",
    "search_prompt_sha256": "searchPromptSha256",
    "web_fetch": "webFetch",
  });
});

export function searchFromJSON(
  jsonString: string,
): SafeParseResult<Search, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Search$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Search' from JSON`,
  );
}

/** @internal */
export const SearchEvaluationConfiguration$inboundSchema: z.ZodType<
  SearchEvaluationConfiguration,
  unknown
> = z.object({
  id: z.string(),
  inference: z.lazy(() => Inference$inboundSchema),
  routing: z.lazy(() => Routing$inboundSchema),
  schema_version: SchemaVersion$inboundSchema,
  search: z.lazy(() => Search$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "schema_version": "schemaVersion",
  });
});

export function searchEvaluationConfigurationFromJSON(
  jsonString: string,
): SafeParseResult<SearchEvaluationConfiguration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchEvaluationConfiguration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchEvaluationConfiguration' from JSON`,
  );
}
