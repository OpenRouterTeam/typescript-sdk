/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d8c6c402e488
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * An item in the compacted context window — echoed input items followed by a single `compaction` item carrying the encrypted summary of the compacted history.
 */
export type CompactOutputItem = {
  /**
   * Provider-encrypted payload (present on `compaction` items), tagged with the producing endpoint. Echo the item back verbatim on subsequent `/responses` requests and OpenRouter will route the conversation to that endpoint.
   */
  encryptedContent?: string | null | undefined;
  type: string;
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const CompactOutputItem$inboundSchema: z.ZodType<
  CompactOutputItem,
  unknown
> = collectExtraKeys$(
  z.object({
    encrypted_content: z.nullable(z.string()).optional(),
    type: z.string(),
  }).catchall(z.any()),
  "additionalProperties",
  true,
).transform((v) => {
  return remap$(v, {
    "encrypted_content": "encryptedContent",
  });
});

export function compactOutputItemFromJSON(
  jsonString: string,
): SafeParseResult<CompactOutputItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompactOutputItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompactOutputItem' from JSON`,
  );
}
