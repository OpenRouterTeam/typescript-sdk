/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7e23388e40f3
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type PipelineStageData = {
  /**
   * Task-type tag from the classification taxonomy. Emitted by auto-beta and phaser router stages when classification succeeds, and absent when classification is unavailable. The taxonomy evolves independently of this API contract.
   */
  taskType?: string | undefined;
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const PipelineStageData$inboundSchema: z.ZodType<
  PipelineStageData,
  unknown
> = collectExtraKeys$(
  z.object({
    task_type: z.string().optional(),
  }).catchall(z.any()),
  "additionalProperties",
  true,
).transform((v) => {
  return remap$(v, {
    "task_type": "taskType",
  });
});

export function pipelineStageDataFromJSON(
  jsonString: string,
): SafeParseResult<PipelineStageData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PipelineStageData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PipelineStageData' from JSON`,
  );
}
