/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7dda2a2f9b4b
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { AgentSlug, AgentSlug$outboundSchema } from "./agentslug.js";
import {
  AgentSource,
  AgentSource$Outbound,
  AgentSource$outboundSchema,
} from "./agentsource.js";

export type CreateAgentSessionRequest = {
  /**
   * Identifier for the cloud agent to invoke. Format: `<provider>/<agent-name>`. New adapters are added by OpenRouter; the set is intentionally closed.
   */
  agent: AgentSlug;
  /**
   * ID of a previously-created AgentContext to attach. Org-level and per-user knowledge from this context is translated into the agent provider native format and injected at session start.
   */
  contextId?: string | undefined;
  /**
   * Namespaced provider-specific extensions. Keys must be of the form `<provider>/<feature>`. Unknown extensions are passed through to the underlying provider.
   */
  extensions?: { [k: string]: any | null } | undefined;
  /**
   * When true, OpenRouter still adds auth brokering, billing, and context injection but does NOT transform the request body — the raw payload reaches the underlying agent provider as-is. Use this to access provider-specific features that have not yet been promoted to the core spec or an extension.
   */
  passthrough?: boolean | undefined;
  /**
   * The instruction the agent should act on.
   */
  prompt: string;
  /**
   * Where the agent should run. v1 ships GitHub-only; future versions will add other source types behind the `type` discriminator.
   */
  source?: AgentSource | undefined;
};

/** @internal */
export type CreateAgentSessionRequest$Outbound = {
  agent: string;
  context_id?: string | undefined;
  extensions?: { [k: string]: any | null } | undefined;
  passthrough?: boolean | undefined;
  prompt: string;
  source?: AgentSource$Outbound | undefined;
};

/** @internal */
export const CreateAgentSessionRequest$outboundSchema: z.ZodType<
  CreateAgentSessionRequest$Outbound,
  CreateAgentSessionRequest
> = z.object({
  agent: AgentSlug$outboundSchema,
  contextId: z.string().optional(),
  extensions: z.record(z.string(), z.nullable(z.any())).optional(),
  passthrough: z.boolean().optional(),
  prompt: z.string(),
  source: AgentSource$outboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    contextId: "context_id",
  });
});

export function createAgentSessionRequestToJSON(
  createAgentSessionRequest: CreateAgentSessionRequest,
): string {
  return JSON.stringify(
    CreateAgentSessionRequest$outboundSchema.parse(createAgentSessionRequest),
  );
}
