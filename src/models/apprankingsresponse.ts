/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b6c27d448609
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  AppRankingsItem,
  AppRankingsItem$inboundSchema,
} from "./apprankingsitem.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  RankingsDailyMeta,
  RankingsDailyMeta$inboundSchema,
} from "./rankingsdailymeta.js";

export type AppRankingsResponse = {
  /**
   * Apps ranked per the requested `sort`, re-numbered 1..N after category filtering. `popular` sorts by `total_tokens` descending; `trending` sorts by absolute excess token growth descending and may return fewer than `limit` rows when few apps are growing.
   */
  data: Array<AppRankingsItem>;
  meta: RankingsDailyMeta;
};

/** @internal */
export const AppRankingsResponse$inboundSchema: z.ZodType<
  AppRankingsResponse,
  unknown
> = z.object({
  data: z.array(AppRankingsItem$inboundSchema),
  meta: RankingsDailyMeta$inboundSchema,
});

export function appRankingsResponseFromJSON(
  jsonString: string,
): SafeParseResult<AppRankingsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AppRankingsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AppRankingsResponse' from JSON`,
  );
}
