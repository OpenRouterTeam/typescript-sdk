/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4684252ebfd9
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * `started` when a new run began. `steered` when the session already had a run going and the prompt was delivered into it instead.
 */
export const InternInvokeAcceptedResponseStatus = {
  Started: "started",
  Steered: "steered",
} as const;
/**
 * `started` when a new run began. `steered` when the session already had a run going and the prompt was delivered into it instead.
 */
export type InternInvokeAcceptedResponseStatus = OpenEnum<
  typeof InternInvokeAcceptedResponseStatus
>;

/**
 * The intern admitted the prompt. The run continues on the intern.
 */
export type InternInvokeAcceptedResponse = {
  /**
   * The session the run belongs to. Send it back to continue the conversation.
   */
  sessionId: string;
  /**
   * `started` when a new run began. `steered` when the session already had a run going and the prompt was delivered into it instead.
   */
  status: InternInvokeAcceptedResponseStatus;
};

/** @internal */
export const InternInvokeAcceptedResponseStatus$inboundSchema: z.ZodType<
  InternInvokeAcceptedResponseStatus,
  unknown
> = openEnums.inboundSchema(InternInvokeAcceptedResponseStatus);

/** @internal */
export const InternInvokeAcceptedResponse$inboundSchema: z.ZodType<
  InternInvokeAcceptedResponse,
  unknown
> = z.object({
  session_id: z.string(),
  status: InternInvokeAcceptedResponseStatus$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "session_id": "sessionId",
  });
});

export function internInvokeAcceptedResponseFromJSON(
  jsonString: string,
): SafeParseResult<InternInvokeAcceptedResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InternInvokeAcceptedResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InternInvokeAcceptedResponse' from JSON`,
  );
}
