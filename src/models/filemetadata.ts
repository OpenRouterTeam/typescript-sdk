/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7a5c3a86d7a2
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const FileMetadataType = {
  File: "file",
} as const;
export type FileMetadataType = ClosedEnum<typeof FileMetadataType>;

/**
 * Metadata describing a stored file.
 */
export type FileMetadata = {
  createdAt: string;
  downloadable: boolean;
  filename: string;
  id: string;
  mimeType: string;
  sizeBytes: number;
  type: FileMetadataType;
};

/** @internal */
export const FileMetadataType$inboundSchema: z.ZodEnum<
  typeof FileMetadataType
> = z.enum(FileMetadataType);

/** @internal */
export const FileMetadata$inboundSchema: z.ZodType<FileMetadata, unknown> = z
  .object({
    created_at: z.string(),
    downloadable: z.boolean(),
    filename: z.string(),
    id: z.string(),
    mime_type: z.string(),
    size_bytes: z.int(),
    type: FileMetadataType$inboundSchema,
  }).transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "mime_type": "mimeType",
      "size_bytes": "sizeBytes",
    });
  });

export function fileMetadataFromJSON(
  jsonString: string,
): SafeParseResult<FileMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FileMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FileMetadata' from JSON`,
  );
}
