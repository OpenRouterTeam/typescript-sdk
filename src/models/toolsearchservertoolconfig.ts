/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cd3efc23b324
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";

/**
 * How the model searches. `bm25` (default) ranks tools against a natural-language query; `regex` filters them by a pattern. Anthropic's `tool_search_tool_bm25` and `tool_search_tool_regex` map onto this field.
 */
export const ToolSearchServerToolConfigMode = {
  Bm25: "bm25",
  Regex: "regex",
} as const;
/**
 * How the model searches. `bm25` (default) ranks tools against a natural-language query; `regex` filters them by a pattern. Anthropic's `tool_search_tool_bm25` and `tool_search_tool_regex` map onto this field.
 */
export type ToolSearchServerToolConfigMode = OpenEnum<
  typeof ToolSearchServerToolConfigMode
>;

/**
 * Configuration for the openrouter:tool_search server tool
 */
export type ToolSearchServerToolConfig = {
  /**
   * Maximum tools returned by one search. Defaults to 5.
   */
  maxResults?: number | undefined;
  /**
   * How the model searches. `bm25` (default) ranks tools against a natural-language query; `regex` filters them by a pattern. Anthropic's `tool_search_tool_bm25` and `tool_search_tool_regex` map onto this field.
   */
  mode?: ToolSearchServerToolConfigMode | undefined;
};

/** @internal */
export const ToolSearchServerToolConfigMode$outboundSchema: z.ZodType<
  string,
  ToolSearchServerToolConfigMode
> = openEnums.outboundSchema(ToolSearchServerToolConfigMode);

/** @internal */
export type ToolSearchServerToolConfig$Outbound = {
  max_results?: number | undefined;
  mode: string;
};

/** @internal */
export const ToolSearchServerToolConfig$outboundSchema: z.ZodType<
  ToolSearchServerToolConfig$Outbound,
  ToolSearchServerToolConfig
> = z.object({
  maxResults: z.int().optional(),
  mode: ToolSearchServerToolConfigMode$outboundSchema.default("bm25"),
}).transform((v) => {
  return remap$(v, {
    maxResults: "max_results",
  });
});

export function toolSearchServerToolConfigToJSON(
  toolSearchServerToolConfig: ToolSearchServerToolConfig,
): string {
  return JSON.stringify(
    ToolSearchServerToolConfig$outboundSchema.parse(toolSearchServerToolConfig),
  );
}
