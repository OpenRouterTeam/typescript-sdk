/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 38335a290948
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Response body returned after rotating a tenant engine webhook secret.
 */
export type RotateWebhookSecretResponse = {
  /**
   * Unix-epoch ms after which the old secret is no longer accepted. Equal to `now + 5 * 60_000`.
   */
  overlapExpiresAt: number;
  /**
   * New webhook secret in base64url. Shown ONCE — store it in your engine before closing the modal.
   */
  secret: string;
  /**
   * Reference identifier stored on the engine row. The dispatcher loads the cleartext value via this id.
   */
  secretId: string;
};

/** @internal */
export const RotateWebhookSecretResponse$inboundSchema: z.ZodType<
  RotateWebhookSecretResponse,
  unknown
> = z.object({
  overlapExpiresAt: z.int(),
  secret: z.string(),
  secretId: z.string(),
});

export function rotateWebhookSecretResponseFromJSON(
  jsonString: string,
): SafeParseResult<RotateWebhookSecretResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RotateWebhookSecretResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RotateWebhookSecretResponse' from JSON`,
  );
}
