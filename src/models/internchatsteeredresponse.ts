/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 81a2168a45b8
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * The message was handed to the turn already running on this session.
 */
export const InternChatSteeredResponseStatus = {
  Steered: "steered",
} as const;
/**
 * The message was handed to the turn already running on this session.
 */
export type InternChatSteeredResponseStatus = ClosedEnum<
  typeof InternChatSteeredResponseStatus
>;

/**
 * The message was delivered into the turn already running on the same session. Its effect is streamed on that turn, not here.
 */
export type InternChatSteeredResponse = {
  /**
   * The session of the running turn the message was delivered to.
   */
  sessionId: string;
  /**
   * The message was handed to the turn already running on this session.
   */
  status: InternChatSteeredResponseStatus;
};

/** @internal */
export const InternChatSteeredResponseStatus$inboundSchema: z.ZodEnum<
  typeof InternChatSteeredResponseStatus
> = z.enum(InternChatSteeredResponseStatus);

/** @internal */
export const InternChatSteeredResponse$inboundSchema: z.ZodType<
  InternChatSteeredResponse,
  unknown
> = z.object({
  session_id: z.string(),
  status: InternChatSteeredResponseStatus$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "session_id": "sessionId",
  });
});

export function internChatSteeredResponseFromJSON(
  jsonString: string,
): SafeParseResult<InternChatSteeredResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InternChatSteeredResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InternChatSteeredResponse' from JSON`,
  );
}
