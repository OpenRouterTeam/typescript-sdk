/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0ec3a338b395
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * How often the spend counted against this budget resets.
 */
export const Interval = {
  Daily: "daily",
  Weekly: "weekly",
  Monthly: "monthly",
  Lifetime: "lifetime",
} as const;
/**
 * How often the spend counted against this budget resets.
 */
export type Interval = OpenEnum<typeof Interval>;

/**
 * What the spend counted against this budget is accumulated over.
 */
export const Scope = {
  User: "user",
  ApiKey: "api_key",
  OrgMember: "org_member",
  Workspace: "workspace",
} as const;
/**
 * What the spend counted against this budget is accumulated over.
 */
export type Scope = OpenEnum<typeof Scope>;

/**
 * Which configured limit this budget comes from. `user_credits` is the account credit balance, `api_key_limit` the spending limit set on the key itself, `api_key_guardrail` / `org_member_guardrail` / `org_default_guardrail` are guardrail budgets, and `workspace_budget` is a budget shared by everything in the key's workspace.
 */
export const BudgetType = {
  UserCredits: "user_credits",
  ApiKeyLimit: "api_key_limit",
  ApiKeyGuardrail: "api_key_guardrail",
  OrgMemberGuardrail: "org_member_guardrail",
  OrgDefaultGuardrail: "org_default_guardrail",
  WorkspaceBudget: "workspace_budget",
} as const;
/**
 * Which configured limit this budget comes from. `user_credits` is the account credit balance, `api_key_limit` the spending limit set on the key itself, `api_key_guardrail` / `org_member_guardrail` / `org_default_guardrail` are guardrail budgets, and `workspace_budget` is a budget shared by everything in the key's workspace.
 */
export type BudgetType = OpenEnum<typeof BudgetType>;

export type Budget = {
  /**
   * Identifier of the configured budget (guardrail or workspace budget), or null when the budget has no standalone record.
   */
  id: string | null;
  /**
   * Whether BYOK (bring-your-own-key) spend counts toward this budget.
   */
  includesByok: boolean;
  /**
   * How often the spend counted against this budget resets.
   */
  interval: Interval;
  /**
   * Whether this budget is already exhausted and blocking requests.
   */
  isExceeded: boolean;
  /**
   * Spending limit in USD for the current interval.
   */
  limitUsd: number;
  /**
   * Human-readable name of the guardrail, workspace, or API key, when it has one.
   */
  name: string | null;
  /**
   * Spend in USD still available before this budget blocks requests.
   */
  remainingUsd: number;
  /**
   * ISO 8601 UTC timestamp when the spend counter resets, or null for lifetime budgets.
   */
  resetsAt: Date | null;
  /**
   * What the spend counted against this budget is accumulated over.
   */
  scope: Scope;
  /**
   * Which configured limit this budget comes from. `user_credits` is the account credit balance, `api_key_limit` the spending limit set on the key itself, `api_key_guardrail` / `org_member_guardrail` / `org_default_guardrail` are guardrail budgets, and `workspace_budget` is a budget shared by everything in the key's workspace.
   */
  type: BudgetType;
  /**
   * Spend in USD already accumulated against this budget in the current interval.
   */
  usageUsd: number;
  /**
   * Fraction of the limit already spent, between 0 and 1.
   */
  utilization: number;
};

/** @internal */
export const Interval$inboundSchema: z.ZodType<Interval, unknown> = openEnums
  .inboundSchema(Interval);

/** @internal */
export const Scope$inboundSchema: z.ZodType<Scope, unknown> = openEnums
  .inboundSchema(Scope);

/** @internal */
export const BudgetType$inboundSchema: z.ZodType<BudgetType, unknown> =
  openEnums.inboundSchema(BudgetType);

/** @internal */
export const Budget$inboundSchema: z.ZodType<Budget, unknown> = z.object({
  id: z.nullable(z.string()),
  includes_byok: z.boolean(),
  interval: Interval$inboundSchema,
  is_exceeded: z.boolean(),
  limit_usd: z.number(),
  name: z.nullable(z.string()),
  remaining_usd: z.number(),
  resets_at: z.nullable(
    z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  ),
  scope: Scope$inboundSchema,
  type: BudgetType$inboundSchema,
  usage_usd: z.number(),
  utilization: z.number(),
}).transform((v) => {
  return remap$(v, {
    "includes_byok": "includesByok",
    "is_exceeded": "isExceeded",
    "limit_usd": "limitUsd",
    "remaining_usd": "remainingUsd",
    "resets_at": "resetsAt",
    "usage_usd": "usageUsd",
  });
});

export function budgetFromJSON(
  jsonString: string,
): SafeParseResult<Budget, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Budget$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Budget' from JSON`,
  );
}
