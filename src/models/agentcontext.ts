/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5cc42e8f9234
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  AgentContextScope,
  AgentContextScope$inboundSchema,
} from "./agentcontextscope.js";
import {
  AgentMcpServer,
  AgentMcpServer$inboundSchema,
} from "./agentmcpserver.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Object type discriminator.
 */
export const AgentContextObject = {
  AgentContext: "agent.context",
} as const;
/**
 * Object type discriminator.
 */
export type AgentContextObject = ClosedEnum<typeof AgentContextObject>;

export type AgentContext = {
  /**
   * ISO 8601 UTC timestamp.
   */
  createdAt: Date;
  /**
   * OpenRouter-issued context ID.
   */
  id: string;
  /**
   * Vendor-neutral knowledge entries. Translated per-adapter into Claude Skills, Codex `AGENTS.md`, Devin Knowledge notes, etc.
   */
  knowledge: Array<string>;
  /**
   * Remote MCP server URLs to attach. The agent connects directly; OpenRouter does not proxy runtime MCP traffic.
   */
  mcpServers: Array<AgentMcpServer>;
  /**
   * Human-readable name. Conventionally `<owner>/<repo>` or a project shorthand.
   */
  name: string;
  /**
   * Object type discriminator.
   */
  object: AgentContextObject;
  /**
   * Scope at which an AgentContext applies.
   */
  scope: AgentContextScope;
  /**
   * ISO 8601 UTC timestamp.
   */
  updatedAt: Date;
};

/** @internal */
export const AgentContextObject$inboundSchema: z.ZodEnum<
  typeof AgentContextObject
> = z.enum(AgentContextObject);

/** @internal */
export const AgentContext$inboundSchema: z.ZodType<AgentContext, unknown> = z
  .object({
    created_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
    id: z.string(),
    knowledge: z.array(z.string()),
    mcp_servers: z.array(AgentMcpServer$inboundSchema),
    name: z.string(),
    object: AgentContextObject$inboundSchema,
    scope: AgentContextScope$inboundSchema,
    updated_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  }).transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "mcp_servers": "mcpServers",
      "updated_at": "updatedAt",
    });
  });

export function agentContextFromJSON(
  jsonString: string,
): SafeParseResult<AgentContext, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentContext$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentContext' from JSON`,
  );
}
