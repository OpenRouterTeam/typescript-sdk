/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: df990f384b54
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ImageGenerationUsage,
  ImageGenerationUsage$inboundSchema,
} from "./imagegenerationusage.js";

/**
 * Emitted when generation completes and the final image is available
 */
export type ImageGenCompletedEvent = {
  /**
   * Base64-encoded final image data
   */
  b64Json: string;
  /**
   * MIME type of the image. Omitted when the output is a standard raster format (PNG). Present for non-raster outputs such as SVG (`image/svg+xml`).
   */
  contentType?: string | undefined;
  /**
   * Unix timestamp (seconds) when the image was generated
   */
  created: number;
  /**
   * The event type
   */
  type: "image_generation.completed";
  /**
   * Token and cost usage for the image generation request, when available
   */
  usage?: ImageGenerationUsage | undefined;
};

/** @internal */
export const ImageGenCompletedEvent$inboundSchema: z.ZodType<
  ImageGenCompletedEvent,
  unknown
> = z.object({
  b64_json: z.string(),
  content_type: z.string().optional(),
  created: z.int(),
  type: z.literal("image_generation.completed"),
  usage: ImageGenerationUsage$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "b64_json": "b64Json",
    "content_type": "contentType",
  });
});

export function imageGenCompletedEventFromJSON(
  jsonString: string,
): SafeParseResult<ImageGenCompletedEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ImageGenCompletedEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ImageGenCompletedEvent' from JSON`,
  );
}
