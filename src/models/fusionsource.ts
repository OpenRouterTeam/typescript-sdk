/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 36747573906e
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A web page retrieved via web search during a fusion run.
 */
export type FusionSource = {
  /**
   * Title of the retrieved web page.
   */
  title: string;
  /**
   * URL of the web page a panel or the judge retrieved during the run.
   */
  url: string;
};

/** @internal */
export const FusionSource$inboundSchema: z.ZodType<FusionSource, unknown> = z
  .object({
    title: z.string(),
    url: z.string(),
  });
/** @internal */
export type FusionSource$Outbound = {
  title: string;
  url: string;
};

/** @internal */
export const FusionSource$outboundSchema: z.ZodType<
  FusionSource$Outbound,
  FusionSource
> = z.object({
  title: z.string(),
  url: z.string(),
});

export function fusionSourceToJSON(fusionSource: FusionSource): string {
  return JSON.stringify(FusionSource$outboundSchema.parse(fusionSource));
}
export function fusionSourceFromJSON(
  jsonString: string,
): SafeParseResult<FusionSource, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FusionSource$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FusionSource' from JSON`,
  );
}
