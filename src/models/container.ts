/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 28b3b43717a5
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const ContainerObject = {
  Container: "container",
} as const;
export type ContainerObject = ClosedEnum<typeof ContainerObject>;

export type Container = {
  /**
   * Stored size of the session in bytes, as last settled; counts against the workspace storage quota.
   */
  bytes: number;
  /**
   * Container id: the canonical sandbox session key, usable as `container_id` on the container-file routes.
   */
  id: string;
  /**
   * Unix timestamp (seconds) of the last storage activity. The session and its files expire 30 days after it.
   */
  lastActiveAt: number;
  object: ContainerObject;
};

/** @internal */
export const ContainerObject$inboundSchema: z.ZodEnum<typeof ContainerObject> =
  z.enum(ContainerObject);

/** @internal */
export const Container$inboundSchema: z.ZodType<Container, unknown> = z.object({
  bytes: z.int(),
  id: z.string(),
  last_active_at: z.int(),
  object: ContainerObject$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "last_active_at": "lastActiveAt",
  });
});

export function containerFromJSON(
  jsonString: string,
): SafeParseResult<Container, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Container$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Container' from JSON`,
  );
}
