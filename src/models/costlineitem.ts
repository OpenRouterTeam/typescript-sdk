/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: c0ed91db42f5
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A single billing line item showing what was charged and why
 */
export type CostLineItem = {
  /**
   * Total cost for this line item (quantity * unit_price)
   */
  cost: number;
  /**
   * Number of units consumed
   */
  quantity: number;
  /**
   * Human-readable label for this billing line item
   */
  skuLabel: string;
  /**
   * Price per unit in USD
   */
  unitPrice: number;
};

/** @internal */
export const CostLineItem$inboundSchema: z.ZodType<CostLineItem, unknown> = z
  .object({
    cost: z.number(),
    quantity: z.number(),
    sku_label: z.string(),
    unit_price: z.number(),
  }).transform((v) => {
    return remap$(v, {
      "sku_label": "skuLabel",
      "unit_price": "unitPrice",
    });
  });

export function costLineItemFromJSON(
  jsonString: string,
): SafeParseResult<CostLineItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CostLineItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CostLineItem' from JSON`,
  );
}
