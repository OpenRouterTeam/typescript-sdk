/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b964c2f2b8ed
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AgentSessionResult = {
  /**
   * List of file paths the agent modified.
   */
  filesChanged?: Array<string> | undefined;
  /**
   * URL of the pull request the agent opened, if any. Adapters that do not produce PRs leave this unset.
   */
  prUrl?: string | undefined;
  /**
   * Free-text summary of what the agent did. May be empty for short tasks.
   */
  summary?: string | undefined;
};

/** @internal */
export const AgentSessionResult$inboundSchema: z.ZodType<
  AgentSessionResult,
  unknown
> = z.object({
  files_changed: z.array(z.string()).optional(),
  pr_url: z.string().optional(),
  summary: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "files_changed": "filesChanged",
    "pr_url": "prUrl",
  });
});

export function agentSessionResultFromJSON(
  jsonString: string,
): SafeParseResult<AgentSessionResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentSessionResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentSessionResult' from JSON`,
  );
}
