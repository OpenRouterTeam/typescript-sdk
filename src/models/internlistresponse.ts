/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cd6ace7e64c6
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Intern, Intern$inboundSchema } from "./intern.js";

/**
 * Interns visible to the authenticated API key.
 */
export type InternListResponse = {
  data: Array<Intern>;
  /**
   * True when more interns match the current filters.
   */
  hasMore: boolean;
  /**
   * Opaque cursor, present when `has_more` is true. Pass it as `starting_after` to fetch the next page.
   */
  nextCursor?: string | undefined;
};

/** @internal */
export const InternListResponse$inboundSchema: z.ZodType<
  InternListResponse,
  unknown
> = z.object({
  data: z.array(Intern$inboundSchema),
  has_more: z.boolean(),
  next_cursor: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "has_more": "hasMore",
    "next_cursor": "nextCursor",
  });
});

export function internListResponseFromJSON(
  jsonString: string,
): SafeParseResult<InternListResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InternListResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InternListResponse' from JSON`,
  );
}
