/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ece4a8ddc017
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const ContainerFileObject = {
  ContainerFile: "container.file",
} as const;
export type ContainerFileObject = ClosedEnum<typeof ContainerFileObject>;

/**
 * Container files are always produced by the assistant sandbox.
 */
export const ContainerFileSource = {
  Assistant: "assistant",
} as const;
/**
 * Container files are always produced by the assistant sandbox.
 */
export type ContainerFileSource = ClosedEnum<typeof ContainerFileSource>;

export type ContainerFile = {
  /**
   * File size in bytes.
   */
  bytes: number;
  /**
   * The container the file belongs to — echoes the `container_id` path parameter (OpenAI field name).
   */
  containerId: string;
  /**
   * Unix timestamp (seconds) when the file was last synced.
   */
  createdAt: number;
  /**
   * Container file id: `cfile_` + base64url of the file path.
   */
  id: string;
  object: ContainerFileObject;
  /**
   * Container-relative file path.
   */
  path: string;
  /**
   * Container files are always produced by the assistant sandbox.
   */
  source: ContainerFileSource;
};

/** @internal */
export const ContainerFileObject$inboundSchema: z.ZodEnum<
  typeof ContainerFileObject
> = z.enum(ContainerFileObject);

/** @internal */
export const ContainerFileSource$inboundSchema: z.ZodEnum<
  typeof ContainerFileSource
> = z.enum(ContainerFileSource);

/** @internal */
export const ContainerFile$inboundSchema: z.ZodType<ContainerFile, unknown> = z
  .object({
    bytes: z.int(),
    container_id: z.string(),
    created_at: z.int(),
    id: z.string(),
    object: ContainerFileObject$inboundSchema,
    path: z.string(),
    source: ContainerFileSource$inboundSchema,
  }).transform((v) => {
    return remap$(v, {
      "container_id": "containerId",
      "created_at": "createdAt",
    });
  });

export function containerFileFromJSON(
  jsonString: string,
): SafeParseResult<ContainerFile, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContainerFile$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContainerFile' from JSON`,
  );
}
