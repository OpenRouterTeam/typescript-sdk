/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 19d90882812d
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Concrete model targeted by this tilde-latest alias, when applicable
 */
export type ModelAliasTarget = {
  /**
   * Human-readable name of the concrete model targeted by this alias
   */
  name: string;
  /**
   * Canonical slug of the concrete model targeted by this alias
   */
  slug: string;
};

/** @internal */
export const ModelAliasTarget$inboundSchema: z.ZodType<
  ModelAliasTarget,
  unknown
> = z.object({
  name: z.string(),
  slug: z.string(),
});

export function modelAliasTargetFromJSON(
  jsonString: string,
): SafeParseResult<ModelAliasTarget, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ModelAliasTarget$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ModelAliasTarget' from JSON`,
  );
}
