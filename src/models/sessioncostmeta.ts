/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: de705cb6caba
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Dataset version.
 */
export const SessionCostMetaVersion = {
  V1: "v1",
} as const;
/**
 * Dataset version.
 */
export type SessionCostMetaVersion = ClosedEnum<typeof SessionCostMetaVersion>;

export type SessionCostMeta = {
  /**
   * ISO-8601 timestamp when the response was generated.
   */
  asOf: string;
  /**
   * Dataset version.
   */
  version: SessionCostMetaVersion;
  /**
   * Number of days in the weekly session sample window.
   */
  windowDays: number;
  /**
   * UTC date of the final day in the session sample window.
   */
  windowEndDate: string;
};

/** @internal */
export const SessionCostMetaVersion$inboundSchema: z.ZodEnum<
  typeof SessionCostMetaVersion
> = z.enum(SessionCostMetaVersion);

/** @internal */
export const SessionCostMeta$inboundSchema: z.ZodType<
  SessionCostMeta,
  unknown
> = z.object({
  as_of: z.string(),
  version: SessionCostMetaVersion$inboundSchema,
  window_days: z.int(),
  window_end_date: z.string(),
}).transform((v) => {
  return remap$(v, {
    "as_of": "asOf",
    "window_days": "windowDays",
    "window_end_date": "windowEndDate",
  });
});

export function sessionCostMetaFromJSON(
  jsonString: string,
): SafeParseResult<SessionCostMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionCostMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionCostMeta' from JSON`,
  );
}
