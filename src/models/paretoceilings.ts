/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7f3f19c76d0a
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * Hard maximum raw-value caps. Models exceeding a ceiling are penalised (score halved) but not removed. latency_ms is in milliseconds. cost_usd is per-token cost (sum of prompt + completion per-token prices).
 */
export type ParetoCeilings = {
  costUsd?: number | undefined;
  latencyMs?: number | undefined;
};

/** @internal */
export type ParetoCeilings$Outbound = {
  cost_usd?: number | undefined;
  latency_ms?: number | undefined;
};

/** @internal */
export const ParetoCeilings$outboundSchema: z.ZodType<
  ParetoCeilings$Outbound,
  ParetoCeilings
> = z.object({
  costUsd: z.number().optional(),
  latencyMs: z.number().optional(),
}).transform((v) => {
  return remap$(v, {
    costUsd: "cost_usd",
    latencyMs: "latency_ms",
  });
});

export function paretoCeilingsToJSON(paretoCeilings: ParetoCeilings): string {
  return JSON.stringify(ParetoCeilings$outboundSchema.parse(paretoCeilings));
}
