/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 71596c10c79b
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A call to a custom (freeform-grammar) tool created by the model — distinct from `function_call`. Used for tools like Codex CLI's `apply_patch` whose payload is opaque text rather than JSON arguments.
 */
export type OutputCustomToolCallItem = {
  callId: string;
  id?: string | undefined;
  input: string;
  name: string;
  /**
   * Namespace qualifier for tools registered as part of a namespace tool group (e.g. an MCP server)
   */
  namespace?: string | undefined;
  type: "custom_tool_call";
};

/** @internal */
export const OutputCustomToolCallItem$inboundSchema: z.ZodType<
  OutputCustomToolCallItem,
  unknown
> = z.object({
  call_id: z.string(),
  id: z.string().optional(),
  input: z.string(),
  name: z.string(),
  namespace: z.string().optional(),
  type: z.literal("custom_tool_call"),
}).transform((v) => {
  return remap$(v, {
    "call_id": "callId",
  });
});

export function outputCustomToolCallItemFromJSON(
  jsonString: string,
): SafeParseResult<OutputCustomToolCallItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OutputCustomToolCallItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OutputCustomToolCallItem' from JSON`,
  );
}
