/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e618eb0802a7
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  TaskClassificationItem,
  TaskClassificationItem$inboundSchema,
} from "./taskclassificationitem.js";
import {
  TaskClassificationMacroCategory,
  TaskClassificationMacroCategory$inboundSchema,
} from "./taskclassificationmacrocategory.js";

export type TaskClassificationResponseData = {
  /**
   * UTC date (YYYY-MM-DD) of the window upper bound (yesterday). Data is exclusive of the current incomplete UTC day. This is the expected latest date in the snapshot; it does not confirm data presence for that date.
   */
  asOf: string;
  /**
   * Per-task classification market-share data, sorted by usage_share descending.
   */
  classifications: Array<TaskClassificationItem>;
  /**
   * Aggregate market-share data per macro-category (code, data, agent, general).
   */
  macroCategories: Array<TaskClassificationMacroCategory>;
  /**
   * Number of trailing days covered by this snapshot.
   */
  windowDays: number;
};

export type TaskClassificationResponse = {
  data: TaskClassificationResponseData;
};

/** @internal */
export const TaskClassificationResponseData$inboundSchema: z.ZodType<
  TaskClassificationResponseData,
  unknown
> = z.object({
  as_of: z.string(),
  classifications: z.array(TaskClassificationItem$inboundSchema),
  macro_categories: z.array(TaskClassificationMacroCategory$inboundSchema),
  window_days: z.int(),
}).transform((v) => {
  return remap$(v, {
    "as_of": "asOf",
    "macro_categories": "macroCategories",
    "window_days": "windowDays",
  });
});

export function taskClassificationResponseDataFromJSON(
  jsonString: string,
): SafeParseResult<TaskClassificationResponseData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaskClassificationResponseData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaskClassificationResponseData' from JSON`,
  );
}

/** @internal */
export const TaskClassificationResponse$inboundSchema: z.ZodType<
  TaskClassificationResponse,
  unknown
> = z.object({
  data: z.lazy(() => TaskClassificationResponseData$inboundSchema),
});

export function taskClassificationResponseFromJSON(
  jsonString: string,
): SafeParseResult<TaskClassificationResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaskClassificationResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaskClassificationResponse' from JSON`,
  );
}
