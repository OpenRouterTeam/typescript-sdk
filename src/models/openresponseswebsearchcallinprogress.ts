/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5ca24ba4cf86
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Web search call in progress
 */
export type OpenResponsesWebSearchCallInProgress = {
  type: "response.web_search_call.in_progress";
  itemId: string;
  outputIndex: number;
  sequenceNumber: number;
};

/** @internal */
export const OpenResponsesWebSearchCallInProgress$inboundSchema: z.ZodType<
  OpenResponsesWebSearchCallInProgress,
  unknown
> = z.object({
  type: z.literal("response.web_search_call.in_progress"),
  item_id: z.string(),
  output_index: z.number(),
  sequence_number: z.number(),
}).transform((v) => {
  return remap$(v, {
    "item_id": "itemId",
    "output_index": "outputIndex",
    "sequence_number": "sequenceNumber",
  });
});

export function openResponsesWebSearchCallInProgressFromJSON(
  jsonString: string,
): SafeParseResult<OpenResponsesWebSearchCallInProgress, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      OpenResponsesWebSearchCallInProgress$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OpenResponsesWebSearchCallInProgress' from JSON`,
  );
}
