/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b7f925e9d10e
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A single tool grant record.
 */
export type ToolGrantRecord = {
  engineId: string;
  grantedAt: string;
  revokedAt: string | null;
  userId: string;
};

/** @internal */
export const ToolGrantRecord$inboundSchema: z.ZodType<
  ToolGrantRecord,
  unknown
> = z.object({
  engineId: z.string(),
  grantedAt: z.string(),
  revokedAt: z.nullable(z.string()),
  userId: z.string(),
});

export function toolGrantRecordFromJSON(
  jsonString: string,
): SafeParseResult<ToolGrantRecord, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ToolGrantRecord$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ToolGrantRecord' from JSON`,
  );
}
