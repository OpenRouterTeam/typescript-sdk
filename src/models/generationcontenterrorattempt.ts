/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0a3c55570f8e
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A single failed upstream attempt recorded for the generation
 */
export type GenerationContentErrorAttempt = {
  /**
   * HTTP status returned by this attempt
   */
  code: number;
  /**
   * Error message returned by this attempt
   */
  message: string;
  /**
   * Provider that served this attempt, when known
   */
  providerName: string | null;
  /**
   * Raw error body returned by the provider, when stored
   */
  raw: string | null;
};

/** @internal */
export const GenerationContentErrorAttempt$inboundSchema: z.ZodType<
  GenerationContentErrorAttempt,
  unknown
> = z.object({
  code: z.int(),
  message: z.string(),
  provider_name: z.nullable(z.string()),
  raw: z.nullable(z.string()),
}).transform((v) => {
  return remap$(v, {
    "provider_name": "providerName",
  });
});

export function generationContentErrorAttemptFromJSON(
  jsonString: string,
): SafeParseResult<GenerationContentErrorAttempt, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GenerationContentErrorAttempt$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GenerationContentErrorAttempt' from JSON`,
  );
}
