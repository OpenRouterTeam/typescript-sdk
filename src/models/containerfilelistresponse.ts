/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 74b945b6c8e1
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { ContainerFile, ContainerFile$inboundSchema } from "./containerfile.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const ContainerFileListResponseObject = {
  List: "list",
} as const;
export type ContainerFileListResponseObject = ClosedEnum<
  typeof ContainerFileListResponseObject
>;

export type ContainerFileListResponse = {
  data: Array<ContainerFile>;
  firstId: string | null;
  /**
   * True when another page can be fetched by passing `after=last_id`; `last_id` is non-null whenever this is true.
   */
  hasMore: boolean;
  /**
   * Cursor for the next page (pass as `after`). The last entry’s id, except when the page stopped at the per-request scan bound on hidden bookkeeping objects: then it names the scan position and may not appear in `data`. Null only when `has_more` is false and `data` is empty.
   */
  lastId: string | null;
  object: ContainerFileListResponseObject;
};

/** @internal */
export const ContainerFileListResponseObject$inboundSchema: z.ZodEnum<
  typeof ContainerFileListResponseObject
> = z.enum(ContainerFileListResponseObject);

/** @internal */
export const ContainerFileListResponse$inboundSchema: z.ZodType<
  ContainerFileListResponse,
  unknown
> = z.object({
  data: z.array(ContainerFile$inboundSchema),
  first_id: z.nullable(z.string()),
  has_more: z.boolean(),
  last_id: z.nullable(z.string()),
  object: ContainerFileListResponseObject$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "first_id": "firstId",
    "has_more": "hasMore",
    "last_id": "lastId",
  });
});

export function containerFileListResponseFromJSON(
  jsonString: string,
): SafeParseResult<ContainerFileListResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContainerFileListResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContainerFileListResponse' from JSON`,
  );
}
