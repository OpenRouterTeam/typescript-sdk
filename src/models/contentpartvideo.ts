/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5845b0ae6dcf
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

export type VideoUrl = {
  url: string;
};

export type ContentPartVideo = {
  type: "video_url";
  videoUrl: VideoUrl;
};

/** @internal */
export type VideoUrl$Outbound = {
  url: string;
};

/** @internal */
export const VideoUrl$outboundSchema: z.ZodType<VideoUrl$Outbound, VideoUrl> = z
  .object({
    url: z.string(),
  });

export function videoUrlToJSON(videoUrl: VideoUrl): string {
  return JSON.stringify(VideoUrl$outboundSchema.parse(videoUrl));
}

/** @internal */
export type ContentPartVideo$Outbound = {
  type: "video_url";
  video_url: VideoUrl$Outbound;
};

/** @internal */
export const ContentPartVideo$outboundSchema: z.ZodType<
  ContentPartVideo$Outbound,
  ContentPartVideo
> = z.object({
  type: z.literal("video_url"),
  videoUrl: z.lazy(() => VideoUrl$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    videoUrl: "video_url",
  });
});

export function contentPartVideoToJSON(
  contentPartVideo: ContentPartVideo,
): string {
  return JSON.stringify(
    ContentPartVideo$outboundSchema.parse(contentPartVideo),
  );
}
