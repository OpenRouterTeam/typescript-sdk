/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d737d34295bb
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type PipelineStage = {
  costUsd?: number | null | undefined;
  data?: { [k: string]: any | null } | undefined;
  guardrailId?: string | undefined;
  guardrailScope?: string | undefined;
  name: string;
  type: string;
};

/** @internal */
export const PipelineStage$inboundSchema: z.ZodType<PipelineStage, unknown> = z
  .object({
    cost_usd: z.nullable(z.number()).optional(),
    data: z.record(z.string(), z.nullable(z.any())).optional(),
    guardrail_id: z.string().optional(),
    guardrail_scope: z.string().optional(),
    name: z.string(),
    type: z.string(),
  }).transform((v) => {
    return remap$(v, {
      "cost_usd": "costUsd",
      "guardrail_id": "guardrailId",
      "guardrail_scope": "guardrailScope",
    });
  });

export function pipelineStageFromJSON(
  jsonString: string,
): SafeParseResult<PipelineStage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PipelineStage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PipelineStage' from JSON`,
  );
}
