/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 259a18622bbe
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A specific version of a preset, containing config and optional system prompt.
 */
export type PresetDesignatedVersion = {
  config: { [k: string]: any | null };
  createdAt: string;
  creatorId: string;
  id: string;
  presetId: string;
  systemPrompt: string | null;
  updatedAt: string;
  version: number;
};

/** @internal */
export const PresetDesignatedVersion$inboundSchema: z.ZodType<
  PresetDesignatedVersion,
  unknown
> = z.object({
  config: z.record(z.string(), z.nullable(z.any())),
  created_at: z.string(),
  creator_id: z.string(),
  id: z.string(),
  preset_id: z.string(),
  system_prompt: z.nullable(z.string()),
  updated_at: z.string(),
  version: z.int(),
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
    "creator_id": "creatorId",
    "preset_id": "presetId",
    "system_prompt": "systemPrompt",
    "updated_at": "updatedAt",
  });
});

export function presetDesignatedVersionFromJSON(
  jsonString: string,
): SafeParseResult<PresetDesignatedVersion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PresetDesignatedVersion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PresetDesignatedVersion' from JSON`,
  );
}
