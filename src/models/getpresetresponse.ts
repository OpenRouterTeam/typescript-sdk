/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f3c5ddbd4b26
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Preset, Preset$inboundSchema } from "./preset.js";
import {
  PresetDesignatedVersion,
  PresetDesignatedVersion$inboundSchema,
} from "./presetdesignatedversion.js";

/**
 * A preset with a paginated list of its versions.
 */
export type GetPresetResponse = {
  /**
   * A preset without version details.
   */
  data: Preset;
  totalCount: number;
  versions: Array<PresetDesignatedVersion | null>;
};

/** @internal */
export const GetPresetResponse$inboundSchema: z.ZodType<
  GetPresetResponse,
  unknown
> = z.object({
  data: Preset$inboundSchema,
  total_count: z.int(),
  versions: z.array(z.nullable(PresetDesignatedVersion$inboundSchema)),
}).transform((v) => {
  return remap$(v, {
    "total_count": "totalCount",
  });
});

export function getPresetResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetPresetResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetPresetResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetPresetResponse' from JSON`,
  );
}
