/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 71b1c1f3a7cc
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  DisplayPricingTier,
  DisplayPricingTier$inboundSchema,
} from "./displaypricingtier.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Whether this price is per-token ('token') or per-unit ('unit', e.g. per image, per second)
 */
export const Kind = {
  Token: "token",
  Unit: "unit",
} as const;
/**
 * Whether this price is per-token ('token') or per-unit ('unit', e.g. per image, per second)
 */
export type Kind = OpenEnum<typeof Kind>;

/**
 * Structured pricing line item with SKU label and display metadata
 */
export type DisplayPricingItem = {
  /**
   * Multiplier for display purposes (e.g. 1000000 means price is shown per million tokens)
   */
  displayMultiplier: number;
  /**
   * Whether this price is per-token ('token') or per-unit ('unit', e.g. per image, per second)
   */
  kind: Kind;
  /**
   * Price as a decimal string in USD (before applying displayMultiplier)
   */
  price: string;
  /**
   * Human-readable label for this SKU (e.g. "Input Price", "Video Output")
   */
  skuLabel: string;
  /**
   * Optional pricing tiers (e.g. different resolutions or quality levels)
   */
  tiers?: Array<DisplayPricingTier> | undefined;
  /**
   * Unit label for display (e.g. "/M tokens", "/image", "/second")
   */
  unitLabel: string;
};

/** @internal */
export const Kind$inboundSchema: z.ZodType<Kind, unknown> = openEnums
  .inboundSchema(Kind);

/** @internal */
export const DisplayPricingItem$inboundSchema: z.ZodType<
  DisplayPricingItem,
  unknown
> = z.object({
  displayMultiplier: z.number(),
  kind: Kind$inboundSchema,
  price: z.string(),
  sku_label: z.string(),
  tiers: z.array(DisplayPricingTier$inboundSchema).optional(),
  unitLabel: z.string(),
}).transform((v) => {
  return remap$(v, {
    "sku_label": "skuLabel",
  });
});

export function displayPricingItemFromJSON(
  jsonString: string,
): SafeParseResult<DisplayPricingItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DisplayPricingItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DisplayPricingItem' from JSON`,
  );
}
