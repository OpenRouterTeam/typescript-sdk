/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2676e6003b5f
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Image-specific search settings. Only meaningful when `search_content_types` includes `"image"`.
 */
export type WebSearchImageSettings = {
  caption?: boolean | null | undefined;
  maxResults?: number | null | undefined;
};

/** @internal */
export const WebSearchImageSettings$inboundSchema: z.ZodType<
  WebSearchImageSettings,
  unknown
> = z.object({
  caption: z.nullable(z.boolean()).optional(),
  max_results: z.nullable(z.int()).optional(),
}).transform((v) => {
  return remap$(v, {
    "max_results": "maxResults",
  });
});
/** @internal */
export type WebSearchImageSettings$Outbound = {
  caption?: boolean | null | undefined;
  max_results?: number | null | undefined;
};

/** @internal */
export const WebSearchImageSettings$outboundSchema: z.ZodType<
  WebSearchImageSettings$Outbound,
  WebSearchImageSettings
> = z.object({
  caption: z.nullable(z.boolean()).optional(),
  maxResults: z.nullable(z.int()).optional(),
}).transform((v) => {
  return remap$(v, {
    maxResults: "max_results",
  });
});

export function webSearchImageSettingsToJSON(
  webSearchImageSettings: WebSearchImageSettings,
): string {
  return JSON.stringify(
    WebSearchImageSettings$outboundSchema.parse(webSearchImageSettings),
  );
}
export function webSearchImageSettingsFromJSON(
  jsonString: string,
): SafeParseResult<WebSearchImageSettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WebSearchImageSettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WebSearchImageSettings' from JSON`,
  );
}
