/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a933ffbb5346
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as discriminatedUnionTypes from "../types/discriminatedUnion.js";
import { discriminatedUnion } from "../types/discriminatedUnion.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ShellCallStatus,
  ShellCallStatus$inboundSchema,
} from "./shellcallstatus.js";

export type OutcomeTimeout = {
  type: "timeout";
};

export type OutcomeExit = {
  exitCode: number;
  type: "exit";
};

export type Outcome =
  | OutcomeExit
  | OutcomeTimeout
  | discriminatedUnionTypes.Unknown<"type">;

export type OutputShellCallOutputItemOutput = {
  outcome:
    | OutcomeExit
    | OutcomeTimeout
    | discriminatedUnionTypes.Unknown<"type">;
  stderr: string;
  stdout: string;
};

/**
 * A native `shell_call_output` item matching OpenAI's Responses API shape. Carries per-command stdout, stderr, and the exit/timeout outcome.
 */
export type OutputShellCallOutputItem = {
  callId: string;
  id: string;
  maxOutputLength?: number | null | undefined;
  output: Array<OutputShellCallOutputItemOutput>;
  /**
   * Status of a shell call or its output.
   */
  status: ShellCallStatus;
  type: "shell_call_output";
};

/** @internal */
export const OutcomeTimeout$inboundSchema: z.ZodType<OutcomeTimeout, unknown> =
  z.object({
    type: z.literal("timeout"),
  });

export function outcomeTimeoutFromJSON(
  jsonString: string,
): SafeParseResult<OutcomeTimeout, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OutcomeTimeout$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OutcomeTimeout' from JSON`,
  );
}

/** @internal */
export const OutcomeExit$inboundSchema: z.ZodType<OutcomeExit, unknown> = z
  .object({
    exit_code: z.int(),
    type: z.literal("exit"),
  }).transform((v) => {
    return remap$(v, {
      "exit_code": "exitCode",
    });
  });

export function outcomeExitFromJSON(
  jsonString: string,
): SafeParseResult<OutcomeExit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OutcomeExit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OutcomeExit' from JSON`,
  );
}

/** @internal */
export const Outcome$inboundSchema: z.ZodType<Outcome, unknown> =
  discriminatedUnion("type", {
    exit: z.lazy(() => OutcomeExit$inboundSchema),
    timeout: z.lazy(() => OutcomeTimeout$inboundSchema),
  });

export function outcomeFromJSON(
  jsonString: string,
): SafeParseResult<Outcome, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Outcome$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Outcome' from JSON`,
  );
}

/** @internal */
export const OutputShellCallOutputItemOutput$inboundSchema: z.ZodType<
  OutputShellCallOutputItemOutput,
  unknown
> = z.object({
  outcome: discriminatedUnion("type", {
    exit: z.lazy(() => OutcomeExit$inboundSchema),
    timeout: z.lazy(() => OutcomeTimeout$inboundSchema),
  }),
  stderr: z.string(),
  stdout: z.string(),
});

export function outputShellCallOutputItemOutputFromJSON(
  jsonString: string,
): SafeParseResult<OutputShellCallOutputItemOutput, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OutputShellCallOutputItemOutput$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OutputShellCallOutputItemOutput' from JSON`,
  );
}

/** @internal */
export const OutputShellCallOutputItem$inboundSchema: z.ZodType<
  OutputShellCallOutputItem,
  unknown
> = z.object({
  call_id: z.string(),
  id: z.string(),
  max_output_length: z.nullable(z.int()).optional(),
  output: z.array(z.lazy(() => OutputShellCallOutputItemOutput$inboundSchema)),
  status: ShellCallStatus$inboundSchema,
  type: z.literal("shell_call_output"),
}).transform((v) => {
  return remap$(v, {
    "call_id": "callId",
    "max_output_length": "maxOutputLength",
  });
});

export function outputShellCallOutputItemFromJSON(
  jsonString: string,
): SafeParseResult<OutputShellCallOutputItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OutputShellCallOutputItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OutputShellCallOutputItem' from JSON`,
  );
}
