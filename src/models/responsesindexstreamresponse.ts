/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 49957e6d74e7
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { StreamEvents, StreamEvents$inboundSchema } from "./streamevents.js";

/**
 * Server-sent event payload for a streaming OpenAI Responses event
 */
export type ResponsesIndexStreamResponse = {
  /**
   * Union of all possible event types emitted during response streaming
   */
  data: StreamEvents;
};

/** @internal */
export const ResponsesIndexStreamResponse$inboundSchema: z.ZodType<
  ResponsesIndexStreamResponse,
  unknown
> = z.object({
  data: z.string().transform((v, ctx) => {
    try {
      return JSON.parse(v);
    } catch (err) {
      ctx.addIssue({
        input: v,
        code: "custom",
        message: `malformed json: ${err}`,
      });
      return z.NEVER;
    }
  }).pipe(StreamEvents$inboundSchema),
});

export function responsesIndexStreamResponseFromJSON(
  jsonString: string,
): SafeParseResult<ResponsesIndexStreamResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponsesIndexStreamResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponsesIndexStreamResponse' from JSON`,
  );
}
