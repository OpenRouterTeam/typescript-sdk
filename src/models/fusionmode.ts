/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2c65a580eb35
 */

import * as z from "zod/v4";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";

/**
 * Pipeline mode. "full" (default) runs the N-panel + judge pipeline. "fast" routes to a single panel model (the first in analysis_models) without fan-out or judge synthesis — ~3x faster for mechanical turns.
 */
export const FusionMode = {
  Full: "full",
  Fast: "fast",
} as const;
/**
 * Pipeline mode. "full" (default) runs the N-panel + judge pipeline. "fast" routes to a single panel model (the first in analysis_models) without fan-out or judge synthesis — ~3x faster for mechanical turns.
 */
export type FusionMode = OpenEnum<typeof FusionMode>;

/** @internal */
export const FusionMode$outboundSchema: z.ZodType<string, FusionMode> =
  openEnums.outboundSchema(FusionMode);
