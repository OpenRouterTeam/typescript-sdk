/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 999ae13f294f
 */

import * as z from "zod/v4";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";

/**
 * Controls which reasoning is available to the model. `auto` uses the model default (same as omitting); `all_turns` includes reasoning from earlier turns passed in input; `current_turn` limits to the current turn only.
 */
export const ReasoningContext = {
  Auto: "auto",
  AllTurns: "all_turns",
  CurrentTurn: "current_turn",
} as const;
/**
 * Controls which reasoning is available to the model. `auto` uses the model default (same as omitting); `all_turns` includes reasoning from earlier turns passed in input; `current_turn` limits to the current turn only.
 */
export type ReasoningContext = OpenEnum<typeof ReasoningContext>;

/** @internal */
export const ReasoningContext$inboundSchema: z.ZodType<
  ReasoningContext,
  unknown
> = openEnums.inboundSchema(ReasoningContext);
/** @internal */
export const ReasoningContext$outboundSchema: z.ZodType<
  string,
  ReasoningContext
> = openEnums.outboundSchema(ReasoningContext);
