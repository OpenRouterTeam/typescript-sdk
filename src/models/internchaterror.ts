/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9bf0417c1e10
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  InternChatErrorMetadata,
  InternChatErrorMetadata$inboundSchema,
} from "./internchaterrormetadata.js";

/**
 * The OpenAI-compatible error object. `metadata` is present on refusals from the chat route. Authentication refusals (`401`), the departed-creator `403` and the programme `404` carry only `code` and `message`.
 */
export type InternChatError = {
  /**
   * The HTTP status of the response.
   */
  code: number;
  message: string;
  /**
   * Machine-readable detail for the failure.
   */
  metadata?: InternChatErrorMetadata | undefined;
};

/** @internal */
export const InternChatError$inboundSchema: z.ZodType<
  InternChatError,
  unknown
> = z.object({
  code: z.int(),
  message: z.string(),
  metadata: InternChatErrorMetadata$inboundSchema.optional(),
});

export function internChatErrorFromJSON(
  jsonString: string,
): SafeParseResult<InternChatError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InternChatError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InternChatError' from JSON`,
  );
}
