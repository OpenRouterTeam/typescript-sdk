/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4de0c36d6255
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * The generation phase this chunk belongs to. `content` is the renderable output; `reasoning` and `draft` are intermediate provider phases.
 */
export const ImageGenTextChunkEventPhase = {
  Content: "content",
  Reasoning: "reasoning",
  Draft: "draft",
} as const;
/**
 * The generation phase this chunk belongs to. `content` is the renderable output; `reasoning` and `draft` are intermediate provider phases.
 */
export type ImageGenTextChunkEventPhase = OpenEnum<
  typeof ImageGenTextChunkEventPhase
>;

/**
 * Emitted when a text chunk becomes available during streaming generation of text-based formats (e.g. SVG)
 */
export type ImageGenTextChunkEvent = {
  /**
   * The generation phase this chunk belongs to. `content` is the renderable output; `reasoning` and `draft` are intermediate provider phases.
   */
  phase: ImageGenTextChunkEventPhase;
  /**
   * A text fragment of the image being generated (e.g. partial SVG markup)
   */
  text: string;
  /**
   * The event type
   */
  type: "image_generation.text_chunk";
};

/** @internal */
export const ImageGenTextChunkEventPhase$inboundSchema: z.ZodType<
  ImageGenTextChunkEventPhase,
  unknown
> = openEnums.inboundSchema(ImageGenTextChunkEventPhase);

/** @internal */
export const ImageGenTextChunkEvent$inboundSchema: z.ZodType<
  ImageGenTextChunkEvent,
  unknown
> = z.object({
  phase: ImageGenTextChunkEventPhase$inboundSchema,
  text: z.string(),
  type: z.literal("image_generation.text_chunk"),
});

export function imageGenTextChunkEventFromJSON(
  jsonString: string,
): SafeParseResult<ImageGenTextChunkEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ImageGenTextChunkEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ImageGenTextChunkEvent' from JSON`,
  );
}
