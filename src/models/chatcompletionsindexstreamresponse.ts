/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 60546603ac6d
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  ChatStreamChunk,
  ChatStreamChunk$inboundSchema,
} from "./chatstreamchunk.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Server-sent event payload for a streaming chat completion chunk
 */
export type ChatCompletionsIndexStreamResponse = {
  /**
   * Streaming chat completion chunk
   */
  data: ChatStreamChunk;
};

/** @internal */
export const ChatCompletionsIndexStreamResponse$inboundSchema: z.ZodType<
  ChatCompletionsIndexStreamResponse,
  unknown
> = z.object({
  data: z.string().transform((v, ctx) => {
    try {
      return JSON.parse(v);
    } catch (err) {
      ctx.addIssue({
        input: v,
        code: "custom",
        message: `malformed json: ${err}`,
      });
      return z.NEVER;
    }
  }).pipe(ChatStreamChunk$inboundSchema),
});

export function chatCompletionsIndexStreamResponseFromJSON(
  jsonString: string,
): SafeParseResult<ChatCompletionsIndexStreamResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ChatCompletionsIndexStreamResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ChatCompletionsIndexStreamResponse' from JSON`,
  );
}
