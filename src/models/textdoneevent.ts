/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 006e3645c287
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { StreamLogprob, StreamLogprob$inboundSchema } from "./streamlogprob.js";

/**
 * Event emitted when text streaming is complete
 */
export type TextDoneEvent = {
  type: "response.output_text.done";
  outputIndex: number;
  itemId: string;
  contentIndex: number;
  text: string;
  sequenceNumber: number;
  logprobs: Array<StreamLogprob>;
};

/** @internal */
export const TextDoneEvent$inboundSchema: z.ZodType<TextDoneEvent, unknown> = z
  .object({
    type: z.literal("response.output_text.done"),
    output_index: z.int(),
    item_id: z.string(),
    content_index: z.int(),
    text: z.string(),
    sequence_number: z.int(),
    logprobs: z.array(StreamLogprob$inboundSchema),
  }).transform((v) => {
    return remap$(v, {
      "output_index": "outputIndex",
      "item_id": "itemId",
      "content_index": "contentIndex",
      "sequence_number": "sequenceNumber",
    });
  });

export function textDoneEventFromJSON(
  jsonString: string,
): SafeParseResult<TextDoneEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextDoneEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextDoneEvent' from JSON`,
  );
}
