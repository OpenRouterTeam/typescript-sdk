/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6e46d93381d7
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Dataset version. Field names and grain are stable for the life of `v1`.
 */
export const RankingsDailyMetaVersion = {
  V1: "v1",
} as const;
/**
 * Dataset version. Field names and grain are stable for the life of `v1`.
 */
export type RankingsDailyMetaVersion = ClosedEnum<
  typeof RankingsDailyMetaVersion
>;

export type RankingsDailyMeta = {
  /**
   * ISO-8601 timestamp of when the response was generated. Reflects data-freshness because the underlying materialized view continuously ingests upstream events.
   */
  asOf: string;
  /**
   * Resolved end of the date window (UTC, inclusive).
   */
  endDate: string;
  /**
   * Resolved start of the date window (UTC, inclusive).
   */
  startDate: string;
  /**
   * Dataset version. Field names and grain are stable for the life of `v1`.
   */
  version: RankingsDailyMetaVersion;
};

/** @internal */
export const RankingsDailyMetaVersion$inboundSchema: z.ZodEnum<
  typeof RankingsDailyMetaVersion
> = z.enum(RankingsDailyMetaVersion);

/** @internal */
export const RankingsDailyMeta$inboundSchema: z.ZodType<
  RankingsDailyMeta,
  unknown
> = z.object({
  as_of: z.string(),
  end_date: z.string(),
  start_date: z.string(),
  version: RankingsDailyMetaVersion$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "as_of": "asOf",
    "end_date": "endDate",
    "start_date": "startDate",
  });
});

export function rankingsDailyMetaFromJSON(
  jsonString: string,
): SafeParseResult<RankingsDailyMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RankingsDailyMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RankingsDailyMeta' from JSON`,
  );
}
