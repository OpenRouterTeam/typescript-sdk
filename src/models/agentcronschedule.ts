/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 83c2c42912b4
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Cron schedule. When unset, the agent runs only on explicit invocation.
 */
export type AgentCronSchedule = {
  /**
   * Whether the schedule is currently active. Toggle to pause without losing the cron expression.
   */
  enabled?: boolean | undefined;
  /**
   * Standard cron expression (5 or 6 fields). Interpreted in `timezone` — UTC if not specified.
   */
  expression: string;
  /**
   * IANA timezone name.
   */
  timezone?: string | undefined;
};

/** @internal */
export const AgentCronSchedule$inboundSchema: z.ZodType<
  AgentCronSchedule,
  unknown
> = z.object({
  enabled: z.boolean().default(true),
  expression: z.string(),
  timezone: z.string().default("UTC"),
});
/** @internal */
export type AgentCronSchedule$Outbound = {
  enabled: boolean;
  expression: string;
  timezone: string;
};

/** @internal */
export const AgentCronSchedule$outboundSchema: z.ZodType<
  AgentCronSchedule$Outbound,
  AgentCronSchedule
> = z.object({
  enabled: z.boolean().default(true),
  expression: z.string(),
  timezone: z.string().default("UTC"),
});

export function agentCronScheduleToJSON(
  agentCronSchedule: AgentCronSchedule,
): string {
  return JSON.stringify(
    AgentCronSchedule$outboundSchema.parse(agentCronSchedule),
  );
}
export function agentCronScheduleFromJSON(
  jsonString: string,
): SafeParseResult<AgentCronSchedule, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentCronSchedule$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentCronSchedule' from JSON`,
  );
}
