/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 087aea86ca9d
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * A prompt to run on the intern without holding a connection open.
 */
export type InternInvokeRequest = {
  /**
   * The prompt for the run, at most 32000 characters.
   */
  input: string;
  /**
   * The session to run in. Send the `session_id` from an earlier `202` to continue that conversation, for example to hand the intern a decision on a case it is working. Omit it to start a new session. Only a `session_id` this endpoint issued to the same caller on the same intern is accepted; any other is refused with `404`.
   */
  sessionId?: string | undefined;
};

/** @internal */
export type InternInvokeRequest$Outbound = {
  input: string;
  session_id?: string | undefined;
};

/** @internal */
export const InternInvokeRequest$outboundSchema: z.ZodType<
  InternInvokeRequest$Outbound,
  InternInvokeRequest
> = z.object({
  input: z.string(),
  sessionId: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    sessionId: "session_id",
  });
});

export function internInvokeRequestToJSON(
  internInvokeRequest: InternInvokeRequest,
): string {
  return JSON.stringify(
    InternInvokeRequest$outboundSchema.parse(internInvokeRequest),
  );
}
