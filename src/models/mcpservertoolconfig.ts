/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 43f1dc2ed1ca
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * Configuration for the openrouter:mcp server tool
 */
export type McpServerToolConfig = {
  /**
   * Restrict which tools from the MCP server can be called. If omitted, all tools are allowed.
   */
  allowedTools?: Array<string> | undefined;
  /**
   * Authorization header value for the MCP server (e.g. "Bearer sk-...").
   */
  authorization?: string | undefined;
  /**
   * Custom HTTP headers to send with requests to the MCP server.
   */
  headers?: { [k: string]: string } | undefined;
  /**
   * A description of the MCP server for the model to understand its purpose.
   */
  serverDescription?: string | undefined;
  /**
   * A human-readable label for the MCP server.
   */
  serverLabel?: string | undefined;
  /**
   * The URL of the remote MCP server to connect to.
   */
  serverUrl: string;
};

/** @internal */
export type McpServerToolConfig$Outbound = {
  allowed_tools?: Array<string> | undefined;
  authorization?: string | undefined;
  headers?: { [k: string]: string } | undefined;
  server_description?: string | undefined;
  server_label?: string | undefined;
  server_url: string;
};

/** @internal */
export const McpServerToolConfig$outboundSchema: z.ZodType<
  McpServerToolConfig$Outbound,
  McpServerToolConfig
> = z.object({
  allowedTools: z.array(z.string()).optional(),
  authorization: z.string().optional(),
  headers: z.record(z.string(), z.string()).optional(),
  serverDescription: z.string().optional(),
  serverLabel: z.string().optional(),
  serverUrl: z.string(),
}).transform((v) => {
  return remap$(v, {
    allowedTools: "allowed_tools",
    serverDescription: "server_description",
    serverLabel: "server_label",
    serverUrl: "server_url",
  });
});

export function mcpServerToolConfigToJSON(
  mcpServerToolConfig: McpServerToolConfig,
): string {
  return JSON.stringify(
    McpServerToolConfig$outboundSchema.parse(mcpServerToolConfig),
  );
}
