/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 31085cbdd2cc
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * Configuration for the openrouter:deep_research server tool.
 */
export type DeepResearchServerToolConfig = {
  /**
   * Maximum number of research iterations (sub-question rounds). Defaults to 10. Capped at 50.
   */
  maxIterations?: number | undefined;
  /**
   * Maximum total page reads across all iterations. Defaults to 30. Capped at 200.
   */
  maxReads?: number | undefined;
  /**
   * Maximum total web searches across all iterations. Defaults to 30. Capped at 200.
   */
  maxSearches?: number | undefined;
  /**
   * Maximum agentic tool-calling steps each research iteration gets. Defaults to 8. Capped at 16.
   */
  maxToolCalls?: number | undefined;
  /**
   * Slug of the model to use for all research phases (planning, iteration, synthesis). When omitted, defaults to the model used in the outer API request.
   */
  model?: string | undefined;
};

/** @internal */
export type DeepResearchServerToolConfig$Outbound = {
  max_iterations?: number | undefined;
  max_reads?: number | undefined;
  max_searches?: number | undefined;
  max_tool_calls?: number | undefined;
  model?: string | undefined;
};

/** @internal */
export const DeepResearchServerToolConfig$outboundSchema: z.ZodType<
  DeepResearchServerToolConfig$Outbound,
  DeepResearchServerToolConfig
> = z.object({
  maxIterations: z.int().optional(),
  maxReads: z.int().optional(),
  maxSearches: z.int().optional(),
  maxToolCalls: z.int().optional(),
  model: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    maxIterations: "max_iterations",
    maxReads: "max_reads",
    maxSearches: "max_searches",
    maxToolCalls: "max_tool_calls",
  });
});

export function deepResearchServerToolConfigToJSON(
  deepResearchServerToolConfig: DeepResearchServerToolConfig,
): string {
  return JSON.stringify(
    DeepResearchServerToolConfig$outboundSchema.parse(
      deepResearchServerToolConfig,
    ),
  );
}
