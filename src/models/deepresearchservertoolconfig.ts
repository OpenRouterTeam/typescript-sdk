/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 31085cbdd2cc
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";

/**
 * How much context to retrieve per search result. Defaults to `medium` (15000 chars).
 */
export const SearchContextSize = {
  Low: "low",
  Medium: "medium",
  High: "high",
} as const;
/**
 * How much context to retrieve per search result. Defaults to `medium` (15000 chars).
 */
export type SearchContextSize = OpenEnum<typeof SearchContextSize>;

/**
 * Configuration for the openrouter:deep_research server tool
 */
export type DeepResearchServerToolConfig = {
  /**
   * Limit search results to these domains. Supported by Exa, Firecrawl, Parallel, and most native providers (Anthropic, OpenAI, xAI). Not supported with Perplexity. Cannot be used with excluded_domains.
   */
  allowedDomains?: Array<string> | undefined;
  /**
   * Exclude search results from these domains. Supported by Exa, Firecrawl, Parallel, Anthropic, and xAI. Not supported with OpenAI (silently ignored) or Perplexity. Cannot be used with allowed_domains.
   */
  excludedDomains?: Array<string> | undefined;
  /**
   * Maximum number of URLs to deep-fetch in a single deep_research call. Defaults to 8.
   */
  maxFetches?: number | undefined;
  /**
   * Maximum number of search results to return per sub-query. Defaults to 5.
   */
  maxResultsPerQuery?: number | undefined;
  /**
   * Maximum number of sub-queries the model may pass in addition to the main query. Defaults to 5.
   */
  maxSubQueries?: number | undefined;
  /**
   * Maximum number of deep_research calls per request. Defaults to 3.
   */
  maxUses?: number | undefined;
  /**
   * Maximum content length per fetched page (approximate tokens). Pages exceeding this are truncated. Defaults to 6000.
   */
  perPageContentTokens?: number | undefined;
  /**
   * How much context to retrieve per search result. Defaults to `medium` (15000 chars).
   */
  searchContextSize?: SearchContextSize | undefined;
};

/** @internal */
export const SearchContextSize$outboundSchema: z.ZodType<
  string,
  SearchContextSize
> = openEnums.outboundSchema(SearchContextSize);

/** @internal */
export type DeepResearchServerToolConfig$Outbound = {
  allowed_domains?: Array<string> | undefined;
  excluded_domains?: Array<string> | undefined;
  max_fetches?: number | undefined;
  max_results_per_query?: number | undefined;
  max_sub_queries?: number | undefined;
  max_uses?: number | undefined;
  per_page_content_tokens?: number | undefined;
  search_context_size?: string | undefined;
};

/** @internal */
export const DeepResearchServerToolConfig$outboundSchema: z.ZodType<
  DeepResearchServerToolConfig$Outbound,
  DeepResearchServerToolConfig
> = z.object({
  allowedDomains: z.array(z.string()).optional(),
  excludedDomains: z.array(z.string()).optional(),
  maxFetches: z.int().optional(),
  maxResultsPerQuery: z.int().optional(),
  maxSubQueries: z.int().optional(),
  maxUses: z.int().optional(),
  perPageContentTokens: z.int().optional(),
  searchContextSize: SearchContextSize$outboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    allowedDomains: "allowed_domains",
    excludedDomains: "excluded_domains",
    maxFetches: "max_fetches",
    maxResultsPerQuery: "max_results_per_query",
    maxSubQueries: "max_sub_queries",
    maxUses: "max_uses",
    perPageContentTokens: "per_page_content_tokens",
    searchContextSize: "search_context_size",
  });
});

export function deepResearchServerToolConfigToJSON(
  deepResearchServerToolConfig: DeepResearchServerToolConfig,
): string {
  return JSON.stringify(
    DeepResearchServerToolConfig$outboundSchema.parse(
      deepResearchServerToolConfig,
    ),
  );
}
