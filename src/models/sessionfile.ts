/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 603a6c4d4196
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const SessionFileType = {
  SessionFile: "session_file",
} as const;
export type SessionFileType = ClosedEnum<typeof SessionFileType>;

/**
 * A file created by a server-side tool sandbox during a session, identified by its path within the sandbox home directory.
 */
export type SessionFile = {
  modifiedAt: string;
  path: string;
  sizeBytes: number;
  type: SessionFileType;
};

/** @internal */
export const SessionFileType$inboundSchema: z.ZodEnum<typeof SessionFileType> =
  z.enum(SessionFileType);

/** @internal */
export const SessionFile$inboundSchema: z.ZodType<SessionFile, unknown> = z
  .object({
    modified_at: z.string(),
    path: z.string(),
    size_bytes: z.int(),
    type: SessionFileType$inboundSchema,
  }).transform((v) => {
    return remap$(v, {
      "modified_at": "modifiedAt",
      "size_bytes": "sizeBytes",
    });
  });

export function sessionFileFromJSON(
  jsonString: string,
): SafeParseResult<SessionFile, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionFile$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionFile' from JSON`,
  );
}
