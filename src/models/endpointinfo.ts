/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 33effd05ad97
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type EndpointInfo = {
  model: string;
  provider: string;
  selected: boolean;
  sortRank: number;
  sortValue: number | null;
};

/** @internal */
export const EndpointInfo$inboundSchema: z.ZodType<EndpointInfo, unknown> = z
  .object({
    model: z.string(),
    provider: z.string(),
    selected: z.boolean(),
    sort_rank: z.int(),
    sort_value: z.nullable(z.number()),
  }).transform((v) => {
    return remap$(v, {
      "sort_rank": "sortRank",
      "sort_value": "sortValue",
    });
  });

export function endpointInfoFromJSON(
  jsonString: string,
): SafeParseResult<EndpointInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EndpointInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EndpointInfo' from JSON`,
  );
}
