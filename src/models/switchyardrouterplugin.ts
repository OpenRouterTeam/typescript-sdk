/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 90395eb02944
 */

import * as z from "zod/v4";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";

/**
 * Routing algorithm for this request. "capability" calls a small judge model to rate how demanding the task is, then picks the efficient or capable candidate. "stage" reads the tool-result history (errors, repeated failures, edits landing) and calls the judge only when those signals are undecided. "auto" is "stage" without the judge call. "random" picks one candidate at random. "composite" keeps the tier chosen on the last human turn and re-evaluates tool turns with the stage signals. "passthrough" serves the eligible candidates in the order OpenRouter already ranked them, with no routing decision and no judge call. Omit this field to use the platform default, capability.
 */
export const Algorithm = {
  Capability: "capability",
  Stage: "stage",
  Auto: "auto",
  Random: "random",
  Composite: "composite",
  Passthrough: "passthrough",
} as const;
/**
 * Routing algorithm for this request. "capability" calls a small judge model to rate how demanding the task is, then picks the efficient or capable candidate. "stage" reads the tool-result history (errors, repeated failures, edits landing) and calls the judge only when those signals are undecided. "auto" is "stage" without the judge call. "random" picks one candidate at random. "composite" keeps the tier chosen on the last human turn and re-evaluates tool turns with the stage signals. "passthrough" serves the eligible candidates in the order OpenRouter already ranked them, with no routing decision and no judge call. Omit this field to use the platform default, capability.
 */
export type Algorithm = OpenEnum<typeof Algorithm>;

export type SwitchyardRouterPlugin = {
  /**
   * Routing algorithm for this request. "capability" calls a small judge model to rate how demanding the task is, then picks the efficient or capable candidate. "stage" reads the tool-result history (errors, repeated failures, edits landing) and calls the judge only when those signals are undecided. "auto" is "stage" without the judge call. "random" picks one candidate at random. "composite" keeps the tier chosen on the last human turn and re-evaluates tool turns with the stage signals. "passthrough" serves the eligible candidates in the order OpenRouter already ranked them, with no routing decision and no judge call. Omit this field to use the platform default, capability.
   */
  algorithm?: Algorithm | undefined;
  id: "switchyard-router";
};

/** @internal */
export const Algorithm$outboundSchema: z.ZodType<string, Algorithm> = openEnums
  .outboundSchema(Algorithm);

/** @internal */
export type SwitchyardRouterPlugin$Outbound = {
  algorithm?: string | undefined;
  id: "switchyard-router";
};

/** @internal */
export const SwitchyardRouterPlugin$outboundSchema: z.ZodType<
  SwitchyardRouterPlugin$Outbound,
  SwitchyardRouterPlugin
> = z.object({
  algorithm: Algorithm$outboundSchema.optional(),
  id: z.literal("switchyard-router"),
});

export function switchyardRouterPluginToJSON(
  switchyardRouterPlugin: SwitchyardRouterPlugin,
): string {
  return JSON.stringify(
    SwitchyardRouterPlugin$outboundSchema.parse(switchyardRouterPlugin),
  );
}
