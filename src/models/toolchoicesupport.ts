/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ccb12dd7321c
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Per-variant `tool_choice` support. `tool_choice` in `supported_parameters` only says the parameter is accepted; these flags say which of its values passed testing.
 */
export type ToolChoiceSupport = {
  /**
   * Whether the endpoint supports `"tool_choice": "auto"`.
   */
  auto: boolean;
  /**
   * Whether the endpoint supports naming a function, `"tool_choice": {"type": "function", ...}`.
   */
  function: boolean;
  /**
   * Whether the endpoint supports `"tool_choice": "none"`.
   */
  none: boolean;
  /**
   * Whether the endpoint supports `"tool_choice": "required"`.
   */
  required: boolean;
};

/** @internal */
export const ToolChoiceSupport$inboundSchema: z.ZodType<
  ToolChoiceSupport,
  unknown
> = z.object({
  auto: z.boolean(),
  function: z.boolean(),
  none: z.boolean(),
  required: z.boolean(),
});

export function toolChoiceSupportFromJSON(
  jsonString: string,
): SafeParseResult<ToolChoiceSupport, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ToolChoiceSupport$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ToolChoiceSupport' from JSON`,
  );
}
