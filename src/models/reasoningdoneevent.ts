/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 3af93f83e103
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Event emitted when reasoning text streaming is complete
 */
export type ReasoningDoneEvent = {
  type: "response.reasoning_text.done";
  outputIndex: number;
  itemId: string;
  contentIndex: number;
  text: string;
  sequenceNumber: number;
};

/** @internal */
export const ReasoningDoneEvent$inboundSchema: z.ZodType<
  ReasoningDoneEvent,
  unknown
> = z.object({
  type: z.literal("response.reasoning_text.done"),
  output_index: z.number(),
  item_id: z.string(),
  content_index: z.number(),
  text: z.string(),
  sequence_number: z.number(),
}).transform((v) => {
  return remap$(v, {
    "output_index": "outputIndex",
    "item_id": "itemId",
    "content_index": "contentIndex",
    "sequence_number": "sequenceNumber",
  });
});

export function reasoningDoneEventFromJSON(
  jsonString: string,
): SafeParseResult<ReasoningDoneEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReasoningDoneEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReasoningDoneEvent' from JSON`,
  );
}
