/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5a41ea9ea215
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { Budget, Budget$inboundSchema } from "./budget.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Budget state for the API key making the request
 */
export type BudgetResponseData = {
  /**
   * Every budget currently constraining this API key, in the order they are enforced.
   */
  budgets: Array<Budget>;
  /**
   * Whether any budget is already exhausted.
   */
  isExceeded: boolean;
  mostConstraining: Budget | null;
};

export type BudgetResponse = {
  /**
   * Budget state for the API key making the request
   */
  data: BudgetResponseData;
};

/** @internal */
export const BudgetResponseData$inboundSchema: z.ZodType<
  BudgetResponseData,
  unknown
> = z.object({
  budgets: z.array(Budget$inboundSchema),
  is_exceeded: z.boolean(),
  most_constraining: z.nullable(Budget$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "is_exceeded": "isExceeded",
    "most_constraining": "mostConstraining",
  });
});

export function budgetResponseDataFromJSON(
  jsonString: string,
): SafeParseResult<BudgetResponseData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BudgetResponseData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BudgetResponseData' from JSON`,
  );
}

/** @internal */
export const BudgetResponse$inboundSchema: z.ZodType<BudgetResponse, unknown> =
  z.object({
    data: z.lazy(() => BudgetResponseData$inboundSchema),
  });

export function budgetResponseFromJSON(
  jsonString: string,
): SafeParseResult<BudgetResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BudgetResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BudgetResponse' from JSON`,
  );
}
