/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 1559135a390c
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Emitted when `operation.diff` streaming completes for an `apply_patch_call`.
 */
export type ApplyPatchCallOperationDiffDoneEvent = {
  diff: string;
  itemId: string;
  outputIndex: number;
  sequenceNumber: number;
  type: "response.apply_patch_call_operation_diff.done";
};

/** @internal */
export const ApplyPatchCallOperationDiffDoneEvent$inboundSchema: z.ZodType<
  ApplyPatchCallOperationDiffDoneEvent,
  unknown
> = z.object({
  diff: z.string(),
  item_id: z.string(),
  output_index: z.int(),
  sequence_number: z.int(),
  type: z.literal("response.apply_patch_call_operation_diff.done"),
}).transform((v) => {
  return remap$(v, {
    "item_id": "itemId",
    "output_index": "outputIndex",
    "sequence_number": "sequenceNumber",
  });
});

export function applyPatchCallOperationDiffDoneEventFromJSON(
  jsonString: string,
): SafeParseResult<ApplyPatchCallOperationDiffDoneEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ApplyPatchCallOperationDiffDoneEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ApplyPatchCallOperationDiffDoneEvent' from JSON`,
  );
}
