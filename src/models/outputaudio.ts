/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5a700be84046
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * An audio output content part inside an assistant message.
 */
export type OutputAudio = {
  /**
   * Base64-encoded audio bytes returned by the model.
   */
  data: string;
  /**
   * Unix timestamp (seconds) at which the audio data expires.
   */
  expiresAt?: number | undefined;
  /**
   * Provider-assigned audio identifier (when available).
   */
  id?: string | undefined;
  /**
   * Text transcript of the audio output.
   */
  transcript: string;
  type: "output_audio";
};

/** @internal */
export const OutputAudio$inboundSchema: z.ZodType<OutputAudio, unknown> = z
  .object({
    data: z.string(),
    expires_at: z.int().optional(),
    id: z.string().optional(),
    transcript: z.string(),
    type: z.literal("output_audio"),
  }).transform((v) => {
    return remap$(v, {
      "expires_at": "expiresAt",
    });
  });
/** @internal */
export type OutputAudio$Outbound = {
  data: string;
  expires_at?: number | undefined;
  id?: string | undefined;
  transcript: string;
  type: "output_audio";
};

/** @internal */
export const OutputAudio$outboundSchema: z.ZodType<
  OutputAudio$Outbound,
  OutputAudio
> = z.object({
  data: z.string(),
  expiresAt: z.int().optional(),
  id: z.string().optional(),
  transcript: z.string(),
  type: z.literal("output_audio"),
}).transform((v) => {
  return remap$(v, {
    expiresAt: "expires_at",
  });
});

export function outputAudioToJSON(outputAudio: OutputAudio): string {
  return JSON.stringify(OutputAudio$outboundSchema.parse(outputAudio));
}
export function outputAudioFromJSON(
  jsonString: string,
): SafeParseResult<OutputAudio, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OutputAudio$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OutputAudio' from JSON`,
  );
}
