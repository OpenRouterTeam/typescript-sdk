/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f10a43356b30
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  InternChatDelta,
  InternChatDelta$inboundSchema,
} from "./internchatdelta.js";

/**
 * `null` while streaming. `stop` when the run completed, `tool_calls` when the run is waiting for the caller to answer the streamed tool call, `error` on the terminal error chunk.
 */
export const InternChatChoiceFinishReason = {
  Error: "error",
  Stop: "stop",
  ToolCalls: "tool_calls",
} as const;
/**
 * `null` while streaming. `stop` when the run completed, `tool_calls` when the run is waiting for the caller to answer the streamed tool call, `error` on the terminal error chunk.
 */
export type InternChatChoiceFinishReason = OpenEnum<
  typeof InternChatChoiceFinishReason
>;

/**
 * The single choice this endpoint streams.
 */
export type InternChatChoice = {
  /**
   * The incremental content of one chunk. The first chunk carries `role`, text chunks carry `content`, reasoning chunks carry `reasoning`, and an interaction chunk carries one complete `tool_calls` entry.
   */
  delta: InternChatDelta;
  /**
   * `null` while streaming. `stop` when the run completed, `tool_calls` when the run is waiting for the caller to answer the streamed tool call, `error` on the terminal error chunk.
   */
  finishReason: InternChatChoiceFinishReason | null;
  index: number;
};

/** @internal */
export const InternChatChoiceFinishReason$inboundSchema: z.ZodType<
  InternChatChoiceFinishReason,
  unknown
> = openEnums.inboundSchema(InternChatChoiceFinishReason);

/** @internal */
export const InternChatChoice$inboundSchema: z.ZodType<
  InternChatChoice,
  unknown
> = z.object({
  delta: InternChatDelta$inboundSchema,
  finish_reason: z.nullable(InternChatChoiceFinishReason$inboundSchema),
  index: z.int(),
}).transform((v) => {
  return remap$(v, {
    "finish_reason": "finishReason",
  });
});

export function internChatChoiceFromJSON(
  jsonString: string,
): SafeParseResult<InternChatChoice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InternChatChoice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InternChatChoice' from JSON`,
  );
}
