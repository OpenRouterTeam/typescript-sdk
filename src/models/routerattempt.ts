/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 353df41da8e5
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type RouterAttempt = {
  durationMs: number;
  model: string;
  provider: string;
  status: number;
};

/** @internal */
export const RouterAttempt$inboundSchema: z.ZodType<RouterAttempt, unknown> = z
  .object({
    duration_ms: z.int(),
    model: z.string(),
    provider: z.string(),
    status: z.int(),
  }).transform((v) => {
    return remap$(v, {
      "duration_ms": "durationMs",
    });
  });

export function routerAttemptFromJSON(
  jsonString: string,
): SafeParseResult<RouterAttempt, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RouterAttempt$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RouterAttempt' from JSON`,
  );
}
