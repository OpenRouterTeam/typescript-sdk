/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 94a6b307dab3
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AppRankingsItem = {
  /**
   * Stable numeric identifier of the app on OpenRouter.
   */
  appId: number;
  /**
   * Public display name of the app.
   */
  appName: string;
  /**
   * 1-based position of the app within this response, per the requested `sort`.
   */
  rank: number;
  /**
   * Number of requests attributed to the app inside the date window.
   */
  totalRequests: number;
  /**
   * Sum of `prompt_tokens + completion_tokens` attributed to the app inside the date window, returned as a decimal string so 64-bit values are not truncated.
   */
  totalTokens: string;
};

/** @internal */
export const AppRankingsItem$inboundSchema: z.ZodType<
  AppRankingsItem,
  unknown
> = z.object({
  app_id: z.int(),
  app_name: z.string(),
  rank: z.int(),
  total_requests: z.int(),
  total_tokens: z.string(),
}).transform((v) => {
  return remap$(v, {
    "app_id": "appId",
    "app_name": "appName",
    "total_requests": "totalRequests",
    "total_tokens": "totalTokens",
  });
});

export function appRankingsItemFromJSON(
  jsonString: string,
): SafeParseResult<AppRankingsItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AppRankingsItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AppRankingsItem' from JSON`,
  );
}
