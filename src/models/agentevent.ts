/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a806a03e63f5
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Event type, normalized across providers.
 */
export const AgentEventType = {
  SessionCreated: "session.created",
  SessionStatus: "session.status",
  SessionMessage: "session.message",
  SessionCompleted: "session.completed",
  SessionFailed: "session.failed",
} as const;
/**
 * Event type, normalized across providers.
 */
export type AgentEventType = OpenEnum<typeof AgentEventType>;

export type AgentEvent = {
  /**
   * Event payload. Shape depends on `type`; consumers should validate before use.
   */
  data?: any | null | undefined;
  /**
   * Session this event belongs to.
   */
  sessionId: string;
  /**
   * Event type, normalized across providers.
   */
  type: AgentEventType;
};

/** @internal */
export const AgentEventType$inboundSchema: z.ZodType<AgentEventType, unknown> =
  openEnums.inboundSchema(AgentEventType);

/** @internal */
export const AgentEvent$inboundSchema: z.ZodType<AgentEvent, unknown> = z
  .object({
    data: z.nullable(z.any()).optional(),
    session_id: z.string(),
    type: AgentEventType$inboundSchema,
  }).transform((v) => {
    return remap$(v, {
      "session_id": "sessionId",
    });
  });

export function agentEventFromJSON(
  jsonString: string,
): SafeParseResult<AgentEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentEvent' from JSON`,
  );
}
