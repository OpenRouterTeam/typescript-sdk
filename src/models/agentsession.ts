/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 91cc81fac922
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  AgentSessionResult,
  AgentSessionResult$inboundSchema,
} from "./agentsessionresult.js";
import {
  AgentSessionStatus,
  AgentSessionStatus$inboundSchema,
} from "./agentsessionstatus.js";
import { AgentSlug, AgentSlug$inboundSchema } from "./agentslug.js";
import { AgentSource, AgentSource$inboundSchema } from "./agentsource.js";
import { AgentUsage, AgentUsage$inboundSchema } from "./agentusage.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Object type discriminator.
 */
export const AgentSessionObject = {
  AgentSession: "agent.session",
} as const;
/**
 * Object type discriminator.
 */
export type AgentSessionObject = ClosedEnum<typeof AgentSessionObject>;

export type AgentSession = {
  /**
   * Identifier for the cloud agent to invoke. Format: `<provider>/<agent-name>`. New adapters are added by OpenRouter; the set is intentionally closed.
   */
  agent: AgentSlug;
  /**
   * ID of the AgentContext attached to this session, or null if no context was provided.
   */
  contextId: string | null;
  /**
   * ISO 8601 UTC timestamp when the session was created.
   */
  createdAt: Date;
  /**
   * Namespaced provider-specific extensions. Keys must be of the form `<provider>/<feature>`. Unknown extensions are passed through to the underlying provider.
   */
  extensions?: { [k: string]: any | null } | undefined;
  /**
   * Unique OpenRouter-issued session ID. Stable for the lifetime of the session.
   */
  id: string;
  /**
   * Object type discriminator.
   */
  object: AgentSessionObject;
  /**
   * The prompt the application sent to the agent.
   */
  prompt: string;
  result?: AgentSessionResult | undefined;
  /**
   * Where the agent should run. v1 ships GitHub-only; future versions will add other source types behind the `type` discriminator.
   */
  source?: AgentSource | undefined;
  /**
   * Normalized session status across all agent providers.
   */
  status: AgentSessionStatus;
  /**
   * ISO 8601 UTC timestamp of the most recent state change.
   */
  updatedAt: Date;
  usage?: AgentUsage | undefined;
};

/** @internal */
export const AgentSessionObject$inboundSchema: z.ZodEnum<
  typeof AgentSessionObject
> = z.enum(AgentSessionObject);

/** @internal */
export const AgentSession$inboundSchema: z.ZodType<AgentSession, unknown> = z
  .object({
    agent: AgentSlug$inboundSchema,
    context_id: z.nullable(z.string()),
    created_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
    extensions: z.record(z.string(), z.nullable(z.any())).optional(),
    id: z.string(),
    object: AgentSessionObject$inboundSchema,
    prompt: z.string(),
    result: AgentSessionResult$inboundSchema.optional(),
    source: AgentSource$inboundSchema.optional(),
    status: AgentSessionStatus$inboundSchema,
    updated_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
    usage: AgentUsage$inboundSchema.optional(),
  }).transform((v) => {
    return remap$(v, {
      "context_id": "contextId",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
    });
  });

export function agentSessionFromJSON(
  jsonString: string,
): SafeParseResult<AgentSession, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentSession$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentSession' from JSON`,
  );
}
