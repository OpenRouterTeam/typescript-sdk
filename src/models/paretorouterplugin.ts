/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4e557119b1dd
 */

import * as z from "zod/v4";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import {
  ParetoCeilings,
  ParetoCeilings$Outbound,
  ParetoCeilings$outboundSchema,
} from "./paretoceilings.js";
import {
  ParetoFloors,
  ParetoFloors$Outbound,
  ParetoFloors$outboundSchema,
} from "./paretofloors.js";
import {
  ParetoObjectives,
  ParetoObjectives$Outbound,
  ParetoObjectives$outboundSchema,
} from "./paretoobjectives.js";

/**
 * Preset routing strategy. "balanced" weights all objectives equally, "fast" prioritises low latency, "cheap" prioritises low cost, "quality" prioritises benchmark scores. Defaults depend on the router slug: "quality" for pareto-code, "fast" for pareto-agent. Ignored when explicit objectives are provided.
 */
export const Preset = {
  Balanced: "balanced",
  Fast: "fast",
  Cheap: "cheap",
  Quality: "quality",
  Custom: "custom",
} as const;
/**
 * Preset routing strategy. "balanced" weights all objectives equally, "fast" prioritises low latency, "cheap" prioritises low cost, "quality" prioritises benchmark scores. Defaults depend on the router slug: "quality" for pareto-code, "fast" for pareto-agent. Ignored when explicit objectives are provided.
 */
export type Preset = OpenEnum<typeof Preset>;

export type ParetoRouterPlugin = {
  /**
   * Hard maximum raw-value caps. Models exceeding a ceiling are penalised (score halved) but not removed. latency_ms is in milliseconds. cost_usd is per-token cost (sum of prompt + completion per-token prices).
   */
  ceilings?: ParetoCeilings | undefined;
  /**
   * Hard minimum scores (0-1, same scale as frontier entries). Models scoring below any floor are rejected before scoring.
   */
  floors?: ParetoFloors | undefined;
  id: "pareto-router";
  /**
   * Custom objective weights (0-1). Higher weight = more influence on model selection. Weights are normalised internally so they do not need to sum to 1. Overrides preset when provided.
   */
  objectives?: ParetoObjectives | undefined;
  /**
   * Preset routing strategy. "balanced" weights all objectives equally, "fast" prioritises low latency, "cheap" prioritises low cost, "quality" prioritises benchmark scores. Defaults depend on the router slug: "quality" for pareto-code, "fast" for pareto-agent. Ignored when explicit objectives are provided.
   */
  preset?: Preset | undefined;
};

/** @internal */
export const Preset$outboundSchema: z.ZodType<string, Preset> = openEnums
  .outboundSchema(Preset);

/** @internal */
export type ParetoRouterPlugin$Outbound = {
  ceilings?: ParetoCeilings$Outbound | undefined;
  floors?: ParetoFloors$Outbound | undefined;
  id: "pareto-router";
  objectives?: ParetoObjectives$Outbound | undefined;
  preset?: string | undefined;
};

/** @internal */
export const ParetoRouterPlugin$outboundSchema: z.ZodType<
  ParetoRouterPlugin$Outbound,
  ParetoRouterPlugin
> = z.object({
  ceilings: ParetoCeilings$outboundSchema.optional(),
  floors: ParetoFloors$outboundSchema.optional(),
  id: z.literal("pareto-router"),
  objectives: ParetoObjectives$outboundSchema.optional(),
  preset: Preset$outboundSchema.optional(),
});

export function paretoRouterPluginToJSON(
  paretoRouterPlugin: ParetoRouterPlugin,
): string {
  return JSON.stringify(
    ParetoRouterPlugin$outboundSchema.parse(paretoRouterPlugin),
  );
}
