/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a6bf464e166e
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  AgentCronSchedule,
  AgentCronSchedule$inboundSchema,
} from "./agentcronschedule.js";
import {
  AgentCustomCode,
  AgentCustomCode$inboundSchema,
} from "./agentcustomcode.js";
import { AgentTool, AgentTool$inboundSchema } from "./agenttool.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AgentConfig = {
  /**
   * Inline custom code executed at every step of the agent loop. Optional.
   */
  customCode?: AgentCustomCode | undefined;
  /**
   * Free-form natural-language instructions that always prepend the agent prompt. The chat-to-mutate endpoint rewrites this field as the conversation evolves.
   */
  instructions?: string | undefined;
  /**
   * Cron schedule. When unset, the agent runs only on explicit invocation.
   */
  schedule?: AgentCronSchedule | undefined;
  /**
   * Allowed tool list. Empty means the agent has no tools.
   */
  tools?: Array<AgentTool> | undefined;
};

/** @internal */
export const AgentConfig$inboundSchema: z.ZodType<AgentConfig, unknown> = z
  .object({
    custom_code: AgentCustomCode$inboundSchema.optional(),
    instructions: z.string().optional(),
    schedule: AgentCronSchedule$inboundSchema.optional(),
    tools: z.array(AgentTool$inboundSchema).optional(),
  }).transform((v) => {
    return remap$(v, {
      "custom_code": "customCode",
    });
  });

export function agentConfigFromJSON(
  jsonString: string,
): SafeParseResult<AgentConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentConfig' from JSON`,
  );
}
