/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: fc7ae48602c5
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A compaction item produced by server-side context compaction (`context_management`). The `encrypted_content` blob is only decryptable by the provider endpoint that produced it — echo the item back verbatim on subsequent requests and OpenRouter will route the conversation to that endpoint.
 */
export type OutputCompactionItem = {
  createdBy?: string | undefined;
  encryptedContent: string;
  id: string;
  type: "compaction";
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const OutputCompactionItem$inboundSchema: z.ZodType<
  OutputCompactionItem,
  unknown
> = collectExtraKeys$(
  z.object({
    created_by: z.string().optional(),
    encrypted_content: z.string(),
    id: z.string(),
    type: z.literal("compaction"),
  }).catchall(z.any()),
  "additionalProperties",
  true,
).transform((v) => {
  return remap$(v, {
    "created_by": "createdBy",
    "encrypted_content": "encryptedContent",
  });
});

export function outputCompactionItemFromJSON(
  jsonString: string,
): SafeParseResult<OutputCompactionItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OutputCompactionItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OutputCompactionItem' from JSON`,
  );
}
