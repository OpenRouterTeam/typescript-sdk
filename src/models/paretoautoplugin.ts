/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6e297ddd35a4
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";

export type ParetoAutoPlugin = {
  /**
   * Set to false to disable the pareto-auto plugin for this request. Defaults to true.
   */
  enabled?: boolean | undefined;
  id: "pareto-auto";
  /**
   * Price sensitivity between 0 and 1. 0 selects the highest-quality model regardless of price. 1 selects the cheapest model that meets the quality floor for the detected workload tier. Defaults to 0.3.
   */
  priceSensitivity?: number | undefined;
};

/** @internal */
export type ParetoAutoPlugin$Outbound = {
  enabled?: boolean | undefined;
  id: "pareto-auto";
  price_sensitivity?: number | undefined;
};

/** @internal */
export const ParetoAutoPlugin$outboundSchema: z.ZodType<
  ParetoAutoPlugin$Outbound,
  ParetoAutoPlugin
> = z.object({
  enabled: z.boolean().optional(),
  id: z.literal("pareto-auto"),
  priceSensitivity: z.number().optional(),
}).transform((v) => {
  return remap$(v, {
    priceSensitivity: "price_sensitivity",
  });
});

export function paretoAutoPluginToJSON(
  paretoAutoPlugin: ParetoAutoPlugin,
): string {
  return JSON.stringify(
    ParetoAutoPlugin$outboundSchema.parse(paretoAutoPlugin),
  );
}
