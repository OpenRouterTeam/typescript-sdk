/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ff3c776b9f81
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  OpenRouterFile,
  OpenRouterFile$inboundSchema,
} from "./openrouterfile.js";

/**
 * A page of files in the OpenRouter shape.
 */
export type OpenRouterFileList = {
  shape: "openrouter";
  /**
   * Opaque cursor for the next page; null when there are no more results.
   */
  cursor: string | null;
  data: Array<OpenRouterFile>;
  firstId: string | null;
  hasMore: boolean;
  lastId: string | null;
};

/** @internal */
export const OpenRouterFileList$inboundSchema: z.ZodType<
  OpenRouterFileList,
  unknown
> = z.object({
  _shape: z.literal("openrouter"),
  cursor: z.nullable(z.string()),
  data: z.array(OpenRouterFile$inboundSchema),
  first_id: z.nullable(z.string()),
  has_more: z.boolean(),
  last_id: z.nullable(z.string()),
}).transform((v) => {
  return remap$(v, {
    "_shape": "shape",
    "first_id": "firstId",
    "has_more": "hasMore",
    "last_id": "lastId",
  });
});

export function openRouterFileListFromJSON(
  jsonString: string,
): SafeParseResult<OpenRouterFileList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OpenRouterFileList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OpenRouterFileList' from JSON`,
  );
}
