/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e3853c83720d
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";

/**
 * The primary use-case category (chat, reasoning, code, creative, image, tts, stt, video, embedding)
 */
export const Task = {
  Chat: "chat",
  Reasoning: "reasoning",
  Code: "code",
  Creative: "creative",
  Image: "image",
  Tts: "tts",
  Stt: "stt",
  Video: "video",
  Embedding: "embedding",
} as const;
/**
 * The primary use-case category (chat, reasoning, code, creative, image, tts, stt, video, embedding)
 */
export type Task = OpenEnum<typeof Task>;

/**
 * The optimization axis for routing (fast, smart, latest, high, precise, cheap)
 */
export const Constraint = {
  Fast: "fast",
  Smart: "smart",
  Latest: "latest",
  High: "high",
  Precise: "precise",
  Cheap: "cheap",
} as const;
/**
 * The optimization axis for routing (fast, smart, latest, high, precise, cheap)
 */
export type Constraint = OpenEnum<typeof Constraint>;

/**
 * Optional quality/cost tier (free, standard, premium)
 */
export const Tier = {
  Free: "free",
  Standard: "standard",
  Premium: "premium",
} as const;
/**
 * Optional quality/cost tier (free, standard, premium)
 */
export type Tier = OpenEnum<typeof Tier>;

/**
 * The routing intent specifying task and optimization constraint
 */
export type Intent = {
  /**
   * The primary use-case category (chat, reasoning, code, creative, image, tts, stt, video, embedding)
   */
  task: Task;
  /**
   * The optimization axis for routing (fast, smart, latest, high, precise, cheap)
   */
  constraint: Constraint;
  /**
   * Optional quality/cost tier (free, standard, premium)
   */
  tier?: Tier | null | undefined;
};

/**
 * Filters to narrow the candidate model set
 */
export type AutoRouterPluginFilters = {
  /**
   * Provider restrictions (e.g. ["anthropic", "openai"])
   */
  provider?: Array<string> | undefined;
  /**
   * Model family restrictions (e.g. ["opus", "sonnet"])
   */
  family?: Array<string> | undefined;
  /**
   * Required capabilities (e.g. ["vision", "tool_use"])
   */
  capabilities?: Array<string> | undefined;
};

/**
 * The selection strategy to apply among filtered candidates (latency-first, quality-first, cost-first, balanced)
 */
export const Strategy = {
  LatencyFirst: "latency-first",
  QualityFirst: "quality-first",
  CostFirst: "cost-first",
  Balanced: "balanced",
} as const;
/**
 * The selection strategy to apply among filtered candidates (latency-first, quality-first, cost-first, balanced)
 */
export type Strategy = OpenEnum<typeof Strategy>;

/**
 * Composable routing intent configuration. Alternative to using composable slug syntax (e.g. auto/chat:fast). Currently used alongside slug-based intent resolution.
 */
export type Routing = {
  /**
   * The routing intent specifying task and optimization constraint
   */
  intent: Intent;
  /**
   * Filters to narrow the candidate model set
   */
  filters?: AutoRouterPluginFilters | undefined;
  /**
   * The selection strategy to apply among filtered candidates (latency-first, quality-first, cost-first, balanced)
   */
  strategy?: Strategy | null | undefined;
};

export type AutoRouterPlugin = {
  id: "auto-router";
  /**
   * Set to false to disable the auto-router plugin for this request. Defaults to true.
   */
  enabled?: boolean | undefined;
  /**
   * List of model patterns to filter which models the auto-router can route between. Supports wildcards (e.g., "anthropic/*" matches all Anthropic models). When not specified, uses the default supported models list.
   */
  allowedModels?: Array<string> | undefined;
  /**
   * Composable routing intent configuration. Alternative to using composable slug syntax (e.g. auto/chat:fast). Currently used alongside slug-based intent resolution.
   */
  routing?: Routing | undefined;
};

/** @internal */
export const Task$outboundSchema: z.ZodType<string, Task> = openEnums
  .outboundSchema(Task);

/** @internal */
export const Constraint$outboundSchema: z.ZodType<string, Constraint> =
  openEnums.outboundSchema(Constraint);

/** @internal */
export const Tier$outboundSchema: z.ZodType<string, Tier> = openEnums
  .outboundSchema(Tier);

/** @internal */
export type Intent$Outbound = {
  task: string;
  constraint: string;
  tier?: string | null | undefined;
};

/** @internal */
export const Intent$outboundSchema: z.ZodType<Intent$Outbound, Intent> = z
  .object({
    task: Task$outboundSchema,
    constraint: Constraint$outboundSchema,
    tier: z.nullable(Tier$outboundSchema).optional(),
  });

export function intentToJSON(intent: Intent): string {
  return JSON.stringify(Intent$outboundSchema.parse(intent));
}

/** @internal */
export type AutoRouterPluginFilters$Outbound = {
  provider?: Array<string> | undefined;
  family?: Array<string> | undefined;
  capabilities?: Array<string> | undefined;
};

/** @internal */
export const AutoRouterPluginFilters$outboundSchema: z.ZodType<
  AutoRouterPluginFilters$Outbound,
  AutoRouterPluginFilters
> = z.object({
  provider: z.array(z.string()).optional(),
  family: z.array(z.string()).optional(),
  capabilities: z.array(z.string()).optional(),
});

export function autoRouterPluginFiltersToJSON(
  autoRouterPluginFilters: AutoRouterPluginFilters,
): string {
  return JSON.stringify(
    AutoRouterPluginFilters$outboundSchema.parse(autoRouterPluginFilters),
  );
}

/** @internal */
export const Strategy$outboundSchema: z.ZodType<string, Strategy> = openEnums
  .outboundSchema(Strategy);

/** @internal */
export type Routing$Outbound = {
  intent: Intent$Outbound;
  filters?: AutoRouterPluginFilters$Outbound | undefined;
  strategy?: string | null | undefined;
};

/** @internal */
export const Routing$outboundSchema: z.ZodType<Routing$Outbound, Routing> = z
  .object({
    intent: z.lazy(() => Intent$outboundSchema),
    filters: z.lazy(() => AutoRouterPluginFilters$outboundSchema).optional(),
    strategy: z.nullable(Strategy$outboundSchema).optional(),
  });

export function routingToJSON(routing: Routing): string {
  return JSON.stringify(Routing$outboundSchema.parse(routing));
}

/** @internal */
export type AutoRouterPlugin$Outbound = {
  id: "auto-router";
  enabled?: boolean | undefined;
  allowed_models?: Array<string> | undefined;
  routing?: Routing$Outbound | undefined;
};

/** @internal */
export const AutoRouterPlugin$outboundSchema: z.ZodType<
  AutoRouterPlugin$Outbound,
  AutoRouterPlugin
> = z.object({
  id: z.literal("auto-router"),
  enabled: z.boolean().optional(),
  allowedModels: z.array(z.string()).optional(),
  routing: z.lazy(() => Routing$outboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    allowedModels: "allowed_models",
  });
});

export function autoRouterPluginToJSON(
  autoRouterPlugin: AutoRouterPlugin,
): string {
  return JSON.stringify(
    AutoRouterPlugin$outboundSchema.parse(autoRouterPlugin),
  );
}
