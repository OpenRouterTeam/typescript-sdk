/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: fdc0b3d6fc36
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Correctness = {
  model: string;
  notes: string;
  satisfiesSpec: boolean;
};

export type Defect = {
  defect: string;
  model: string;
};

/**
 * Action-oriented analysis the fusion judge produces in `coding` mode, tuned for multi-turn coding rather than prose synthesis.
 */
export type FusionCodingAnalysisResult = {
  approachDivergence: Array<string>;
  correctness: Array<Correctness>;
  defects: Array<Defect>;
  gaps: Array<string>;
  recommendedBasis: string;
};

/** @internal */
export const Correctness$inboundSchema: z.ZodType<Correctness, unknown> = z
  .object({
    model: z.string(),
    notes: z.string(),
    satisfies_spec: z.boolean(),
  }).transform((v) => {
    return remap$(v, {
      "satisfies_spec": "satisfiesSpec",
    });
  });

export function correctnessFromJSON(
  jsonString: string,
): SafeParseResult<Correctness, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Correctness$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Correctness' from JSON`,
  );
}

/** @internal */
export const Defect$inboundSchema: z.ZodType<Defect, unknown> = z.object({
  defect: z.string(),
  model: z.string(),
});

export function defectFromJSON(
  jsonString: string,
): SafeParseResult<Defect, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Defect$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Defect' from JSON`,
  );
}

/** @internal */
export const FusionCodingAnalysisResult$inboundSchema: z.ZodType<
  FusionCodingAnalysisResult,
  unknown
> = z.object({
  approach_divergence: z.array(z.string()),
  correctness: z.array(z.lazy(() => Correctness$inboundSchema)),
  defects: z.array(z.lazy(() => Defect$inboundSchema)),
  gaps: z.array(z.string()),
  recommended_basis: z.string(),
}).transform((v) => {
  return remap$(v, {
    "approach_divergence": "approachDivergence",
    "recommended_basis": "recommendedBasis",
  });
});

export function fusionCodingAnalysisResultFromJSON(
  jsonString: string,
): SafeParseResult<FusionCodingAnalysisResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FusionCodingAnalysisResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FusionCodingAnalysisResult' from JSON`,
  );
}
