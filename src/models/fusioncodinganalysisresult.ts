/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: fdc0b3d6fc36
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Correctness = {
  model: string;
  notes: string;
  satisfiesSpec: boolean;
};

export type Defect = {
  defect: string;
  model: string;
};

/**
 * Action-oriented analysis the fusion judge produces in `coding` mode, tuned for multi-turn coding rather than prose synthesis.
 */
export type FusionCodingAnalysisResult = {
  approachDivergence: Array<string>;
  correctness: Array<Correctness>;
  defects: Array<Defect>;
  gaps: Array<string>;
  recommendedBasis: string;
};

/** @internal */
export const Correctness$inboundSchema: z.ZodType<Correctness, unknown> = z
  .object({
    model: z.string(),
    notes: z.string(),
    satisfies_spec: z.boolean(),
  }).transform((v) => {
    return remap$(v, {
      "satisfies_spec": "satisfiesSpec",
    });
  });
/** @internal */
export type Correctness$Outbound = {
  model: string;
  notes: string;
  satisfies_spec: boolean;
};

/** @internal */
export const Correctness$outboundSchema: z.ZodType<
  Correctness$Outbound,
  Correctness
> = z.object({
  model: z.string(),
  notes: z.string(),
  satisfiesSpec: z.boolean(),
}).transform((v) => {
  return remap$(v, {
    satisfiesSpec: "satisfies_spec",
  });
});

export function correctnessToJSON(correctness: Correctness): string {
  return JSON.stringify(Correctness$outboundSchema.parse(correctness));
}
export function correctnessFromJSON(
  jsonString: string,
): SafeParseResult<Correctness, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Correctness$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Correctness' from JSON`,
  );
}

/** @internal */
export const Defect$inboundSchema: z.ZodType<Defect, unknown> = z.object({
  defect: z.string(),
  model: z.string(),
});
/** @internal */
export type Defect$Outbound = {
  defect: string;
  model: string;
};

/** @internal */
export const Defect$outboundSchema: z.ZodType<Defect$Outbound, Defect> = z
  .object({
    defect: z.string(),
    model: z.string(),
  });

export function defectToJSON(defect: Defect): string {
  return JSON.stringify(Defect$outboundSchema.parse(defect));
}
export function defectFromJSON(
  jsonString: string,
): SafeParseResult<Defect, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Defect$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Defect' from JSON`,
  );
}

/** @internal */
export const FusionCodingAnalysisResult$inboundSchema: z.ZodType<
  FusionCodingAnalysisResult,
  unknown
> = z.object({
  approach_divergence: z.array(z.string()),
  correctness: z.array(z.lazy(() => Correctness$inboundSchema)),
  defects: z.array(z.lazy(() => Defect$inboundSchema)),
  gaps: z.array(z.string()),
  recommended_basis: z.string(),
}).transform((v) => {
  return remap$(v, {
    "approach_divergence": "approachDivergence",
    "recommended_basis": "recommendedBasis",
  });
});
/** @internal */
export type FusionCodingAnalysisResult$Outbound = {
  approach_divergence: Array<string>;
  correctness: Array<Correctness$Outbound>;
  defects: Array<Defect$Outbound>;
  gaps: Array<string>;
  recommended_basis: string;
};

/** @internal */
export const FusionCodingAnalysisResult$outboundSchema: z.ZodType<
  FusionCodingAnalysisResult$Outbound,
  FusionCodingAnalysisResult
> = z.object({
  approachDivergence: z.array(z.string()),
  correctness: z.array(z.lazy(() => Correctness$outboundSchema)),
  defects: z.array(z.lazy(() => Defect$outboundSchema)),
  gaps: z.array(z.string()),
  recommendedBasis: z.string(),
}).transform((v) => {
  return remap$(v, {
    approachDivergence: "approach_divergence",
    recommendedBasis: "recommended_basis",
  });
});

export function fusionCodingAnalysisResultToJSON(
  fusionCodingAnalysisResult: FusionCodingAnalysisResult,
): string {
  return JSON.stringify(
    FusionCodingAnalysisResult$outboundSchema.parse(fusionCodingAnalysisResult),
  );
}
export function fusionCodingAnalysisResultFromJSON(
  jsonString: string,
): SafeParseResult<FusionCodingAnalysisResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FusionCodingAnalysisResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FusionCodingAnalysisResult' from JSON`,
  );
}
