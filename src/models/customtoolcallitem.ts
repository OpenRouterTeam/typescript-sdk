/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 84d0034c7065
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { ClosedEnum } from "../types/enums.js";
import {
  ToolCallStatus,
  ToolCallStatus$outboundSchema,
} from "./toolcallstatus.js";

export const CustomToolCallItemType = {
  CustomToolCall: "custom_tool_call",
} as const;
export type CustomToolCallItemType = ClosedEnum<typeof CustomToolCallItemType>;

/**
 * A call to a custom (freeform-grammar) tool created by the model — distinct from `function_call`. Used for tools like Codex CLI's `apply_patch` whose payload is opaque text rather than JSON arguments.
 */
export type CustomToolCallItem = {
  /**
   * True when the model called a tool declared with `async: true` and may continue its turn before the output is returned. Return the result in a later request as a `function_call_output` with this `call_id`.
   */
  async?: boolean | null | undefined;
  callId: string;
  id?: string | null | undefined;
  input: string;
  name: string;
  /**
   * Namespace qualifier for tools registered as part of a namespace tool group (e.g. an MCP server)
   */
  namespace?: string | null | undefined;
  status?: ToolCallStatus | null | undefined;
  type: CustomToolCallItemType;
};

/** @internal */
export const CustomToolCallItemType$outboundSchema: z.ZodEnum<
  typeof CustomToolCallItemType
> = z.enum(CustomToolCallItemType);

/** @internal */
export type CustomToolCallItem$Outbound = {
  async?: boolean | null | undefined;
  call_id: string;
  id?: string | null | undefined;
  input: string;
  name: string;
  namespace?: string | null | undefined;
  status?: string | null | undefined;
  type: string;
};

/** @internal */
export const CustomToolCallItem$outboundSchema: z.ZodType<
  CustomToolCallItem$Outbound,
  CustomToolCallItem
> = z.object({
  async: z.nullable(z.boolean()).optional(),
  callId: z.string(),
  id: z.nullable(z.string()).optional(),
  input: z.string(),
  name: z.string(),
  namespace: z.nullable(z.string()).optional(),
  status: z.nullable(ToolCallStatus$outboundSchema).optional(),
  type: CustomToolCallItemType$outboundSchema,
}).transform((v) => {
  return remap$(v, {
    callId: "call_id",
  });
});

export function customToolCallItemToJSON(
  customToolCallItem: CustomToolCallItem,
): string {
  return JSON.stringify(
    CustomToolCallItem$outboundSchema.parse(customToolCallItem),
  );
}
