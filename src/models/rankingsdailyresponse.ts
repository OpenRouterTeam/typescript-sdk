/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d4c3689c5ec7
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  RankingsDailyItem,
  RankingsDailyItem$inboundSchema,
} from "./rankingsdailyitem.js";
import {
  RankingsDailyMeta,
  RankingsDailyMeta$inboundSchema,
} from "./rankingsdailymeta.js";

export type RankingsDailyResponse = {
  /**
   * Rows are sorted by `date` ascending. Within each date the top-N entries are sorted by `total_tokens` descending, and the `other` row (if present) always appears last — after all top-N entries — regardless of its token count.
   */
  data: Array<RankingsDailyItem>;
  meta: RankingsDailyMeta;
};

/** @internal */
export const RankingsDailyResponse$inboundSchema: z.ZodType<
  RankingsDailyResponse,
  unknown
> = z.object({
  data: z.array(RankingsDailyItem$inboundSchema),
  meta: RankingsDailyMeta$inboundSchema,
});

export function rankingsDailyResponseFromJSON(
  jsonString: string,
): SafeParseResult<RankingsDailyResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RankingsDailyResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RankingsDailyResponse' from JSON`,
  );
}
