/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: fa2c53553900
 */

import * as z from "zod/v4";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const Alg = {
  Es256: "ES256",
} as const;
export type Alg = ClosedEnum<typeof Alg>;

export const Crv = {
  P256: "P-256",
} as const;
export type Crv = ClosedEnum<typeof Crv>;

export const Kty = {
  Ec: "EC",
} as const;
export type Kty = ClosedEnum<typeof Kty>;

export const Use = {
  Sig: "sig",
} as const;
export type Use = ClosedEnum<typeof Use>;

export type Key = {
  alg: Alg;
  crv: Crv;
  kid: string;
  kty: Kty;
  use: Use;
  x: string;
  y: string;
};

/**
 * RFC 7517 JWK Set of the keys OpenRouter signs access tokens with.
 */
export type OAuthJwks = {
  keys: Array<Key>;
};

/** @internal */
export const Alg$inboundSchema: z.ZodEnum<typeof Alg> = z.enum(Alg);

/** @internal */
export const Crv$inboundSchema: z.ZodEnum<typeof Crv> = z.enum(Crv);

/** @internal */
export const Kty$inboundSchema: z.ZodEnum<typeof Kty> = z.enum(Kty);

/** @internal */
export const Use$inboundSchema: z.ZodEnum<typeof Use> = z.enum(Use);

/** @internal */
export const Key$inboundSchema: z.ZodType<Key, unknown> = z.object({
  alg: Alg$inboundSchema,
  crv: Crv$inboundSchema,
  kid: z.string(),
  kty: Kty$inboundSchema,
  use: Use$inboundSchema,
  x: z.string(),
  y: z.string(),
});

export function keyFromJSON(
  jsonString: string,
): SafeParseResult<Key, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Key$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Key' from JSON`,
  );
}

/** @internal */
export const OAuthJwks$inboundSchema: z.ZodType<OAuthJwks, unknown> = z.object({
  keys: z.array(z.lazy(() => Key$inboundSchema)),
});

export function oAuthJwksFromJSON(
  jsonString: string,
): SafeParseResult<OAuthJwks, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OAuthJwks$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OAuthJwks' from JSON`,
  );
}
