# OutputDatetimeItemStatus

## Example Usage

```typescript
import { OutputDatetimeItemStatus } from "@openrouter/sdk/models";

let value: OutputDatetimeItemStatus = "completed";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"completed" | "in_progress" | "incomplete" | Unrecognized<string>
```