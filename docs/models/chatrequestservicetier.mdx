---
title: "ChatRequestServiceTier"
---

The service tier to use for processing this request. `fast` is accepted as an alias for `priority`.

## Example Usage

```typescript
import { ChatRequestServiceTier } from "@openrouter/sdk/models";

let value: ChatRequestServiceTier = "auto";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"auto" | "default" | "flex" | "priority" | "scale" | "fast" | Unrecognized<string>
```