# ResponsesRequestConstraint

The optimization axis for routing (fast, smart, latest, high, precise, cheap)

## Example Usage

```typescript
import { ResponsesRequestConstraint } from "@openrouter/sdk/models";

let value: ResponsesRequestConstraint = "fast";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"fast" | "smart" | "latest" | "high" | "precise" | "cheap" | Unrecognized<string>
```