---
title: "TTSPerfWorkload"
---

The generation workload these metrics are scoped to.

## Example Usage

```typescript
import { TTSPerfWorkload } from "@openrouter/sdk/models";

let value: TTSPerfWorkload = "embeddings";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"text_generation" | "image_generation" | "video_generation" | "embeddings" | "rerank" | "tts" | "stt" | "unknown" | Unrecognized<string>
```