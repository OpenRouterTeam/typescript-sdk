# CreateAudioSpeechResponse


## Supported Types

### `ReadableStream<Uint8Array>`

```typescript
const value: ReadableStream<Uint8Array> = bytesToStream(
  new TextEncoder().encode("<binary audio data>"),
);
```

### `ReadableStream<Uint8Array>`

```typescript
const value: ReadableStream<Uint8Array> = bytesToStream(
  new TextEncoder().encode("<binary audio data>"),
);
```

