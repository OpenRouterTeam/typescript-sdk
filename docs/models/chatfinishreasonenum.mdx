---
title: "ChatFinishReasonEnum"
---

Normalized finish reason. `pause_turn` is an OpenRouter extension: the provider paused a long-running server-tool turn awaiting continuation, so the partial output is not a completed answer.

## Example Usage

```typescript
import { ChatFinishReasonEnum } from "@openrouter/sdk/models";

let value: ChatFinishReasonEnum = "stop";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"tool_calls" | "stop" | "length" | "content_filter" | "error" | "pause_turn" | Unrecognized<string>
```