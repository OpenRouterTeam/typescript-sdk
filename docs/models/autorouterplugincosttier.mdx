---
title: "AutoRouterPluginCostTier"
---

Named cost/quality setting. Tiers select cost-percentile bands: low = [0, 20), medium = [20, 40), high = [40, 60), xhigh = [60, 80), and max = [80, 100]. Numeric cost_quality_tradeoff takes precedence and retains ceiling behavior.

## Example Usage

```typescript
import { AutoRouterPluginCostTier } from "@openrouter/sdk/models";

let value: AutoRouterPluginCostTier = "low";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"low" | "medium" | "high" | "xhigh" | "max" | Unrecognized<string>
```